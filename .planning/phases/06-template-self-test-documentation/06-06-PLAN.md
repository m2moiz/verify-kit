---
phase: 06-template-self-test-documentation
plan: "06-06"
plan_name: readme-and-arch-diagram
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05"]
files_modified:
  - README.md
  - docs/casts/just-verify.cast
  - docs/casts/just-verify.gif
  - docs/casts/record-cast.sh
  - docs/img/vscode-problems.png
autonomous: false
requirements: [DOC-01, DOC-05]
closes_beads: []
must_haves:
  truths:
    - "README opens quickstart-first (D-05): tagline → asciinema GIF → 1-line copier copy → 2-sentence why-this-exists → philosophy paragraph"
    - "README contains inline Mermaid `flowchart TD` block with Universal Foundation + 5 add-on slots (2 shipped, 3 deferred) — D-07 / DOC-05"
    - "README contains the dual-audience six-row checklist verbatim from REQUIREMENTS.md lines 11-18 — D-09"
    - "README documents the X-VerifyKit-Token + VERIFYKIT_AUTH_TOKEN auth scaffold (cross-references 06-02 contract)"
    - "README documents /summarize starter-grade defenses + OWASP LLM01 caveat sentence (cross-references 06-03 contract)"
    - "README documents /echo length cap + control-char strip (cross-references 06-04 contract)"
    - "README references CHANGELOG.md, CODE_OF_CONDUCT.md, SECURITY.md, CONTRIBUTING.md (06-07) — footer link targets exist (or 06-07 not yet landed — README plan must not invent paths)"
    - "docs/casts/just-verify.cast + docs/casts/just-verify.gif + docs/casts/record-cast.sh all exist (D-06 + §2 — GIF for README, .cast for reproducibility, script for re-recording)"
    - "docs/img/vscode-problems.png exists (D-08 — IDE Problems-panel screenshot) with caption naming the IDE"
    - "README does NOT contain `<script>` tags or asciinema.org JS embed (§2 — GitHub strips them); the GIF is the only embed mechanism"
    - "Mermaid block uses `flowchart TD` (modern syntax), classDef for shipped vs deferred, plain text subgraph labels (§6 pitfalls)"
  artifacts:
    - path: "README.md"
      provides: "Top-level project README per DOC-01"
      contains: "verify-kit"
      min_lines: 150
    - path: "docs/casts/just-verify.cast"
      provides: "asciinema source for reproducible re-render"
      contains: "asciicast"
    - path: "docs/casts/just-verify.gif"
      provides: "Rendered GIF embedded in README"
    - path: "docs/casts/record-cast.sh"
      provides: "Re-record script with deterministic --cols/--rows"
      contains: "asciinema rec"
    - path: "docs/img/vscode-problems.png"
      provides: "Dual-audience promise visual: human sees clickable IDE errors"
  key_links:
    - from: "README.md"
      to: "docs/casts/just-verify.gif"
      via: "Markdown image embed"
      pattern: "docs/casts/just-verify.gif"
    - from: "README.md Mermaid block"
      to: "research/00-architecture-overview.md source"
      via: "derived flowchart TD layout"
      pattern: "flowchart TD"
    - from: "README.md footer"
      to: "CODE_OF_CONDUCT.md, SECURITY.md, CHANGELOG.md, CONTRIBUTING.md"
      via: "Markdown links to repo-root files (existence verified at plan completion)"
      pattern: "CODE_OF_CONDUCT.md"
---

<objective>
Land the top-level README.md (DOC-01) with the quickstart-first structure (D-05), inline Mermaid architecture diagram (DOC-05 / D-07), asciinema GIF as headline visual (D-06), IDE Problems-panel screenshot (D-08), dual-audience six-row checklist (D-09), and cross-references to the hardening scaffolds (06-02/03/04) and release-please CHANGELOG (06-05).

Purpose: This is the project's public face. A new reader must go from `copier copy` to "I see my project verified" in under 30 seconds (UX-08 / ROADMAP SC2). The README is also the integration document for the hardening trio — it documents what 06-02/03/04 ship so a consumer using the scaffold understands the auth header + injection defenses.

Output: Top-level README.md, three docs/casts/ artifacts, one docs/img/ screenshot.
</objective>

<constraints>
- D-05: quickstart-first ordering — tagline → cast → 1-line copier copy → 2-sentence why → philosophy.
- D-06: asciinema cast IS the headline visual; §2 mandates **GIF embed** (not `<script>`; not asciinema.org URL) because GitHub strips JS. Ship `.cast` source + `.gif` render + `record-cast.sh` script per §2 recipe.
- D-07: architecture diagram is inline Mermaid (no separate file). Source: research/00-architecture-overview.md.
- D-08: IDE Problems-panel PNG screenshot in `docs/img/`; caption names the IDE.
- D-09: dual-audience six-row checklist lives in README as its own section (~50 lines).
- D-14: PR template (06-07) duplicates the six rows verbatim; this plan emits the canonical text from REQUIREMENTS.md lines 11-18.
- §2 Pitfalls: always pass `--cols/--rows` to asciinema rec; always commit `.cast` alongside `.gif` (reproducibility); never use `<script>` embed; `agg` is the .cast → .gif converter.
- §6 Pitfalls: `flowchart TD` not `graph TD`; no `<br/>` inside subgraph labels; no hardcoded hex colors that fail in dark mode; use classDef for shipped vs deferred.
- REVIEW-CHECKLIST §3 (cross-plan contract): this README MUST NOT re-assert producer contracts — it documents what 06-02/03/04/05 produce. Quote producer artifact names exactly (e.g., `X-VerifyKit-Token`, `VERIFYKIT_AUTH_TOKEN`, `_INJECTION_MARKERS` reference if appropriate). Do NOT invent path-gating shape or symbol names.
- REVIEW-CHECKLIST §1: no cwd-relative paths in any embedded shell snippet — quickstart `copier copy` command uses the literal `gh:m2moiz/verify-kit` GitHub form.
- REVIEW-CHECKLIST §6: README is a public document, not a meta-comment dump. No "cycle-N" / "per Codex" references.
- This plan is `autonomous: false` because Task 5 (recording the asciinema cast) requires running `just verify` on a real scratch project + visual confirmation that the cast renders cleanly in GitHub's README preview — that's a checkpoint:human-verify gate per the workflow's `human_verify_mode` setting. The PNG screenshot capture (Task 6) is similarly a manual capture step.
</constraints>

<context>
@.planning/phases/06-template-self-test-documentation/06-CONTEXT.md
@.planning/phases/06-template-self-test-documentation/06-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/REVIEW-CHECKLIST.md
@.planning/PROJECT.md
@research/00-architecture-overview.md
@.planning/phases/06-template-self-test-documentation/06-01-SUMMARY.md
@.planning/phases/06-template-self-test-documentation/06-02-SUMMARY.md
@.planning/phases/06-template-self-test-documentation/06-03-SUMMARY.md
@.planning/phases/06-template-self-test-documentation/06-04-SUMMARY.md
@.planning/phases/06-template-self-test-documentation/06-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft README.md skeleton (above-the-fold + philosophy + add-on inventory)</name>
  <files>README.md</files>
  <read_first>
    - .planning/PROJECT.md (tagline + core value language)
    - .planning/REQUIREMENTS.md (full v0.1 scope; reference for add-on inventory table)
    - .planning/phases/06-template-self-test-documentation/06-RESEARCH.md (§5 README skeleton)
  </read_first>
  <action>
    Create top-level `README.md` with the following sections in this exact order (D-05):

    1. **Title + tagline**: `# verify-kit` + one-line tagline ("A Copier-based scaffold where `just verify` is the ground truth — both human and coding agent verify the same way.").
    2. **Headline visual**: `![just verify demo](docs/casts/just-verify.gif)` — created in Task 5.
    3. **One-line quickstart**: triple-backtick bash block with `copier copy --trust gh:m2moiz/verify-kit my-project && cd my-project && just verify`. The `--trust` flag is REQUIRED — `copier.yml` declares `_jinja_extensions` (and the template may declare `_tasks` in future), both of which Copier 9.15+ refuses to render without `--trust`. The CI matrix (06-08) uses the same flag; quickstart MUST match.
    4. **2-sentence why-this-exists hook**: from PROJECT.md "Core Value" paragraph, condensed.
    5. **Philosophy paragraph** (3-5 sentences): half-blind agent + dual-audience promise.
    6. **Add-on inventory table**: 5 rows (Universal, Backend, LLM, Web v0.2, Audio v0.2, Game v0.2) with columns (Copier prompt | Generated files | Shipped in). Universal = always; Backend = `has_backend=true`; LLM = `has_llm=true` + `llm_backend` choice; v0.2 rows marked "Deferred".

    Per REVIEW-CHECKLIST §6: no meta-comments. Per §1: the quickstart command is the literal GitHub form (no cwd-relative paths).
  </action>
  <verify>
    <automated>test -f README.md && grep -q "^# verify-kit" README.md && grep -q "docs/casts/just-verify.gif" README.md && grep -q "copier copy --trust gh:m2moiz/verify-kit" README.md && grep -q "Add-on" README.md && wc -l README.md | awk '{exit ($1>=80)?0:1}'</automated>
  </verify>
  <done>README opening sections (1-6) present in declared order; quickstart command uses literal gh: form; add-on table covers all 5 rows.</done>
</task>

<task type="auto">
  <name>Task 2: Append Mermaid architecture diagram block (DOC-05)</name>
  <files>README.md</files>
  <read_first>
    - research/00-architecture-overview.md (source for layered shape)
    - .planning/phases/06-template-self-test-documentation/06-RESEARCH.md (§6 — canonical Mermaid embed)
  </read_first>
  <action>
    Append a `## Architecture` H2 section under the add-on inventory, containing the §6 canonical Mermaid block verbatim:
    - `flowchart TD` (not `graph TD`)
    - Subgraph L0 "Layer 0 — Universal Foundation" with nodes: copier, mise, verify, harness, mcp, agents, otel
    - L0 → solid arrows to Backend, LLM (shipped); L0 -.-> dashed to Web, Audio, Game (deferred)
    - Subgraph addons "Add-on slots" containing Backend, LLM, Web, Audio, Game
    - Backend -.composes.- LLM dashed labelled edge
    - `classDef shipped fill:#7CFFCB,stroke:#0a4,color:#000;` and `classDef deferred fill:#eee,stroke:#999,color:#666,stroke-dasharray: 5 5;`
    - `class Backend,LLM shipped;` and `class Web,Audio,Game deferred;`

    Per §6 Pitfalls: no `<br/>` inside subgraph labels (plain text only); `<br/>` is permitted inside node labels.
    Per D-07: this Mermaid block is the SINGLE SOURCE OF TRUTH for DOC-05 — no separate diagram file. The block lives inside README.md.

    Verify the rendered Mermaid is syntactically valid by:
    `python -c "import re; t=open('README.md').read(); m=re.search(r'\`\`\`mermaid\n(.*?)\`\`\`', t, re.DOTALL); assert m and 'flowchart TD' in m.group(1) and 'classDef shipped' in m.group(1) and 'classDef deferred' in m.group(1)"`
  </action>
  <verify>
    <automated>python -c "
import re
t = open('README.md').read()
m = re.search(r'\`\`\`mermaid\n(.*?)\`\`\`', t, re.DOTALL)
assert m, 'mermaid fenced block missing'
body = m.group(1)
assert 'flowchart TD' in body, 'flowchart TD required (not graph TD)'
assert 'classDef shipped' in body and 'classDef deferred' in body
assert 'Backend' in body and 'LLM' in body and 'Web' in body and 'Audio' in body and 'Game' in body
assert 'Universal Foundation' in body
print('OK')
"</automated>
  </verify>
  <done>Mermaid block present with flowchart TD + all 5 add-on slots + shipped/deferred classDefs.</done>
</task>

<task type="auto">
  <name>Task 3: Append dual-audience six-row checklist + auth/defenses sections + footer links</name>
  <files>README.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 11-18 — canonical six rows)
    - .planning/phases/06-template-self-test-documentation/06-02-SUMMARY.md (auth contract)
    - .planning/phases/06-template-self-test-documentation/06-03-SUMMARY.md (summarize defenses + OWASP LLM01 caveat)
    - .planning/phases/06-template-self-test-documentation/06-04-SUMMARY.md (echo hardening)
    - .planning/phases/06-template-self-test-documentation/06-01-SUMMARY.md (CoC, SECURITY, LICENSE paths)
    - .planning/phases/06-template-self-test-documentation/06-05-SUMMARY.md (CHANGELOG path + commit contract pointer)
    - .planning/phases/06-template-self-test-documentation/06-RESEARCH.md (§9 verbatim six rows)
  </read_first>
  <action>
    Append five more H2 sections to README.md:

    **(a) `## Dual-audience checklist`** — quote the six rows from REQUIREMENTS.md lines 11-18 / §9 VERBATIM. Each row as a bullet with bold header (e.g., `- **Human in terminal sees:** ...`). Add a 2-sentence intro explaining that every shipped feature satisfies all six. ~50 lines per D-09.

    **(b) `## Security` section** — three subsections:
    - `### Route authentication (X-VerifyKit-Token)` — explains the auth scaffold from 06-02: header name, env var `VERIFYKIT_AUTH_TOKEN`, dev-mode fallback when env=dev + token unset, `/healthz` excluded, 503 vs 401 distinction. ~15 lines.
    - `### Input defenses on /summarize` — four bullets per 06-03 contract (length cap 5000, control-char strip, Content-Type via Pydantic default, injection-marker denylist with 3 markers). Include the §4 OWASP LLM01 caveat sentence VERBATIM: "verify-kit's `/summarize` ships starter-grade input defenses... Consumers handling untrusted input should layer additional defenses... See OWASP LLM01."
    - `### Input hardening on /echo` — three bullets per 06-04 contract (length cap, control-char strip, Content-Type). Explicitly note that the injection denylist is intentionally NOT applied to /echo because /echo does not call an LLM (§4 guidance).

    **(c) `## Updating an existing project`** — `copier update` walkthrough (ROADMAP SC2(d)). Cover:
    - The exact command: `copier update` (run from inside an existing scaffolded project).
    - What happens: Copier reads the project's `.copier-answers.yml`, fetches the latest verify-kit template version, runs a three-way merge against the project's local edits.
    - When to expect breaking changes: any release entry with a non-empty "Breaking changes for consumers" callout in CHANGELOG.md (link to 06-05's CHANGELOG anchor). The callout names what to do (re-answer a renamed prompt, delete a deprecated file, etc.).
    - Conflict resolution: Copier leaves merge conflicts as `.rej` files; resolve by hand, re-run `just verify` to confirm. ~15 lines.

    **(d) `## Troubleshooting`** — three common-failure recipes (ROADMAP SC2(e)). Each is a `### Symptom: ...` H3 + 2-3 line fix:
    - `### Symptom: just verify fails on a fresh scaffold` — Confirm `mise install` ran; re-run with `just verify --debug` for full output; check `.verify/report.json` for the first failing check name.
    - `### Symptom: copier update creates merge conflicts` — Expected when local edits diverge from template; resolve `.rej` files, re-run `just verify`.
    - `### Symptom: 401 from /summarize or /__debug/*` — Auth token missing; set `VERIFYKIT_AUTH_TOKEN` in `.env` and re-issue request with `-H "X-VerifyKit-Token: $VERIFYKIT_AUTH_TOKEN"` (link to Security H2 above). ~20 lines.

    **(e) `## Releases & contributing` footer** — links to:
    - `[CHANGELOG.md](CHANGELOG.md)` (from 06-05)
    - `[CONTRIBUTING.md](CONTRIBUTING.md)` (from 06-07, W4 sibling — at plan completion, verify the link target exists by checking the file presence; if 06-07 hasn't landed yet, this Task's verify step is allowed to skip this one assertion)
    - `[CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)` (from 06-01)
    - `[SECURITY.md](SECURITY.md)` (from 06-01)
    - `[LICENSE](LICENSE)` (from 06-01)

    Per REVIEW-CHECKLIST §3: the auth/defenses sections REFERENCE producer contracts; they MUST NOT re-define them. Symbol names (`X-VerifyKit-Token`, `VERIFYKIT_AUTH_TOKEN`, etc.) are quoted from 06-02 SUMMARY; the OWASP LLM01 caveat sentence is quoted from 06-03 SUMMARY's "documentation contract" addendum.
  </action>
  <verify>
    <automated>python -c "
import os, re
r = open('README.md').read()
# Dual-audience checklist (six rows verbatim)
assert 'Dual-audience checklist' in r
for phrase in ['Human in terminal sees', 'Human in VS Code sees', 'Agent calling programmatically', 'Agent has a fix path', 'Human can override agent', 'Both can collaborate']:
    assert phrase in r, f'Six-row checklist row missing: {phrase}'
# Auth section references 06-02 contract verbatim
assert 'X-VerifyKit-Token' in r and 'VERIFYKIT_AUTH_TOKEN' in r
# Defenses section references 06-03/04 contract
assert 'OWASP LLM01' in r or 'OWASP' in r
assert '/summarize' in r and '/echo' in r
# copier update walkthrough + troubleshooting (ROADMAP SC2(d)+(e))
assert 'Updating an existing project' in r and 'copier update' in r and '.copier-answers.yml' in r
assert 'Troubleshooting' in r
for sym in ['just verify fails on a fresh scaffold', 'copier update creates merge conflicts', '401 from /summarize']:
    assert sym in r, f'Troubleshooting symptom missing: {sym}'
# Footer links exist
for link in ['CHANGELOG.md', 'CODE_OF_CONDUCT.md', 'SECURITY.md', 'LICENSE']:
    assert link in r, f'Footer link {link} missing'
    # File existence (skip CONTRIBUTING.md — 06-07 sibling may not have landed yet)
    if link != 'CONTRIBUTING.md':
        assert os.path.exists(link), f'Footer references {link} but file missing'
print('OK')
"</automated>
  </verify>
  <done>Dual-audience six rows verbatim; Security H2 with auth + defenses subsections; copier-update walkthrough + troubleshooting recipes present (ROADMAP SC2(d)+(e)); footer links to all existing 06-01/05 files.</done>
</task>

<task type="auto">
  <name>Task 4: Add docs/casts/record-cast.sh + docs/casts/.gitkeep scaffolding</name>
  <files>docs/casts/record-cast.sh</files>
  <read_first>
    - .planning/phases/06-template-self-test-documentation/06-RESEARCH.md (§2 — deterministic recording recipe)
  </read_first>
  <action>
    Create `docs/casts/record-cast.sh` with the §2 deterministic recording recipe:
    ```bash
    #!/usr/bin/env bash
    # Re-record docs/casts/just-verify.cast and re-render docs/casts/just-verify.gif.
    # Required tools: asciinema, agg (install: `brew install asciinema agg`).
    set -euo pipefail
    HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    asciinema rec \
      --overwrite \
      --cols 100 --rows 28 \
      --idle-time-limit 1.5 \
      --title "just verify — first run on a fresh copier copy" \
      "${HERE}/just-verify.cast"
    agg --cols 100 --rows 28 --font-size 14 \
      "${HERE}/just-verify.cast" "${HERE}/just-verify.gif"
    echo "Re-recorded: ${HERE}/just-verify.{cast,gif}"
    ```

    `chmod +x docs/casts/record-cast.sh`.

    Per REVIEW-CHECKLIST §1: the script uses `"${HERE}"` (absolute path resolved at script-start), not relative paths — re-recording from any cwd works.
    Per §2 Pitfalls: always pass `--cols/--rows` (deterministic), always idle-time-limit 1.5.
  </action>
  <verify>
    <automated>test -x docs/casts/record-cast.sh && bash -n docs/casts/record-cast.sh && grep -q "cols 100 --rows 28" docs/casts/record-cast.sh && grep -q "idle-time-limit 1.5" docs/casts/record-cast.sh && grep -q "agg" docs/casts/record-cast.sh</automated>
  </verify>
  <done>record-cast.sh is executable, syntactically valid bash, contains the §2 deterministic flags.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Record asciinema cast + render GIF + visual-verify in README preview</name>
  <what-built>
    `docs/casts/record-cast.sh` is the executable script from Task 4. The cast itself can only be recorded interactively — it captures `just verify` running on a fresh `copier copy` scratch project. Then `agg` converts the .cast to .gif, and the GIF must be visually inspected for cleanliness (font rendering, padding, ANSI colors, length).
  </what-built>
  <how-to-verify>
    1. Install tooling if absent: `brew install asciinema agg`.
    2. Run: `bash docs/casts/record-cast.sh` — this opens asciinema. Inside the recording session:
       a. `cd /tmp && rm -rf cast-demo`
       b. `copier copy --trust --defaults gh:m2moiz/verify-kit cast-demo` (or use local path)
       c. `cd cast-demo && just verify`
       d. Wait for verify to complete (expect green ✓ for each check).
       e. Press Ctrl-D to end recording.
    3. The script auto-runs `agg` to produce `docs/casts/just-verify.gif`.
    4. Open `docs/casts/just-verify.gif` in Preview and confirm: (a) font is readable at 100 cols × 28 rows, (b) ANSI colors render, (c) total length is ≤30s (idle-compression worked), (d) no flicker or cutoffs.
    5. Push branch + open the PR in GitHub's web UI; confirm the GIF renders inline in the README preview (GitHub's `<img>` rewrite occasionally misbehaves for very large GIFs).
    6. If anything fails: re-record with the script (it overwrites the cast).
  </how-to-verify>
  <resume-signal>Type "approved" once the GIF renders cleanly in GitHub's README preview, or describe issues (e.g., "GIF too large, re-record with smaller --cols").</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Capture docs/img/vscode-problems.png screenshot</name>
  <what-built>
    Nothing automated for this task — the PNG must be captured manually (D-08).
  </what-built>
  <how-to-verify>
    1. Open a scratch verify-kit project in VS Code (or Cursor — pick one IDE; caption in README will name it).
    2. Introduce a Ruff lint error and a pyright type error (e.g., unused import + `x: int = "string"`).
    3. Run `just verify` from the integrated terminal so problem-matchers populate the Problems panel.
    4. Open the Problems panel (Cmd+Shift+M on mac).
    5. Take a screenshot of just that panel + a slice of the source file with the squiggle visible. Save as `docs/img/vscode-problems.png` (create `docs/img/` dir if absent).
    6. Add a caption to the README right under the screenshot: `*VS Code Problems panel showing clickable Ruff + pyright errors via verify-kit's custom problem matchers.*` (or "Cursor" if that was the IDE used — pick one).
    7. Verify the screenshot is committed: `test -f docs/img/vscode-problems.png && grep -q "docs/img/vscode-problems.png" README.md`.
  </how-to-verify>
  <resume-signal>Type "approved" when docs/img/vscode-problems.png is captured and embedded in README with caption naming the IDE.</resume-signal>
</task>

</tasks>

<contract>
**This plan does NOT produce contracts for downstream plans** — it is a consumer of 06-01/02/03/04/05 producer contracts.

**One outbound contract**: README.md exists at repo root and links to the OSS files from 06-01 + CHANGELOG.md from 06-05 + CONTRIBUTING.md from 06-07 (W4 sibling). The footer must keep these link targets stable — if 06-07 renames CONTRIBUTING.md, this README must be updated.
</contract>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → public GitHub readers | Public document; no untrusted input |
| docs/casts/just-verify.gif → README embed | GIF is a binary artifact; checked into repo + reproducible from .cast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Information Disclosure | Asciinema cast accidentally captures secrets (`.env` content shown in terminal during recording) | mitigate | Task 5 checkpoint requires human review of the cast before commit; record-cast.sh recipe uses a fresh scratch project with no secrets |
| T-06-18 | Tampering | README footer link targets drift when 06-07 hasn't landed yet | mitigate | Task 3 verify step checks file existence for all linked files EXCEPT CONTRIBUTING.md (deferred to 06-07 W4 sibling) |
</threat_model>

<completion_checklist>
- `test -f README.md && wc -l README.md | awk '{exit ($1>=150)?0:1}'` — README ≥150 lines
- `grep -q "flowchart TD" README.md && grep -q "classDef shipped" README.md` — Mermaid block present
- All six dual-audience rows present (verified by Task 3 grep loop)
- `test -f docs/casts/just-verify.cast docs/casts/just-verify.gif docs/casts/record-cast.sh` — cast trio shipped
- `test -f docs/img/vscode-problems.png` — IDE screenshot shipped
- `test -x docs/casts/record-cast.sh && bash -n docs/casts/record-cast.sh` — script executable + valid
- `! grep -q "<script" README.md` — §2 forbidden embed not present
- All footer link targets exist (CHANGELOG.md, LICENSE, CODE_OF_CONDUCT.md, SECURITY.md — CONTRIBUTING.md conditional on 06-07)
</completion_checklist>

<commit_strategy>
Three atomic commits (post-checkpoint):
1. `docs(readme): land top-level README with quickstart, Mermaid arch, dual-audience checklist (DOC-01, DOC-05)` — README.md + docs/casts/record-cast.sh
2. `docs(readme): add asciinema cast + rendered GIF (D-06)` — docs/casts/just-verify.{cast,gif} (after Task 5 checkpoint)
3. `docs(readme): add VS Code Problems-panel screenshot (D-08)` — docs/img/vscode-problems.png (after Task 6 checkpoint)
</commit_strategy>

<output>
Create `.planning/phases/06-template-self-test-documentation/06-06-SUMMARY.md` recording:
- Final README structure (table of H2 sections in order)
- Recorded cast metadata (cols/rows/idle-limit/duration)
- IDE used in screenshot (VS Code vs Cursor)
- Confirmation that all producer references (06-01/02/03/04/05) resolved cleanly
</output>
