---
phase: 06-template-self-test-documentation
plan: "06-01"
plan_name: oss-boilerplate
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - CODE_OF_CONDUCT.md
  - SECURITY.md
  - .github/ISSUE_TEMPLATE/bug.yml
  - .github/ISSUE_TEMPLATE/feature.yml
  - .github/ISSUE_TEMPLATE/config.yml
autonomous: true
requirements: [DOC-01]
closes_beads: []
must_haves:
  truths:
    - "Repo root contains LICENSE (MIT), CODE_OF_CONDUCT.md (Contributor Covenant 2.1 verbatim with email substituted), SECURITY.md"
    - ".github/ISSUE_TEMPLATE/ contains bug.yml + feature.yml + config.yml (YAML form templates, NOT markdown)"
    - "CoC attribution footer is preserved (CC-BY-4.0 requirement); placeholder [INSERT CONTACT METHOD] is replaced with m.moiz1995@gmail.com"
    - "SECURITY.md points to GitHub private security advisories URL (https://github.com/m2moiz/verify-kit/security/advisories/new) as the primary disclosure channel"
    - "config.yml sets blank_issues_enabled: false and links security reports to GHSA"
    - "These files are repo-root artifacts (NOT inside template/) — they describe the verify-kit project itself, not consumer projects"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license at repo root (community-profile checker discovery)"
      contains: "MIT License"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant 2.1 verbatim"
      contains: "Contributor Covenant"
      min_lines: 90
    - path: "SECURITY.md"
      provides: "Disclosure policy + private-advisory URL"
      contains: "security advisories"
    - path: ".github/ISSUE_TEMPLATE/bug.yml"
      provides: "Structured YAML form for bug reports"
      contains: "verify-kit version"
    - path: ".github/ISSUE_TEMPLATE/feature.yml"
      provides: "Structured YAML form for feature requests"
      contains: "Problem statement"
    - path: ".github/ISSUE_TEMPLATE/config.yml"
      provides: "Blank-issue disabling + security link"
      contains: "blank_issues_enabled: false"
  key_links:
    - from: "CODE_OF_CONDUCT.md enforcement section"
      to: "operator email m.moiz1995@gmail.com"
      via: "verbatim CoC 2.1 with placeholder substituted"
      pattern: "m\\.moiz1995@gmail\\.com"
    - from: ".github/ISSUE_TEMPLATE/config.yml security contact"
      to: "GitHub private security advisories"
      via: "contact_links url"
      pattern: "security/advisories/new"
---

<objective>
Land the OSS-launch boilerplate at the repo root (per D-15): LICENSE (MIT), CODE_OF_CONDUCT.md (Contributor Covenant 2.1), SECURITY.md, and `.github/ISSUE_TEMPLATE/{bug,feature,config}.yml` as YAML form templates.

Purpose: These files unblock README links (06-06 footer references CoC + SECURITY.md), satisfy GitHub's community-profile checker, and route bug reports / vulnerability disclosure to the operator's chosen channels. This plan is W1 (no dependencies) so it can land first.

Output: Six new files at canonical OSS locations.
</objective>

<constraints>
- D-15: LICENSE (MIT) at root; CODE_OF_CONDUCT.md is Contributor Covenant 2.1; ISSUE_TEMPLATE contains bug + feature.
- §7 Research: CoC 2.1 canonical source = https://www.contributor-covenant.org/version/2/1/code_of_conduct/code_of_conduct.md; substitute `[INSERT CONTACT METHOD]` → `m.moiz1995@gmail.com`; preserve CC-BY-4.0 attribution footer.
- §8 Research: ISSUE_TEMPLATE/* must be YAML form templates (`.yml`), NOT legacy markdown. Include config.yml that disables blank issues and links security to GHSA.
- These files describe the verify-kit project itself; they live at repo root (NOT under `template/`). Do NOT path-gate via Copier prompts — they are not consumer-generated artifacts.
- LICENSE already exists at `template/LICENSE.jinja2` for consumer projects; the repo-root LICENSE is a NEW file describing the verify-kit OSS project license (also MIT, but distinct purpose).
</constraints>

<context>
@.planning/phases/06-template-self-test-documentation/06-CONTEXT.md
@.planning/phases/06-template-self-test-documentation/06-RESEARCH.md
@.planning/REVIEW-CHECKLIST.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land LICENSE + CODE_OF_CONDUCT.md + SECURITY.md at repo root</name>
  <files>LICENSE, CODE_OF_CONDUCT.md, SECURITY.md</files>
  <read_first>
    - .planning/phases/06-template-self-test-documentation/06-RESEARCH.md (§7 Contributor Covenant 2.1)
    - template/LICENSE.jinja2 (existing consumer-facing template — repo-root LICENSE will have the same MIT text but reference verify-kit itself, not the consumer's project)
  </read_first>
  <action>
    Fetch the canonical Contributor Covenant 2.1 markdown source via `curl -fsSL https://www.contributor-covenant.org/version/2/1/code_of_conduct/code_of_conduct.md` (or copy verbatim from the cited URL). Verify in §7 that the placeholder `[INSERT CONTACT METHOD]` exists in the source; substitute it with `m.moiz1995@gmail.com`. Preserve the CC-BY-4.0 attribution footer at the bottom (required by license).

    Create `LICENSE` at repo root with standard MIT text, copyright line `Copyright (c) 2026 Moiz Hussain`, project name "verify-kit". Do NOT use the consumer-template version (template/LICENSE.jinja2); this is a separate file.

    Create `SECURITY.md` at repo root with:
    - "## Supported versions" — table noting only the latest minor (pre-1.0 each release is the only supported line).
    - "## Reporting a vulnerability" — directs reporters to https://github.com/m2moiz/verify-kit/security/advisories/new for private disclosure; states the operator typically responds within 7 days; lists email m.moiz1995@gmail.com as a fallback for reporters without a GitHub account.
    - Explicit non-goal: this is a solo-dev portfolio project; there is no SLA. Per CONTEXT.md Deferred Ideas, full GHSA workflow is post-v0.1.

    Per REVIEW-CHECKLIST §1 there are no paths in this plan that could leak cwd (these are static markdown files at known absolute locations).
  </action>
  <verify>
    <automated>test -f LICENSE && test -f CODE_OF_CONDUCT.md && test -f SECURITY.md && grep -q "MIT License" LICENSE && grep -q "Contributor Covenant" CODE_OF_CONDUCT.md && grep -q "m.moiz1995@gmail.com" CODE_OF_CONDUCT.md && grep -q "security/advisories/new" SECURITY.md && ! grep -q "INSERT CONTACT METHOD" CODE_OF_CONDUCT.md && grep -c "CC BY 4.0\|Creative Commons" CODE_OF_CONDUCT.md | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>LICENSE, CODE_OF_CONDUCT.md, SECURITY.md exist at repo root with the substitutions documented above; CoC attribution preserved; SECURITY.md routes disclosure to GHSA URL.</done>
</task>

<task type="auto">
  <name>Task 2: Land .github/ISSUE_TEMPLATE/{bug.yml,feature.yml,config.yml}</name>
  <files>.github/ISSUE_TEMPLATE/bug.yml, .github/ISSUE_TEMPLATE/feature.yml, .github/ISSUE_TEMPLATE/config.yml</files>
  <read_first>
    - .planning/phases/06-template-self-test-documentation/06-RESEARCH.md (§8 — canonical YAML form templates)
  </read_first>
  <action>
    Create the three files exactly as documented in 06-RESEARCH.md §8.

    `bug.yml`: form with required fields — verify-kit version (input), addons enabled (multi-select dropdown: none/has_backend/has_llm/has_logfire/has_fastapi_mcp), repro steps (textarea), logs (textarea, render: shell), OS/runner (input). Labels: `[bug, triage]`. Title prefix: `"[Bug]: "`.

    `feature.yml`: form with sections — problem statement (textarea, required), proposed solution (textarea, required), alternatives considered (textarea, optional), willingness-to-implement (checkboxes). Labels: `[enhancement, triage]`. Title prefix: `"[Feature]: "`.

    `config.yml`:
    ```yaml
    blank_issues_enabled: false
    contact_links:
      - name: Security vulnerability
        url: https://github.com/m2moiz/verify-kit/security/advisories/new
        about: Use private security advisories for vulnerability reports.
    ```

    Verify YAML parses with `python -c "import yaml; yaml.safe_load(open('.github/ISSUE_TEMPLATE/bug.yml'))"` (and the other two). Per REVIEW-CHECKLIST §5 (YAML inline conditionals), there are no Jinja conditionals here — these are static YAML.
  </action>
  <verify>
    <automated>python -c "import yaml,sys; [yaml.safe_load(open(f)) for f in ['.github/ISSUE_TEMPLATE/bug.yml','.github/ISSUE_TEMPLATE/feature.yml','.github/ISSUE_TEMPLATE/config.yml']]; print('OK')" && grep -q "blank_issues_enabled: false" .github/ISSUE_TEMPLATE/config.yml && grep -q "security/advisories/new" .github/ISSUE_TEMPLATE/config.yml && grep -q "verify-kit version" .github/ISSUE_TEMPLATE/bug.yml && grep -q "Problem statement\|problem-statement" .github/ISSUE_TEMPLATE/feature.yml</automated>
  </verify>
  <done>All three YAML files parse cleanly; config.yml disables blank issues and routes security; bug.yml + feature.yml define structured form fields.</done>
</task>

</tasks>

<contract>
Downstream plans (06-06 README, 06-07 PR template) MUST link to these exact paths:
- `LICENSE` (repo root)
- `CODE_OF_CONDUCT.md` (repo root)
- `SECURITY.md` (repo root)
- `.github/ISSUE_TEMPLATE/` (referenced by GitHub's issue-chooser UI)

If 06-06 or 06-07 need a different filename or location, this plan owns the source-of-truth and must be revised first.
</contract>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public OSS consumer → SECURITY.md disclosure channel | SECURITY.md is the documented public path; channel is GitHub private advisories (encrypted server-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | SECURITY.md leaks operator email to scraper bots | accept | Email is already public on GitHub profile + commits; CoC also lists it; risk pre-exists |
| T-06-02 | Repudiation | CoC attribution stripped accidentally during edits | mitigate | T1 verify step greps for "CC BY 4.0\|Creative Commons" footer presence |
</threat_model>

<completion_checklist>
- `test -f LICENSE CODE_OF_CONDUCT.md SECURITY.md .github/ISSUE_TEMPLATE/bug.yml .github/ISSUE_TEMPLATE/feature.yml .github/ISSUE_TEMPLATE/config.yml`
- `python -c "import yaml; [yaml.safe_load(open(f)) for f in ['.github/ISSUE_TEMPLATE/bug.yml','.github/ISSUE_TEMPLATE/feature.yml','.github/ISSUE_TEMPLATE/config.yml']]"`
- `! grep -q "INSERT CONTACT METHOD" CODE_OF_CONDUCT.md`
- `grep -q "m.moiz1995@gmail.com" CODE_OF_CONDUCT.md`
- `grep -q "security/advisories/new" SECURITY.md .github/ISSUE_TEMPLATE/config.yml`
</completion_checklist>

<commit_strategy>
Single atomic commit:
```
ALLOW_AI_WORKFLOW_FILES=1 git add LICENSE CODE_OF_CONDUCT.md SECURITY.md .github/ISSUE_TEMPLATE/
git commit -m "chore(oss): add LICENSE, Contributor Covenant 2.1, SECURITY.md, issue templates

Lands D-15 OSS boilerplate at repo root. CoC 2.1 verbatim with email
substituted; YAML form issue templates per §8 research; config.yml
disables blank issues and links security disclosure to GHSA.
"
```
</commit_strategy>

<output>
Create `.planning/phases/06-template-self-test-documentation/06-01-SUMMARY.md` recording:
- Exact paths shipped
- CoC version pinned (2.1)
- Email + GHSA URL used (for future audit if either changes)
</output>
