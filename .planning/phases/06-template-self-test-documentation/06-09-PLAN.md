---
phase: 06-template-self-test-documentation
plan: "06-09"
plan_name: llm-readme-pass
type: execute
wave: 6
depends_on: ["06-06"]
files_modified:
  - template/README.md.jinja2
autonomous: false
requirements: []
closes_beads: [verify-kit-1v6]
must_haves:
  truths:
    - "Phase 5 05-05 README LLM section (template/README.md.jinja2, inside {% if has_llm %} block) has been human-read end-to-end for prose quality, accuracy against landed code, and contradictions with the top-level README from 06-06"
    - "Any prose changes go into template/README.md.jinja2 — this is the CONSUMER-FACING README; the top-level README from 06-06 is a DIFFERENT file describing verify-kit itself"
    - "Specific checks: (a) D-04/D-06/D-08/D-17/D-18 content still accurate, (b) Pitfall 5 cost_usd=0.0 asymmetry still documented, (c) Pitfall 8 Basic-Auth idiom (not Bearer) still correct, (d) pydantic-ai consumer pattern (D-21) still matches landed code, (e) `just eval` (not `just verify --check=eval`) still the migration-verification command per D-15"
    - "Bead verify-kit-1v6 closed with either an edit commit OR a 'no changes needed' SUMMARY note (Open Question 5: either deliverable is valid per planner discretion)"
  artifacts:
    - path: "template/README.md.jinja2"
      provides: "Reviewed Phase 5 LLM section (edits OR no-op confirmation)"
  key_links:
    - from: "template/README.md.jinja2 {% if has_llm %} block"
      to: "06-09-SUMMARY.md prose-review notes"
      via: "human read pass"
      pattern: "Langfuse"
---

<objective>
Human-read pass on the Phase 5 LLM section of the CONSUMER-FACING README (`template/README.md.jinja2`) to close bead verify-kit-1v6. Deliverable is either (a) a commit that edits the section to fix prose/accuracy issues, OR (b) a SUMMARY note confirming "read, no changes needed" (Open Question 5 — both are valid per planner discretion).

Purpose: The Phase 5 05-05 README LLM section was authored under TDD-style verify-step pressure and could have rough edges that a human read catches but automated tests don't. This is the only Phase 6 plan whose deliverable is fundamentally a prose-quality judgment.

Output: Possibly an edit to `template/README.md.jinja2`; definitely a 06-09-SUMMARY.md recording the read pass.
</objective>

<constraints>
- D-16: closes bead verify-kit-1v6 (Phase 5 README LLM section human-read pass).
- D-17: dedicated plan per bead.
- §Open Question 5: deliverable shape is planner discretion — edit commit OR no-op SUMMARY note. Both acceptable as long as bead closes with audit evidence.
- This plan operates on `template/README.md.jinja2` (CONSUMER-FACING, rendered into scratch projects) — NOT the top-level `README.md` from 06-06 (which describes verify-kit itself). Do not confuse the two. The Phase 5 LLM section was appended to `template/README.md.jinja2` in plan 05-05 Task 3.
- 06-06 (top-level README) is a sibling — this plan reads it for cross-reference but does NOT edit it.
- Phase 5 polarity test (tests/test_phase05_polarity.py) MUST still pass after any edits — that test grep-asserts the §3 keywords in the consumer README (Langfuse / ~/.zshrc / Hetzner / pydantic_ai / Basic / etc.). Edits that remove or rephrase those keywords would break the polarity test.
- REVIEW-CHECKLIST §6: no meta-comments in the rendered template. The Phase 5 §3 inline references (D-04, D-06, etc.) are permanent reference, not meta.
- This plan is `autonomous: false` because the deliverable is a human judgment call: reading prose and deciding whether it reads cleanly to a non-expert consumer.
</constraints>

<context>
@.planning/phases/06-template-self-test-documentation/06-CONTEXT.md
@.planning/phases/05-llm-add-on/05-CONTEXT.md
@.planning/phases/05-llm-add-on/05-05-PLAN.md
@.planning/phases/05-llm-add-on/05-05-SUMMARY.md
@.planning/REVIEW-CHECKLIST.md
@template/README.md.jinja2
@tests/test_phase05_polarity.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-read pass of the {% if has_llm %} block in template/README.md.jinja2</name>
  <what-built>
    Nothing built yet — this task IS the human review.
  </what-built>
  <how-to-verify>
    1. Render a scratch project with LLM enabled:
       `cd /tmp && rm -rf scratch-06-09 && copier copy --trust --defaults --data has_backend=true --data has_llm=true /Users/moiz/Documents/code/verify-kit /tmp/scratch-06-09`
    2. Open `/tmp/scratch-06-09/README.md` in your preferred markdown previewer (or `gh markdown-preview` or VS Code's preview).
    3. Read the entire LLM section end-to-end. Pay attention to:
       - **Voice**: Does it read as a consumer-facing setup guide, or does it leak verify-kit-internal jargon?
       - **Accuracy against landed code**:
         - `from harness.llm import call_llm, llm_call, cost_budget` — confirm these are the actual exports in `template/harness/{% if has_llm %}llm.py{% endif %}.jinja2` (note the Jinja path-gating — the file's literal path on disk includes the `{% if has_llm %}llm.py{% endif %}` conditional segment per Phase 5 path-gating contract; `template/harness/llm.py.jinja2` does NOT exist).
         - The `pydantic_ai.Agent("anthropic:claude-haiku-4-5", output_type=str)` snippet — confirm pydantic-ai's v1.100+ API still uses `output_type=`.
         - `cost_usd=0.0` asymmetry note — still factually true for claude-agent-sdk routing path per Pitfall 5.
         - `OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic $(echo -n ... | base64)` — Basic, not Bearer (Pitfall 8). Operator can run this locally to confirm the encoding produces a valid header.
         - `just eval` (not `just verify --check=eval`) as the migration-verification command — confirm `justfile.jinja2` still defines `eval` recipe.
       - **Contradictions with 06-06 top-level README**: does the consumer-facing migration story conflict with anything the top-level README says? (Unlikely — they describe different things — but check.)
       - **Typos, broken markdown, code-block fence mismatches, dangling links.**
    4. If issues found → edit `template/README.md.jinja2` to fix them. Re-render and re-read. Verify `uv run pytest tests/test_phase05_polarity.py -v` still passes (the polarity test grep-asserts the structural keywords — your edits must preserve them).
    5. If NO issues found → skip the edit step. Record the read in 06-09-SUMMARY.md as "read end-to-end on YYYY-MM-DD, no changes required; bead verify-kit-1v6 closed as no-op".
    6. In either case: `bd close verify-kit-1v6 --reason="..."` with appropriate reason text.
  </how-to-verify>
  <resume-signal>Type "approved-edited" (if edits made) or "approved-noop" (if no changes needed); either way state the bead closure reason text.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Confirm Phase 5 polarity test still passes (regression guard)</name>
  <files></files>
  <action>
    Whether Task 1 produced edits or not, run the Phase 5 polarity test to confirm no regression:
    `uv run pytest tests/test_phase05_polarity.py -v`

    This test grep-asserts the structural keywords in the rendered consumer README (Langfuse, ~/.zshrc, Hetzner, pydantic_ai, Agent(, output_type, Basic, OTEL_EXPORTER_OTLP_HEADERS, cost_usd=0|$0|0.0, just eval). If any edit removed or renamed those keywords, the test fires here.

    If the test fails: STOP, do not commit. Re-edit template/README.md.jinja2 to restore the asserted keywords (the assertions encode Phase 5 contract — edits that break them are scope-violating).
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase05_polarity.py -v</automated>
  </verify>
  <done>Phase 5 polarity test green; no regression introduced by Task 1.</done>
</task>

</tasks>

<contract>
**This plan produces no downstream contracts** — it closes a bead. The artifact (edit or no-op SUMMARY note) is the audit-trail.
</contract>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 5 consumer README → consumer reading | Prose quality affects consumer's setup success but no security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-24 | Tampering | Edit removes a keyword asserted by Phase 5 polarity test | mitigate | Task 2 runs the polarity test post-edit; failure blocks commit |
</threat_model>

<completion_checklist>
- Task 1 checkpoint resolved with `approved-edited` or `approved-noop`
- `uv run pytest tests/test_phase05_polarity.py -v` passes
- `bd update verify-kit-1v6 --status=in_progress` at plan start
- `bd close verify-kit-1v6 --reason="<edited|no-op> per 06-09 human-read pass on YYYY-MM-DD"` at plan end
- 06-09-SUMMARY.md records: (a) date of read, (b) issues found (if any), (c) final disposition (edited / no-op)
</completion_checklist>

<commit_strategy>
Conditional:
- If edits made: `docs(template-readme): clarify LLM section after human-read pass (verify-kit-1v6)` — single commit with the edit
- If no-op: no code commit; `.planning/phases/06-template-self-test-documentation/06-09-SUMMARY.md` is committed via `ALLOW_AI_WORKFLOW_FILES=1 git commit -m "docs(phase-6): close verify-kit-1v6 (LLM README read pass — no changes needed)"`
</commit_strategy>

<output>
Create `.planning/phases/06-template-self-test-documentation/06-09-SUMMARY.md` recording:
- Date of read pass
- Bullets of any issues found + fixes applied (or "no issues found")
- Confirmation Phase 5 polarity test passes
- bead verify-kit-1v6 close confirmation
</output>
