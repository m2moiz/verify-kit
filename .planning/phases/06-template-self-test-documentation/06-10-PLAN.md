---
phase: 06-template-self-test-documentation
plan: "06-10"
plan_name: phase4-audit-ceremonies
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-backend-fastapi-add-on/04-SECURITY.md
  - .planning/phases/04-backend-fastapi-add-on/04-VALIDATION.md
  - .planning/STATE.md
autonomous: false
requirements: []
closes_beads: []
must_haves:
  truths:
    - "/gsd:secure-phase 4 ceremony invoked; output reconciled with existing 04-SECURITY.md (which currently shows status: verified, threats_total: 14, threats_closed: 14)"
    - "/gsd:validate-phase 4 ceremony invoked; output reconciled with existing 04-VALIDATION.md (435 lines, Nyquist coverage)"
    - "STATE.md 'Todos' section has both '[ ] Phase 4 secure-phase (optional follow-up; threat model unaudited)' and '[ ] Phase 4 validate-phase (optional follow-up; Nyquist coverage unaudited)' flipped to [x] with date + audit-trail row"
    - "Each ceremony either short-circuits (threats_open == 0 / coverage complete) OR surfaces new gaps; new gaps filed as beads (NOT rolled into Phase 6) per §10 guidance"
    - "Updated 04-SECURITY.md + 04-VALIDATION.md include a new audit-trail row noting the Phase 6 re-run date"
  artifacts:
    - path: ".planning/phases/04-backend-fastapi-add-on/04-SECURITY.md"
      provides: "Updated Phase 4 security audit with Phase 6 re-run row"
    - path: ".planning/phases/04-backend-fastapi-add-on/04-VALIDATION.md"
      provides: "Updated Phase 4 validation audit with Phase 6 re-run row"
    - path: ".planning/STATE.md"
      provides: "Todos section updated"
  key_links:
    - from: ".planning/STATE.md Todos"
      to: "04-SECURITY.md + 04-VALIDATION.md audit-trail rows"
      via: "ceremony re-run dates match"
      pattern: "Phase 4 secure-phase"
---

<objective>
Close the two deferred Phase 4 audit-ceremony todos: invoke `/gsd:secure-phase 4` and `/gsd:validate-phase 4`, reconcile their output with the already-on-disk `04-SECURITY.md` + `04-VALIDATION.md`, file any newly-surfaced gaps as beads (NOT rolled into Phase 6), update STATE.md.

Purpose: STATE.md still shows both as `[ ]` because they were authored during Phase 4 execution but never formally re-audited via the ceremony workflows. Per §10 the audit files already exist and likely short-circuit (04-SECURITY.md says `status: verified, threats_open: 0, auditor: gsd:secure-phase`). This plan captures the no-op (or non-no-op) as evidence so the deferred-audit checkbox closes cleanly before v0.1 ships.

Output: Updated 04-SECURITY.md + 04-VALIDATION.md (audit-trail row added) + STATE.md todos flipped.
</objective>

<constraints>
- D-16: closes 2 deferred Phase 4 audits.
- D-17: dedicated plan per audit ceremony (combined here into one PLAN with two tasks per §10 lightweight-shape guidance — each ceremony is 1 invocation + 1 reconciliation, not enough work to justify two separate plans).
- §10 Research: DO NOT delete the existing 04-SECURITY.md / 04-VALIDATION.md before re-running; ceremonies are designed to update-in-place. Short-circuit expected: secure-phase if `threats_open == 0`; validate-phase if all requirements COVERED.
- §10 Research: if re-audit surfaces NEW threats or NEW gaps, those become beads — NOT in scope for Phase 6.
- This plan is `autonomous: false` because invoking `/gsd:secure-phase` and `/gsd:validate-phase` are user-driven slash commands; this plan documents WHEN/HOW the operator runs them and captures the output.
- REVIEW-CHECKLIST §1: STATE.md / 04-SECURITY.md / 04-VALIDATION.md are absolute paths inside `.planning/`; no cwd leak.
- ALLOW_AI_WORKFLOW_FILES=1 required for commits inside `.planning/`.
</constraints>

<context>
@.planning/STATE.md
@.planning/phases/04-backend-fastapi-add-on/04-SECURITY.md
@.planning/phases/04-backend-fastapi-add-on/04-VALIDATION.md
@.planning/phases/04-backend-fastapi-add-on/04-VERIFICATION.md
@.planning/phases/06-template-self-test-documentation/06-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Invoke /gsd:secure-phase 4 + reconcile output</name>
  <what-built>
    Nothing automated — this is a workflow invocation.
  </what-built>
  <how-to-verify>
    1. Run `/gsd:secure-phase 4` in this Claude Code session (or a fresh session if context budget is tight).
    2. Capture the workflow's output. Per §10, expect short-circuit: existing 04-SECURITY.md frontmatter says `status: verified, threats_total: 14, threats_closed: 14, auditor: gsd:secure-phase` — the ceremony should detect `threats_open == 0` and exit cleanly with "no-op: prior audit still valid".
    3. If short-circuit confirmed: append an audit-trail row to 04-SECURITY.md noting:
       ```markdown
       | Re-audit (Phase 6 closure) | 2026-MM-DD | gsd:secure-phase short-circuited: threats_open == 0; prior audit still valid |
       ```
       (Use the appropriate audit-trail table format that already exists in 04-SECURITY.md — read it first.)
    4. If NOT short-circuit (workflow surfaces new threats):
       - For each new threat, file a Beads issue: `bd create --title="[Phase 4 retroactive threat] T-04-XX <name>" --description="Surfaced by /gsd:secure-phase 4 re-run on YYYY-MM-DD during Phase 6 closure. STRIDE category: X. Component: Y. Disposition recommendation: <mitigate|accept|transfer>." --type=task --priority=1`
       - DO NOT roll the fix into Phase 6 — per §10 these become independent post-v0.1 work (or get folded into v0.2 if appropriate).
       - Update 04-SECURITY.md with the new threats appended to the register; update frontmatter `threats_open` accordingly.
    5. Commit the updated 04-SECURITY.md.
  </how-to-verify>
  <resume-signal>Type "approved-short-circuit" or "approved-with-new-beads:bd-id-1,bd-id-2,..." after reconciling.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Invoke /gsd:validate-phase 4 + reconcile output</name>
  <what-built>
    Nothing automated — this is a workflow invocation.
  </what-built>
  <how-to-verify>
    1. Run `/gsd:validate-phase 4` in this Claude Code session.
    2. Capture the workflow's output. Per §10, expect short-circuit since 04-VALIDATION.md (435 lines) already documents Nyquist coverage and Phase 4 verify-work already gap-closed (per STATE.md `Phase 4 verify-work (gaps closed in 59d3ae5)`).
    3. If short-circuit: append an audit-trail row to 04-VALIDATION.md:
       ```markdown
       | Re-audit (Phase 6 closure) | 2026-MM-DD | gsd:validate-phase short-circuited: requirements all COVERED; prior coverage still valid |
       ```
    4. If NEW gaps surface:
       - File each as a Beads issue with appropriate priority.
       - Update 04-VALIDATION.md with the new gaps appended.
       - DO NOT generate the missing tests in Phase 6 (per §10) — that's post-v0.1 work.
    5. Commit the updated 04-VALIDATION.md.
  </how-to-verify>
  <resume-signal>Type "approved-short-circuit" or "approved-with-new-beads:bd-id-1,..." after reconciling.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Flip STATE.md todos + commit</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (find the Todos section)
  </read_first>
  <action>
    Edit `.planning/STATE.md` Todos section:
    - Change `- [ ] Phase 4 secure-phase (optional follow-up; threat model unaudited)` → `- [x] Phase 4 secure-phase (re-run YYYY-MM-DD per 06-10; <short-circuit|N new beads filed>)`.
    - Change `- [ ] Phase 4 validate-phase (optional follow-up; Nyquist coverage unaudited)` → `- [x] Phase 4 validate-phase (re-run YYYY-MM-DD per 06-10; <short-circuit|N new beads filed>)`.

    Update `last_updated` and `last_activity` in frontmatter to today's date.

    Per REVIEW-CHECKLIST §1: STATE.md path is .planning/STATE.md (absolute under repo root).
    Commit with `ALLOW_AI_WORKFLOW_FILES=1 git commit -m "docs(state): close Phase 4 secure-phase + validate-phase deferred audits (per 06-10)"`.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] Phase 4 secure-phase" .planning/STATE.md | awk '{exit ($1>=1)?0:1}' && grep -c "^- \[x\] Phase 4 validate-phase" .planning/STATE.md | awk '{exit ($1>=1)?0:1}' && ! grep -q "^- \[ \] Phase 4 secure-phase" .planning/STATE.md && ! grep -q "^- \[ \] Phase 4 validate-phase" .planning/STATE.md</automated>
  </verify>
  <done>Both Phase 4 audit todos flipped to [x]; STATE.md committed.</done>
</task>

</tasks>

<contract>
**This plan produces no downstream contracts in Phase 6** — it closes deferred-audit todos. Any new beads filed during reconciliation become independent post-v0.1 work tracked in Beads.
</contract>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GSD workflow → 04-SECURITY.md / 04-VALIDATION.md edits | Workflow is trusted (operator-invoked); writes are in-repo + git-tracked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-25 | Repudiation | STATE.md todo flipped to [x] without actually invoking the ceremony | mitigate | Checkpoint tasks require operator to type approved-* signal; SUMMARY records the workflow invocation date + reconciliation outcome |
</threat_model>

<completion_checklist>
- Both checkpoints (Task 1, Task 2) resolved
- `grep -E "^- \[x\] Phase 4 (secure|validate)-phase" .planning/STATE.md | wc -l` returns 2
- `04-SECURITY.md` and `04-VALIDATION.md` each have a new audit-trail row dated today
- If new beads filed, they appear in `bd list --status=open`
- 06-10-SUMMARY.md records: ceremony output for each task, any new beads created, reconciliation decisions
</completion_checklist>

<commit_strategy>
Up to three atomic commits (depending on outcomes):
1. `docs(phase-4): re-audit secure-phase per 06-10 (<short-circuit|N new threats>)` — 04-SECURITY.md change
2. `docs(phase-4): re-audit validate-phase per 06-10 (<short-circuit|N new gaps>)` — 04-VALIDATION.md change
3. `docs(state): close Phase 4 deferred-audit todos (per 06-10)` — STATE.md change

All require `ALLOW_AI_WORKFLOW_FILES=1`.
</commit_strategy>

<output>
Create `.planning/phases/06-template-self-test-documentation/06-10-SUMMARY.md` recording:
- Date both ceremonies were invoked
- Short-circuit vs surfaced-new-gaps outcome per ceremony
- List of new beads filed (if any) with IDs
- STATE.md flip confirmation
</output>
