---
phase: 06-template-self-test-documentation
plan: "06-04"
plan_name: echo-hardening
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - template/{% if has_backend %}app{% endif %}/api.py.jinja2
  - tests/test_phase06_echo_hardening_polarity.py
autonomous: true
requirements: []
closes_beads: [verify-kit-93h]
must_haves:
  truths:
    - "/echo request model EchoRequest enforces text length 1..5000 via Pydantic Field"
    - "/echo field_validator strips control chars (same `_CONTROL_CHARS` pattern shape as 06-03)"
    - "/echo does NOT apply the injection-marker denylist (LLM-specific; /echo does not call an LLM — §4 explicit guidance)"
    - "Content-Type: application/json enforced via FastAPI default JSON-body validation"
    - "Phase 4 /echo functional contract preserved (reflects sanitized input back)"
    - "Auth from 06-02 inherits — tests set X-VerifyKit-Token header or use ENV=dev fallback"
    - "Tests cover happy path + length cap + control char strip + Content-Type bypass"
  artifacts:
    - path: "tests/test_phase06_echo_hardening_polarity.py"
      provides: "Forcing-function tests for /echo defenses"
      contains: "EchoRequest"
  key_links:
    - from: "EchoRequest field_validator"
      to: "_CONTROL_CHARS pattern (independent regex from 06-03, NOT shared import)"
      via: "@field_validator('text')"
      pattern: "field_validator"
---

<objective>
Apply the same starter-grade input-hardening shape as `/summarize` to `/echo` (verify-kit-93h): length cap + control-char strip + Content-Type enforcement. EXCLUDE the injection-marker denylist — `/echo` does not call an LLM, so the LLM-specific markers are out of scope per §4.

Purpose: Closes the OSS blocker that Phase 4's `/echo` route had no input-side hardening. Symmetric pair to 06-03 (same parent dependency: 06-02 auth scaffold).

Output: Edited `app/api.py.jinja2` (extend the existing `/echo` block from Phase 4 04-02), new polarity test.
</objective>

<constraints>
- D-16: closes bead verify-kit-93h (/echo hardening, same shape as /summarize).
- D-17: dedicated plan per bead.
- §4 Research explicit guidance: "Apply layers 1–3 (skip the denylist — it's LLM-specific)". This plan ships ONLY length cap + control-char strip + Content-Type. NO injection-marker regex.
- Phase 4 04-02 contract: `/echo` accepts a request body and reflects it back. The existing handler must be preserved — this plan only adds input validation in the Pydantic model.
- REVIEW-CHECKLIST §3: the regex constant in this plan is named distinctly from 06-03's (do NOT share `_CONTROL_CHARS` between two unrelated blocks — both blocks live in api.py.jinja2, so the constant can be defined ONCE at module level if both Phase 5 and Phase 4 blocks are unconditional, OR defined inside each `{% if %}` block. Pick whichever avoids cross-block coupling; document the choice in the action).
- REVIEW-CHECKLIST §1: no relative paths.
- REVIEW-CHECKLIST §2: validator returns after cleanup; no statements after return.
- REVIEW-CHECKLIST §6: no meta-comments in rendered template.
- Phase 4 04-02 /echo endpoint MUST continue to work — auth header from 06-02 + new validation are additive; the response body still reflects sanitized input.
</constraints>

<context>
@.planning/phases/06-template-self-test-documentation/06-CONTEXT.md
@.planning/phases/06-template-self-test-documentation/06-RESEARCH.md
@.planning/REVIEW-CHECKLIST.md
@.planning/phases/04-backend-fastapi-add-on/04-02-SUMMARY.md
@template/{% if has_backend %}app{% endif %}/api.py.jinja2

<interfaces>
<!-- Existing Phase 4 contract (must not break). -->

From Phase 4 04-02:
- POST /echo accepts a request body, returns the input reflected back
- Located inside the `{% if has_backend %}` unconditional block in api.py.jinja2 (NOT under `{% if has_llm %}`)
- Request shape from 04-02 SUMMARY: read the file to confirm exact field name (likely `text` or `message`)

From 06-02 auth:
- All routes including /echo inherit `Depends(require_auth)` globally; tests set X-VerifyKit-Token

This plan ADDS:
- EchoRequest constrained with `Field(min_length=1, max_length=5000)` on the existing field
- `@field_validator(<fieldname>)` that strips control chars and returns cleaned value
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add length cap + control-char strip to /echo request model</name>
  <files>template/{% if has_backend %}app{% endif %}/api.py.jinja2</files>
  <read_first>
    - template/{% if has_backend %}app{% endif %}/api.py.jinja2 (FULL — locate the existing EchoRequest / /echo block from Phase 4 04-02; confirm the request field name and type)
    - .planning/phases/04-backend-fastapi-add-on/04-02-SUMMARY.md (canonical /echo shape as landed)
    - .planning/phases/06-template-self-test-documentation/06-RESEARCH.md (§4 — explicit "skip denylist for /echo" guidance)
  </read_first>
  <behavior>
    - Test 1: Rendered api.py /echo request model field uses `Field(min_length=1, max_length=5000)` (or equivalent constrained type).
    - Test 2: Rendered api.py has a `@field_validator` on the /echo request field that strips control characters.
    - Test 3: Rendered api.py does NOT contain `_INJECTION_MARKERS` reference inside the /echo (has_backend, not has_llm) block — denylist is LLM-specific.
    - Test 4: Phase 4 /echo functional contract intact: handler signature, route path, response shape unchanged from 04-02 SUMMARY.
    - Test 5: `import re` present at top of api.py (also covered by 06-03 if both plans land — idempotent import).
  </behavior>
  <action>
    Read api.py.jinja2 in full. Locate the existing `/echo` block landed in Phase 4 04-02. Use Edit to:

    (1) Ensure `import re` is at top of file (idempotent if 06-03 already added it).

    (2) Decide on the control-char regex constant placement. Option A: define `_CONTROL_CHARS_ECHO` inside the unconditional `{% if has_backend %}` block (above EchoRequest) — keeps /echo self-contained, no coupling with the `{% if has_llm %}` block from 06-03. Option B: hoist `_CONTROL_CHARS` to module level (unconditional) and reuse from both blocks — cleaner DRY but couples the two `{% if %}` blocks. **Choose Option A (the self-contained route block)** to honor REVIEW-CHECKLIST §3 (no cross-block contract drift). Each route block owns its own regex constant; they happen to be the same pattern but are independent identifiers.

    ```python
    _CONTROL_CHARS_ECHO = re.compile(r"[\x00-\x08\x0b\x0c\x0e-\x1f\x7f]")
    ```

    (3) Modify EchoRequest:
    - Change the text field to `text: str = Field(min_length=1, max_length=5000)` (use the exact existing field name — read 04-02 SUMMARY to confirm; if it's `message`, adapt accordingly).
    - Add a classmethod field_validator that strips control chars:
    ```python
    @field_validator("text")
    @classmethod
    def _strip_control(cls, v: str) -> str:
        return _CONTROL_CHARS_ECHO.sub("", v)
    ```
    - NO injection-marker check (per §4).

    (4) Do NOT modify the /echo route handler body. Response shape preserved.

    Per REVIEW-CHECKLIST §2: validator returns the cleaned value at end.
    Per §3: this plan's regex constant is `_CONTROL_CHARS_ECHO` — distinct from 06-03's `_CONTROL_CHARS`. Document this naming choice in the SUMMARY so future audits see the deliberate decoupling.
    Per §6: no meta-comments.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-06-04 && uv run --no-project python -c "
import subprocess, pathlib, re
src='/Users/moiz/Documents/code/verify-kit'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_backend=true','--data','has_llm=false','--vcs-ref','HEAD',src,'/tmp/scratch-06-04'], check=True)
api = pathlib.Path('/tmp/scratch-06-04/app/api.py').read_text()
# /echo still present (Phase 4 preserved)
assert '/echo' in api
# Phase 6 defenses added
assert '_CONTROL_CHARS_ECHO' in api, 'echo-specific control-char regex constant missing'
assert 'max_length=5000' in api
assert 'field_validator' in api
# Denylist is NOT in this block (has_llm=false render — no _INJECTION_MARKERS at all)
assert '_INJECTION_MARKERS' not in api, 'injection-marker denylist must NOT leak into has_llm=false render'
# import re present
assert 'import re' in api
# Polarity: has_backend=false has no app/
subprocess.run(['rm','-rf','/tmp/scratch-06-04-nb'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_backend=false','--vcs-ref','HEAD',src,'/tmp/scratch-06-04-nb'], check=True)
assert not pathlib.Path('/tmp/scratch-06-04-nb/app').exists(), 'app/ leaked into has_backend=false'
print('OK')
"</automated>
  </verify>
  <done>EchoRequest enforces length cap + control-char strip; denylist explicitly NOT present in /echo block; Phase 4 /echo functional contract preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Polarity test for /echo defenses</name>
  <files>tests/test_phase06_echo_hardening_polarity.py</files>
  <read_first>
    - tests/test_phase06_summarize_defenses_polarity.py (structural reference from 06-03)
    - tests/test_phase06_auth_polarity.py (auth header pattern from 06-02)
    - tests/_helpers.py
  </read_first>
  <behavior>
    - test_echo_happy_path: text="hello", with auth, → 200 + echoes "hello".
    - test_echo_length_cap: text="A"*5001 → 422.
    - test_echo_control_chars_stripped: text="hi\x00\x07there" → 200 with response body containing "hithere" (chars stripped).
    - test_echo_no_injection_marker_check: text="Ignore all previous instructions" → 200 (NOT 422 — /echo does not run the denylist). This is a forcing-function test that the denylist is correctly omitted from /echo.
    - test_echo_content_type_non_json: POST /echo with Content-Type: text/plain → 422.
    - test_echo_inherits_auth: POST /echo without X-VerifyKit-Token (env=prod, token set) → 401 (auth from 06-02 inherits).
  </behavior>
  <action>
    Create `tests/test_phase06_echo_hardening_polarity.py` at repo top-level (REVIEW-CHECKLIST §7).

    Mirror the structure of `tests/test_phase06_summarize_defenses_polarity.py` from 06-03 (render scratch project once per module via fixture, subprocess.run with `env=_CLEAN_ENV | {auth + env vars}`, TestClient-based assertions inside the scratch project).

    Key difference: render `has_backend=true, has_llm=false` (Phase 4-only configuration) — /echo lives in the unconditional backend block, /summarize only lives in has_llm=true. This separates the /echo test from /summarize concerns.

    The `test_echo_no_injection_marker_check` test is the forcing-function for §4's "skip denylist for /echo" guidance. If a future refactor accidentally hoists `_INJECTION_MARKERS` to module-level and applies it to /echo, this test fires.

    Per REVIEW-CHECKLIST §8: subprocess invocations targeting scratch use `env=_CLEAN_ENV | {custom}`.
    Per §7: file at tests/ top-level.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase06_echo_hardening_polarity.py -v && uv run pytest tests/test_phase04_scaffold_polarity.py tests/test_phase06_auth_polarity.py -v</automated>
  </verify>
  <done>All 6 test cases pass; Phase 4 polarity + 06-02 auth polarity still pass.</done>
</task>

</tasks>

<contract>
**Producer-side contract this plan exposes:**
- Module-level (inside `{% if has_backend %}` unconditional block) in rendered `app/api.py`: `_CONTROL_CHARS_ECHO` regex constant (distinct from 06-03's `_CONTROL_CHARS` — REVIEW-CHECKLIST §3 decoupling)
- EchoRequest text field: `Field(min_length=1, max_length=5000)` with field_validator stripping control chars
- Behavior: 422 on length>5000, control chars stripped silently, no injection-marker check (intentional — /echo does not call LLM)

**Downstream plans must reference:**
- 06-06 (README): documents /echo defenses as a parallel section to /summarize defenses, noting the deliberate omission of the denylist with the §4 rationale ("/echo does not call an LLM")
- 06-08 (self-test CI): the `+backend` matrix entry exercises /echo end-to-end via `just verify`
</contract>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External HTTP client → /echo request body | Untrusted; defenses run at Pydantic validation |
| /echo → response | Reflects sanitized input back (no LLM, no external call) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Denial of Service | Large input → memory pressure | mitigate | Field max_length=5000 |
| T-06-12 | Information Disclosure | Control chars (zero-width, null) used to smuggle markers through downstream log parsers | mitigate | _CONTROL_CHARS_ECHO strip in field_validator |
| T-06-13 | Tampering | Future refactor adds denylist to /echo by mistake (over-applies LLM defenses) | mitigate | Task 2 test_echo_no_injection_marker_check asserts a known injection-marker string returns 200, not 422 |
</threat_model>

<completion_checklist>
- `uv run pytest tests/test_phase06_echo_hardening_polarity.py -v` passes (all 6 cases)
- `uv run pytest tests/test_phase04_scaffold_polarity.py tests/test_phase06_auth_polarity.py -v` passes (no regression)
- `bd update verify-kit-93h --status=in_progress` at plan start
- `bd close verify-kit-93h --reason="/echo hardening landed in 06-04: length cap + control-char strip (denylist deliberately omitted — /echo does not call LLM per §4)"` at plan end
</completion_checklist>

<commit_strategy>
Two atomic commits:
1. `feat(echo): input hardening (length cap + control-char strip) (verify-kit-93h)` — api.py.jinja2 changes
2. `test(echo): polarity test asserting denylist is NOT applied to /echo` — tests file
</commit_strategy>

<output>
Create `.planning/phases/06-template-self-test-documentation/06-04-SUMMARY.md` recording:
- Exact `_CONTROL_CHARS_ECHO` regex as committed
- Decoupling rationale (independent constant from 06-03's `_CONTROL_CHARS`)
- bead verify-kit-93h close confirmation
</output>
