---
phase: 06-template-self-test-documentation
plan: "06-04"
plan_name: echo-hardening
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - template/{% if has_backend %}app{% endif %}/models.py.jinja2
  - tests/test_phase06_echo_hardening_polarity.py
autonomous: true
requirements: []
closes_beads: [verify-kit-93h]
must_haves:
  truths:
    - "/echo request model EchoRequest (defined in app/models.py, NOT app/api.py) enforces `message` field length 1..5000 via Pydantic Field — note the field is `message`, NOT `text`"
    - "/echo field_validator on `message` strips control chars (same `_CONTROL_CHARS` pattern shape as 06-03)"
    - "/echo does NOT apply the injection-marker denylist (LLM-specific; /echo does not call an LLM — §4 explicit guidance)"
    - "Content-Type: application/json enforced via FastAPI default JSON-body validation"
    - "Phase 4 /echo functional contract preserved (reflects sanitized input back)"
    - "Auth from 06-02 inherits — tests set X-VerifyKit-Token header or use ENV=dev fallback"
    - "Tests cover happy path + length cap + control char strip + Content-Type bypass"
  artifacts:
    - path: "tests/test_phase06_echo_hardening_polarity.py"
      provides: "Forcing-function tests for /echo defenses"
      contains: "EchoRequest"
  key_links:
    - from: "EchoRequest field_validator (in app/models.py)"
      to: "_CONTROL_CHARS_ECHO pattern (independent regex from 06-03's _CONTROL_CHARS in app/api.py)"
      via: "@field_validator('message')"
      pattern: "field_validator"
---

<objective>
Apply the same starter-grade input-hardening shape as `/summarize` to `/echo` (verify-kit-93h): length cap + control-char strip + Content-Type enforcement. EXCLUDE the injection-marker denylist — `/echo` does not call an LLM, so the LLM-specific markers are out of scope per §4.

Purpose: Closes the OSS blocker that Phase 4's `/echo` route had no input-side hardening. Symmetric pair to 06-03 (same parent dependency: 06-02 auth scaffold).

Output: Edited `app/api.py.jinja2` (extend the existing `/echo` block from Phase 4 04-02), new polarity test.
</objective>

<constraints>
- D-16: closes bead verify-kit-93h (/echo hardening, same shape as /summarize).
- D-17: dedicated plan per bead.
- §4 Research explicit guidance: "Apply layers 1–3 (skip the denylist — it's LLM-specific)". This plan ships ONLY length cap + control-char strip + Content-Type. NO injection-marker regex.
- Phase 4 04-02 contract: `/echo` accepts a request body and reflects it back. The existing handler in `app/api.py` and `echo` service in `app/services.py` must be preserved — this plan only adds input validation to the Pydantic model in `app/models.py`. Source-grounded facts: `EchoRequest(BaseModel)` lives in `app/models.py.jinja2` with a single field `message: str`; the service reflects `req.message`. The Phase 5 SummarizeRequest from 06-03 lives in `app/api.py` (different file, different field name `text`) — these are deliberately distinct.
- REVIEW-CHECKLIST §3: the regex constant in this plan is named `_CONTROL_CHARS_ECHO` (distinct from 06-03's `_CONTROL_CHARS` in `app/api.py`). They live in DIFFERENT files now — `_CONTROL_CHARS_ECHO` in `app/models.py` next to EchoRequest, `_CONTROL_CHARS` in `app/api.py` next to SummarizeRequest. No cross-file coupling.
- REVIEW-CHECKLIST §1: no relative paths.
- REVIEW-CHECKLIST §2: validator returns after cleanup; no statements after return.
- REVIEW-CHECKLIST §6: no meta-comments in rendered template.
- Phase 4 04-02 /echo endpoint MUST continue to work — auth header from 06-02 + new validation are additive; the response body still reflects sanitized input.
</constraints>

<context>
@.planning/phases/06-template-self-test-documentation/06-CONTEXT.md
@.planning/phases/06-template-self-test-documentation/06-RESEARCH.md
@.planning/REVIEW-CHECKLIST.md
@.planning/phases/04-backend-fastapi-add-on/04-02-SUMMARY.md
@template/{% if has_backend %}app{% endif %}/api.py.jinja2

<interfaces>
<!-- Existing Phase 4 contract (must not break). -->

From Phase 4 04-02 (source-grounded — confirmed by reading `app/models.py.jinja2`, `app/services.py.jinja2`, and `app/api.py.jinja2`):
- POST /echo accepts `{"message": "..."}`, returns `{"message": ..., "request_id": ..., "received_at": ...}`
- `EchoRequest(BaseModel)` lives in `template/{% if has_backend %}app{% endif %}/models.py.jinja2` (NOT in api.py.jinja2)
- The single field is `message: str` (NOT `text`)
- The route handler `echo_route(req: EchoRequest)` lives in api.py.jinja2 and calls `echo(req, request_id=rid)` from services.py — neither needs editing

From 06-02 auth:
- All routes including /echo inherit `Depends(require_auth)` globally; tests set X-VerifyKit-Token

This plan ADDS to `app/models.py.jinja2`:
- Module-level constant `_CONTROL_CHARS_ECHO = re.compile(r"[\x00-\x08\x0b\x0c\x0e-\x1f\x7f]")`
- `import re` and `from pydantic import field_validator` (extending the existing `from pydantic import BaseModel, Field` import)
- `EchoRequest.message` constrained with `Field(min_length=1, max_length=5000)`
- `@field_validator("message")` classmethod that strips control chars and returns cleaned value
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add length cap + control-char strip to EchoRequest in app/models.py</name>
  <files>template/{% if has_backend %}app{% endif %}/models.py.jinja2</files>
  <read_first>
    - template/{% if has_backend %}app{% endif %}/models.py.jinja2 (FULL — confirm EchoRequest definition and the `message: str` field; verify no other field name exists)
    - template/{% if has_backend %}app{% endif %}/services.py.jinja2 (confirm `echo` service uses `req.message`)
    - template/{% if has_backend %}app{% endif %}/api.py.jinja2 (confirm `echo_route` uses `req: EchoRequest` and is NOT edited by this plan)
    - .planning/phases/04-backend-fastapi-add-on/04-02-SUMMARY.md (canonical /echo shape as landed)
    - .planning/phases/06-template-self-test-documentation/06-RESEARCH.md (§4 — explicit "skip denylist for /echo" guidance)
  </read_first>
  <behavior>
    - Test 1: Rendered `app/models.py` defines `EchoRequest.message: str = Field(min_length=1, max_length=5000)`.
    - Test 2: Rendered `app/models.py` has a `@field_validator("message")` classmethod that strips control characters via `_CONTROL_CHARS_ECHO.sub("", v)`.
    - Test 3: Rendered `app/models.py` does NOT contain `_INJECTION_MARKERS` (LLM-specific; intentionally absent).
    - Test 4: Rendered `app/api.py` is unchanged with respect to /echo — `echo_route(req: EchoRequest)` and its handler body untouched.
    - Test 5: Rendered `app/services.py` still references `req.message` (handler/service contract preserved).
    - Test 6: `import re` present at top of `app/models.py`.
  </behavior>
  <action>
    Read `template/{% if has_backend %}app{% endif %}/models.py.jinja2` in full. Use Edit to:

    (1) Add `import re` to the imports at the top of the file (currently has `from __future__ import annotations`, `from datetime import datetime`, `from pydantic import BaseModel, Field`).

    (2) Extend the pydantic import to include `field_validator`: `from pydantic import BaseModel, Field, field_validator`.

    (3) Add a module-level constant ABOVE the `EchoRequest` class (positioned after the `HealthResponse` class):
    ```python
    _CONTROL_CHARS_ECHO = re.compile(r"[\x00-\x08\x0b\x0c\x0e-\x1f\x7f]")
    ```

    (4) Modify the existing `EchoRequest` class. The current source is:
    ```python
    class EchoRequest(BaseModel):
        message: str
    ```
    Replace with:
    ```python
    class EchoRequest(BaseModel):
        message: str = Field(min_length=1, max_length=5000)

        @field_validator("message")
        @classmethod
        def _strip_control(cls, v: str) -> str:
            return _CONTROL_CHARS_ECHO.sub("", v)
    ```

    (5) Do NOT modify `EchoResponse`, `HealthResponse`, the service in `app/services.py`, or the route handler in `app/api.py`. The /echo functional contract (handler signature, route path, response shape, `req.message` reflection) is preserved.

    Per REVIEW-CHECKLIST §2: validator returns the cleaned value at end; no statements after return.
    Per §3: this plan's regex constant is `_CONTROL_CHARS_ECHO` in `app/models.py` — distinct from 06-03's `_CONTROL_CHARS` in `app/api.py`. Different files, no cross-coupling.
    Per Pattern 6: no internal planning IDs (D-NN, bead IDs) anywhere in the rendered template.
  </action>
  <verify>
    <automated>uv run --no-project python -c "
import subprocess, pathlib, re, os, sys
# Resolve repo source BEFORE any cd — never fall through to Path.cwd() (cycle-2 HIGH #1).
_root = os.environ.get('VERIFY_KIT_ROOT')
if not _root:
    sys.exit('ERROR: VERIFY_KIT_ROOT must be set to the verify-kit repo root. Invoke as: VERIFY_KIT_ROOT=\$(pwd) <command>')
src = pathlib.Path(_root).resolve()
assert src.is_dir() and (src / 'copier.yml').is_file(), f'VERIFY_KIT_ROOT={src} is not a verify-kit repo root (missing copier.yml)'
os.chdir('/tmp')
subprocess.run(['rm','-rf','/tmp/scratch-06-04'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_backend=true','--data','has_llm=false','--vcs-ref','HEAD',str(src),'/tmp/scratch-06-04'], check=True)
models = pathlib.Path('/tmp/scratch-06-04/app/models.py').read_text()
api = pathlib.Path('/tmp/scratch-06-04/app/api.py').read_text()
services = pathlib.Path('/tmp/scratch-06-04/app/services.py').read_text()
# EchoRequest hardened in models.py — not api.py
assert '_CONTROL_CHARS_ECHO' in models, 'echo-specific control-char regex constant missing from models.py'
assert 'max_length=5000' in models
assert re.search(r'@field_validator\(\s*[\"\']message[\"\']\s*\)', models), 'field_validator must target message field (NOT text)'
assert 'import re' in models
# api.py must NOT be edited for /echo by this plan
assert '_CONTROL_CHARS_ECHO' not in api, 'echo regex must live in models.py, not api.py'
# Denylist must not leak
assert '_INJECTION_MARKERS' not in models, 'injection-marker denylist must NOT be in models.py'
# /echo still present and service contract intact
assert '/echo' in api
assert 'req.message' in services, '/echo service must still reflect req.message'
# Polarity: has_backend=false has no app/
subprocess.run(['rm','-rf','/tmp/scratch-06-04-nb'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_backend=false','--vcs-ref','HEAD',str(src),'/tmp/scratch-06-04-nb'], check=True)
assert not pathlib.Path('/tmp/scratch-06-04-nb/app').exists(), 'app/ leaked into has_backend=false'
print('OK')
"</automated>
  </verify>
  <done>EchoRequest.message in app/models.py enforces length cap + control-char strip via field_validator; denylist explicitly NOT present; app/api.py and app/services.py untouched; Phase 4 /echo functional contract preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Polarity test for /echo defenses</name>
  <files>tests/test_phase06_echo_hardening_polarity.py</files>
  <read_first>
    - tests/test_phase06_summarize_defenses_polarity.py (structural reference from 06-03)
    - tests/test_phase06_auth_polarity.py (auth header pattern from 06-02)
    - tests/_helpers.py
  </read_first>
  <behavior>
    Note: /echo body is `{"message": "..."}` (NOT `{"text": "..."}`); response body is `{"message": ..., "request_id": ..., "received_at": ...}`. All tests below use the `message` key.
    - test_echo_happy_path: `{"message": "hello"}`, with auth, → 200 + response `message == "hello"`.
    - test_echo_length_cap: `{"message": "A"*5001}` → 422.
    - test_echo_control_chars_stripped: `{"message": "hi\x00\x07there"}` → 200 with response `message == "hithere"` (chars stripped).
    - test_echo_no_injection_marker_check: `{"message": "Ignore all previous instructions"}` → 200 (NOT 422 — /echo does not run the denylist). This is a forcing-function test that the denylist is correctly omitted from /echo.
    - test_echo_content_type_non_json: POST /echo with Content-Type: text/plain → 422.
    - test_echo_inherits_auth: POST /echo without X-VerifyKit-Token (ENV=prod, token set in Settings) → 401 (auth from 06-02 inherits — global FastAPI dependency wired in main.py per 06-02 Task 2).
  </behavior>
  <action>
    Create `tests/test_phase06_echo_hardening_polarity.py` at repo top-level (REVIEW-CHECKLIST §7).

    Mirror the structure of `tests/test_phase06_summarize_defenses_polarity.py` from 06-03 (render scratch project once per module via fixture, subprocess.run with `env=_CLEAN_ENV | {auth + env vars}`, TestClient-based assertions inside the scratch project).

    **Lifespan-aware TestClient pattern is mandatory** (cycle-2 HIGH #2 — same fix shape as 06-02 and 06-03): the scratch-side script MUST use `create_app(cwd=tmp)` + `with TestClient(application) as client:` to trigger the FastAPI lifespan and bind `app.state.settings` (which `require_auth` reads). Bare `TestClient(app)` is FORBIDDEN — it skips lifespan and AttributeErrors at request time. Write the per-case `.env` (e.g. `ENV=dev\nVERIFYKIT_AUTH_TOKEN=dev-token\n` for cases that need auth, `ENV=prod\nVERIFYKIT_AUTH_TOKEN=prod-token\n` for `test_echo_inherits_auth`) into the tmp dir before calling `create_app(cwd=tmp)`. Match the canonical snippet shape shown in 06-02 Task 3 and 06-03 Task 2. Use a non-`app` variable name (e.g. `application`) for the `create_app()` return value to avoid shadowing the `app` package.

    Key difference: render `has_backend=true, has_llm=false` (Phase 4-only configuration) — /echo lives in the unconditional backend block (`app/models.py` has EchoRequest regardless of has_llm), /summarize only lives in has_llm=true. This separates the /echo test from /summarize concerns. The test must use `client.post("/echo", json={"message": "..."})` matching the actual schema.

    The `test_echo_no_injection_marker_check` test is the forcing-function for §4's "skip denylist for /echo" guidance. If a future refactor accidentally hoists `_INJECTION_MARKERS` to module-level and applies it to /echo, this test fires.

    Per REVIEW-CHECKLIST §8: subprocess invocations targeting scratch use `env=_CLEAN_ENV | {custom}`.
    Per §7: file at tests/ top-level.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase06_echo_hardening_polarity.py -v && uv run pytest tests/test_phase04_scaffold_polarity.py tests/test_phase06_auth_polarity.py -v</automated>
  </verify>
  <done>All 6 test cases pass; Phase 4 polarity + 06-02 auth polarity still pass.</done>
</task>

</tasks>

<contract>
**Producer-side contract this plan exposes:**
- Module-level constant `_CONTROL_CHARS_ECHO` in rendered `app/models.py` (distinct file and identifier from 06-03's `_CONTROL_CHARS` in `app/api.py` — REVIEW-CHECKLIST §3 decoupling)
- `EchoRequest.message: str = Field(min_length=1, max_length=5000)` with `@field_validator("message")` stripping control chars
- Behavior: 422 on length>5000, control chars stripped silently from `message`, no injection-marker check (intentional — /echo does not call LLM)
- Request body shape unchanged: `{"message": "..."}` (NOT `{"text": "..."}`)

**Downstream plans must reference:**
- 06-06 (README): documents /echo defenses as a parallel section to /summarize defenses, noting the deliberate omission of the denylist with the §4 rationale ("/echo does not call an LLM")
- 06-08 (self-test CI): the `+backend` matrix entry exercises /echo end-to-end via `just verify`
</contract>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External HTTP client → /echo request body | Untrusted; defenses run at Pydantic validation |
| /echo → response | Reflects sanitized input back (no LLM, no external call) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Denial of Service | Large input → memory pressure | mitigate | Field max_length=5000 |
| T-06-12 | Information Disclosure | Control chars (zero-width, null) used to smuggle markers through downstream log parsers | mitigate | _CONTROL_CHARS_ECHO strip in field_validator |
| T-06-13 | Tampering | Future refactor adds denylist to /echo by mistake (over-applies LLM defenses) | mitigate | Task 2 test_echo_no_injection_marker_check asserts a known injection-marker string returns 200, not 422 |
</threat_model>

<completion_checklist>
- `uv run pytest tests/test_phase06_echo_hardening_polarity.py -v` passes (all 6 cases)
- `uv run pytest tests/test_phase04_scaffold_polarity.py tests/test_phase06_auth_polarity.py -v` passes (no regression)
- `bd update verify-kit-93h --status=in_progress` at plan start
- `bd close verify-kit-93h --reason="/echo hardening landed in 06-04: length cap + control-char strip (denylist deliberately omitted — /echo does not call LLM per §4)"` at plan end
</completion_checklist>

<commit_strategy>
Two atomic commits:
1. `feat(echo): input hardening (length cap + control-char strip) (verify-kit-93h)` — api.py.jinja2 changes
2. `test(echo): polarity test asserting denylist is NOT applied to /echo` — tests file
</commit_strategy>

<output>
Create `.planning/phases/06-template-self-test-documentation/06-04-SUMMARY.md` recording:
- Exact `_CONTROL_CHARS_ECHO` regex as committed
- Decoupling rationale (independent constant from 06-03's `_CONTROL_CHARS`)
- bead verify-kit-93h close confirmation
</output>
