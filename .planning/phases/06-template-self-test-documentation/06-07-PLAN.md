---
phase: 06-template-self-test-documentation
plan: "06-07"
plan_name: contributing-and-pr-template
type: execute
wave: 4
depends_on: ["06-05"]
files_modified:
  - CONTRIBUTING.md
  - .github/pull_request_template.md
autonomous: true
requirements: [DOC-03]
closes_beads: []
must_haves:
  truths:
    - "CONTRIBUTING.md covers three sections per D-13: (a) smoke-test loop description, (b) 'add a new check in 10 lines' walkthrough, (c) 'add a new add-on slot' walkthrough flagged with 'may evolve' note"
    - "CONTRIBUTING.md documents the conventional-commit contract from 06-05 verbatim: feat: / fix: / feat!: / chore: / docs: / refactor: / perf: / test: with their SemVer effects (pre-1.0 mapping)"
    - ".github/pull_request_template.md contains the six dual-audience rows VERBATIM from REQUIREMENTS.md lines 11-18 (matching the README section from 06-06)"
    - "PR template includes: Summary, Test plan, Dual-audience checklist (6 checkboxes), Conventional-commit type, Breaking changes for consumers"
    - "PR template's 'Breaking changes for consumers' section pairs with release-please CHANGELOG hand-edit convention (D-11) — same wording across both surfaces"
    - "No CI grep gate (D-14 — checkbox + reviewer-eye is the policy)"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Contributor onboarding doc"
      contains: "smoke-test loop"
      min_lines: 80
    - path: ".github/pull_request_template.md"
      provides: "PR template enforcing dual-audience checklist + commit-contract awareness"
      contains: "Dual-audience checklist"
  key_links:
    - from: "CONTRIBUTING.md commit-contract section"
      to: "release-please-config.json changelog-sections"
      via: "matching feat:/fix:/etc shape"
      pattern: "feat:"
    - from: ".github/pull_request_template.md dual-audience checklist"
      to: "README.md dual-audience section (06-06)"
      via: "verbatim six rows from REQUIREMENTS.md"
      pattern: "Human in terminal sees"
---

<objective>
Land CONTRIBUTING.md (DOC-03) and `.github/pull_request_template.md` (D-14): the contributor onboarding doc + the PR template that surfaces the dual-audience checklist as checkboxes alongside the conventional-commit + breaking-changes prompts.

Purpose: CONTRIBUTING explains how a new contributor (or future-Claude) extends the template — adds a check, adds an add-on slot — and ties their commit messages back to the release-please contract from 06-05. The PR template enforces D-14's "checkbox + reviewer eye" policy (no CI grep gate).

Output: Two markdown files.
</objective>

<constraints>
- D-13: CONTRIBUTING.md scope = smoke-test loop + add-a-check + add-an-add-on-slot (last is speculative; flagged "may evolve").
- D-12: SemVer enforced via release-please commit contract; documented HERE (D-12 routes to this plan).
- D-14: PR template checkbox for six dual-audience rows; NO automated CI grep gate.
- §1 Research: commit-contract table (feat: minor pre-1.0, fix: patch, feat!: minor pre-1.0 with breaking footer parsed, chore/docs/refactor/perf/test: no release) — quote verbatim.
- §9 Research: PR template's six rows are VERBATIM from REQUIREMENTS.md lines 11-18. The same text appears in README (06-06). Do NOT reword.
- REVIEW-CHECKLIST §3: this plan REFERENCES the commit-contract producer (06-05's release-please-config.json) and the six-row producer (REQUIREMENTS.md). Do NOT invent new prefix semantics or new checklist rows.
- REVIEW-CHECKLIST §1: no cwd-relative paths in any embedded snippet.
- REVIEW-CHECKLIST §6: no meta-comments.
- The "add a new check in 10 lines" snippet uses the ACTUAL harness API surface (`@register` decorator, `CheckResult` class). Read the existing harness package before authoring the snippet (REVIEW-CHECKLIST §4 anti-drift).
</constraints>

<context>
@.planning/phases/06-template-self-test-documentation/06-CONTEXT.md
@.planning/phases/06-template-self-test-documentation/06-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/REVIEW-CHECKLIST.md
@.planning/phases/06-template-self-test-documentation/06-05-SUMMARY.md
@template/harness/checks/__init__.py.jinja2
@template/harness/registry.py.jinja2
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - .planning/phases/06-template-self-test-documentation/06-RESEARCH.md (§1 commit-contract; §5 CI matrix shape for smoke-test loop)
    - .planning/REVIEW-CHECKLIST.md §4 (extract actual harness API names before authoring "add a check" snippet)
    - template/harness/checks/__init__.py.jinja2 (locate @register decorator + CheckResult — confirm exact names landed in Phase 2/3)
    - template/harness/registry.py.jinja2 (registration site)
  </read_first>
  <action>
    Create `CONTRIBUTING.md` at repo root with the following H2 sections:

    **`## Smoke-test loop` (D-13a)** — explains that every PR runs `.github/workflows/template-selftest.yml` (from 06-08 W5; reference it as "the self-test workflow" without inventing path details if 06-08 hasn't landed). The 5 matrix entries are run end-to-end via `copier copy → just verify`; a regression in any entry fails the PR. Cite §5 for the matrix shape but do not duplicate the YAML.

    **`## Adding a new check in 10 lines` (D-13b)** — concrete code snippet using the ACTUAL harness API. The real surface (grep-extracted from `template/harness/registry.py.jinja2:19-29` + `template/harness/models.py.jinja2:22,61`):
    - `register` is exported from `harness.registry` (NOT `harness.checks`).
    - `CheckResult` is exported from `harness.models` (NOT `harness.checks`).
    - Decorator signature is `register(check_id: str, *, tier: CheckTier = "standard", category: str = "misc", description: str = "", ...)` — first arg is POSITIONAL `check_id`, NOT keyword `id`.
    - Valid `CheckTier` values are `"quick" | "standard" | "slow"` (Literal type at `harness/models.py.jinja2:22`). `"universal"` is INVALID and will not parse.

    Per REVIEW-CHECKLIST §4 — do NOT invent `@register_check` or `CheckResult(ok=...)` or `tier="universal"`; those are drift patterns. Verbatim snippet:

    ```python
    # template/harness/checks/my_check.py.jinja2
    from harness.registry import register
    from harness.models import CheckResult

    @register("my-check", tier="standard", category="example")
    def my_check(cwd) -> CheckResult:
        # ... actual logic ...
        return CheckResult(status="pass", check_id="my-check", duration_ms=0)
    ```

    Mention that the snippet uses the exact symbols landed in `template/harness/registry.py.jinja2` (register) and `template/harness/models.py.jinja2` (CheckResult, CheckTier).

    **`## Adding a new add-on slot` (D-13c, "may evolve")** — describe the Copier prompt + path-gating + Jinja conditional + tests shape currently used by `has_backend` / `has_llm` (Phase 4/5 reference). Open with the note: "**This section is speculative until v0.2 actually adds a new add-on slot (Web/Audio/Game). Treat as a starting point; the canonical procedure will be locked when the first v0.2 slot lands.**" Sketch 4 steps: (1) add prompt to `copier.yml`, (2) gate paths via `_exclude` + bounded `{% if %}` directory names, (3) add Jinja conditional in shared files (`pyproject.toml.jinja2`, `justfile.jinja2`), (4) add a polarity test under `tests/test_phaseNN_polarity.py`. Reference the Phase 4 path-gating two-guard rule from .planning/REVIEW-CHECKLIST.md §3.

    **`## Commit-message contract` (D-12)** — quote §1 Research table verbatim:
    | Prefix | Effect (pre-1.0) | Example |
    | feat: | minor bump (0.1 → 0.2) | feat(llm): add streaming summarize |
    | fix: | patch bump | fix(matrix): cron fires twice |
    | feat!: or BREAKING CHANGE: footer | minor bump + breaking-change footer parsed | feat!: rename has_backend |
    | chore:, docs:, refactor:, perf:, test: | no release PR triggered |

    Note that the operator hand-edits the "Breaking changes for consumers" block in the release PR before merging (D-11, cross-reference 06-05 SUMMARY).

    **`## Running the matrix locally` (optional helper)** — `act -W .github/workflows/template-selftest.yml -j selftest --matrix combo:base` per §11.

    Per REVIEW-CHECKLIST §6: no meta-comments. Per §4: the @register / CheckResult names are GREP-EXTRACTED from the landed harness, not invented.
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md && wc -l CONTRIBUTING.md | awk '{exit ($1>=80)?0:1}' && grep -q "Smoke-test loop\|smoke-test loop" CONTRIBUTING.md && grep -q "Adding a new check" CONTRIBUTING.md && grep -q "Adding a new add-on slot\|add a new add-on slot" CONTRIBUTING.md && grep -q "may evolve\|speculative" CONTRIBUTING.md && grep -q "feat:" CONTRIBUTING.md && grep -q "feat!:\|BREAKING CHANGE" CONTRIBUTING.md && grep -q "from harness.registry import register" CONTRIBUTING.md && grep -q "from harness.models import CheckResult" CONTRIBUTING.md && grep -q "tier=\"standard\"\|tier='standard'" CONTRIBUTING.md && ! grep -q "@register_check\|CheckResult(ok=\|tier=\"universal\"\|tier='universal'\|from harness.checks import" CONTRIBUTING.md</automated>
  </verify>
  <done>CONTRIBUTING.md has all 3 D-13 sections + commit-contract; "add a check" snippet uses real @register/CheckResult API (REVIEW-CHECKLIST §4 anti-drift); add-on slot section flagged "may evolve".</done>
</task>

<task type="auto">
  <name>Task 2: Author .github/pull_request_template.md</name>
  <files>.github/pull_request_template.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 11-18 — canonical six rows)
    - .planning/phases/06-template-self-test-documentation/06-RESEARCH.md (§9 — verbatim PR template)
  </read_first>
  <action>
    Create `.github/pull_request_template.md` with the §9 canonical content:

    ```markdown
    ## Summary

    <!-- 1-3 sentences: what changes, why -->

    ## Test plan

    <!-- How was this verified? `just verify` output? Specific scenarios? -->

    ## Dual-audience checklist

    Every change must answer all six rows. Tick each. If a row is N/A for this PR, write "N/A — <reason>" in the box.

    - [ ] **1. Human in terminal sees:** Pretty colorized output via isatty; spinner; failed checks summarized with one-line next-action hint.
    - [ ] **2. Human in VS Code sees:** SARIF in Problems panel, JUnit in Testing sidebar — no agent involvement required.
    - [ ] **3. Agent calling programmatically gets:** Deterministic JSON with stable schema (introspectable via `describe`), error envelope `{code, message, hint, fix_command, docs_url}`, semantic exit codes.
    - [ ] **4. Agent has a fix path:** Failed check returns `fix_command`; `fix_propose` MCP tool returns unified diff with rationale; agent can re-verify without human round-trip.
    - [ ] **5. Human can override agent:** Every fix is `--dry-run`-able; destructive MCP tools annotated `destructiveHint: true`; Stop-hook escape hatch (`VERIFY_KIT_SKIP=1`); audit log in `.verify-kit/audit.jsonl`.
    - [ ] **6. Both can collaborate mid-flow:** Same `verify-kit trace --last` works for both; state file-backed in `.verify-kit/` so human can `cat` while agent runs.

    ## Conventional-commit type

    <!-- One of: feat / fix / chore / docs / refactor / perf / test (use `feat!:` for breaking) -->

    ## Breaking changes for consumers

    <!-- If this changes any Copier prompt, rendered file path, or generated-project API: describe the migration step for users running `copier update`. Otherwise: "None." -->
    ```

    Per §9 Pitfalls: the six rows MUST be verbatim from REQUIREMENTS.md — do NOT reword. The release-please CHANGELOG hand-edit (D-11) reuses the SAME "Breaking changes for consumers" phrasing — this gives the operator one shape across PR template + CHANGELOG entry.

    Per D-14: no CI grep gate. The checkboxes are advisory; reviewer eye + author honesty is the policy.
  </action>
  <verify>
    <automated>test -f .github/pull_request_template.md && grep -q "Dual-audience checklist" .github/pull_request_template.md && grep -q "Human in terminal sees" .github/pull_request_template.md && grep -q "Human in VS Code sees" .github/pull_request_template.md && grep -q "Agent calling programmatically" .github/pull_request_template.md && grep -q "Agent has a fix path" .github/pull_request_template.md && grep -q "Human can override agent" .github/pull_request_template.md && grep -q "Both can collaborate" .github/pull_request_template.md && grep -q "Breaking changes for consumers" .github/pull_request_template.md && grep -q "Conventional-commit type" .github/pull_request_template.md</automated>
  </verify>
  <done>PR template has 6 dual-audience rows verbatim + Summary + Test plan + Conventional-commit prompt + Breaking-changes-for-consumers prompt matching release-please convention.</done>
</task>

</tasks>

<contract>
**Producer-side contract this plan exposes:**
- `CONTRIBUTING.md` at repo root (06-06 README footer links here)
- `.github/pull_request_template.md` (GitHub auto-loads on PR creation)
- The commit-contract section in CONTRIBUTING is the single source of truth for downstream documentation that mentions feat/fix/etc semantics

**No downstream plan in Phase 6 depends on these artifacts** (06-06 README footer link is the only consumer; if 06-06 lands first, the link will be a dead-link until this plan lands — Task 3 of 06-06 explicitly allows CONTRIBUTING.md to be absent at 06-06 verification time).
</contract>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External contributor → PR template | Author fills in honestly; reviewer enforces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-19 | Repudiation | Contributor unchecks all six rows and ships anyway | accept | Reviewer-eye policy (D-14); CI grep gate explicitly deferred |
| T-06-20 | Tampering | "add a check" snippet drifts from actual harness API (drift pattern §4) | mitigate | Task 1 grep-asserts `@register` (real name) is present and `@register_check` / `CheckResult(ok=` (invented names from §4) are NOT |
</threat_model>

<completion_checklist>
- `test -f CONTRIBUTING.md .github/pull_request_template.md`
- `wc -l CONTRIBUTING.md | awk '{exit ($1>=80)?0:1}'`
- Six rows verbatim in PR template (verified by Task 2 grep loop)
- CONTRIBUTING uses real `@register` decorator name (§4 anti-drift)
- `grep -q "may evolve\|speculative" CONTRIBUTING.md` (D-13c flag)
</completion_checklist>

<commit_strategy>
Single atomic commit:
```
docs(contributing): land CONTRIBUTING.md + PR template (DOC-03, D-12, D-13, D-14)

CONTRIBUTING covers smoke-test loop, add-a-check (using real @register API
per §4 anti-drift), add-an-add-on-slot (flagged may-evolve), and the
conventional-commit contract that release-please parses. PR template
surfaces the six dual-audience rows verbatim from REQUIREMENTS.md as
checkboxes per D-14.
```
</commit_strategy>

<output>
Create `.planning/phases/06-template-self-test-documentation/06-07-SUMMARY.md` recording:
- Exact harness API names used in the "add a check" snippet (for §4 anti-drift audit trail)
- Confirmation the six rows match REQUIREMENTS.md byte-for-byte
- Cross-reference to 06-05 commit contract
</output>
