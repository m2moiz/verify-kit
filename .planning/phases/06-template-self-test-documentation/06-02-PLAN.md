---
phase: 06-template-self-test-documentation
plan: "06-02"
plan_name: auth-scaffold
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - template/{% if has_backend %}app{% endif %}/auth.py.jinja2
  - template/{% if has_backend %}app{% endif %}/main.py.jinja2
  - template/{% if has_backend %}app{% endif %}/settings.py.jinja2
  - template/{% if has_backend %}app{% endif %}/.env.example.jinja2
  - tests/test_phase06_auth_polarity.py
autonomous: true
requirements: []
closes_beads: [verify-kit-3u2]
must_haves:
  truths:
    - "Rendered (has_backend=true) project has app/auth.py defining `require_auth` FastAPI dependency using APIKeyHeader(name='X-VerifyKit-Token', auto_error=False) + secrets.compare_digest"
    - "app/main.py registers `require_auth` as a GLOBAL dependency via FastAPI(dependencies=[Depends(require_auth)])"
    - "Settings exposes `VERIFYKIT_AUTH_TOKEN: str | None = None` field read from env var VERIFYKIT_AUTH_TOKEN (pydantic-settings — Settings already uses UPPERCASE field naming, e.g. ENV/LOG_LEVEL)"
    - "GET /healthz is excluded from auth via in-dependency path check (require_auth inspects request.url.path and short-circuits for /healthz) — app-level dependencies CANNOT be overridden by route-level dependencies=[] in FastAPI"
    - "If VERIFYKIT_AUTH_TOKEN is unset AND settings.ENV == 'dev', request is allowed (logs one-line warning); if ENV != 'dev', returns 503"
    - "If VERIFYKIT_AUTH_TOKEN set, request without matching X-VerifyKit-Token header returns 401"
    - "app/.env.example documents VERIFYKIT_AUTH_TOKEN slot (blank value) with user-facing comment ONLY (no internal planning IDs); the LLM-only root .env.example is intentionally skipped because that polarity has no FastAPI surface; README contract handled by 06-06 (not this plan)"
    - "has_backend=false polarity preserved: no app/auth.py renders"
    - "Phase 4 LIFO middleware order is NOT modified — auth is a Depends layer between middleware and route handler"
  artifacts:
    - path: "template/{% if has_backend %}app{% endif %}/auth.py.jinja2"
      provides: "require_auth dependency function"
      contains: "X-VerifyKit-Token"
      min_lines: 25
    - path: "tests/test_phase06_auth_polarity.py"
      provides: "Forcing-function polarity tests for auth scaffold"
      contains: "X-VerifyKit-Token"
  key_links:
    - from: "app/main.py FastAPI() constructor"
      to: "app/auth.py require_auth"
      via: "dependencies=[Depends(require_auth)] global"
      pattern: "dependencies=\\[Depends\\(require_auth\\)\\]"
    - from: "app/auth.py require_auth"
      to: "settings.VERIFYKIT_AUTH_TOKEN"
      via: "request.app.state.settings (no get_settings factory exists)"
      pattern: "VERIFYKIT_AUTH_TOKEN"
---

<objective>
Land the FastAPI route auth scaffold (verify-kit-3u2): token-gate every route by default via a global `require_auth` dependency, with explicit exclusion for `/healthz` and a dev-mode zero-config fallback.

Purpose: Closes the OSS blocker that a downstream consumer scaffolding via `copier copy` would otherwise ship public-by-default endpoints. The auth scaffold is the producer artifact that 06-03 (`/summarize` defenses) and 06-04 (`/echo` hardening) build on; this plan must land before them and before the self-test CI (06-08) which sets `VERIFYKIT_AUTH_TOKEN` in the matrix env.

Output: New `app/auth.py.jinja2`, edits to `app/main.py.jinja2` + `app/settings.py.jinja2` + both `.env.example.jinja2` variants, plus a polarity test.
</objective>

<constraints>
- D-16: closes bead verify-kit-3u2 (token-gate `/__debug/*` + `/summarize` + `/echo`).
- D-17: dedicated plan per bead.
- §3 Research: `APIKeyHeader(name="X-VerifyKit-Token", auto_error=False)` + `secrets.compare_digest`; global `FastAPI(dependencies=[Depends(require_auth)])`; settings field `VERIFYKIT_AUTH_TOKEN: str | None = None` (UPPERCASE — matches existing Settings field-naming convention); dev-mode fallback (log + allow) when token unset AND settings.ENV == "dev"; in non-dev with token unset → 503.
- `/healthz` MUST remain unauthenticated (docker healthcheck + smoke tests). FastAPI app-level `dependencies=[Depends(...)]` cannot be overridden by route-level `dependencies=[]`; the global dependency runs for every path operation. Therefore `/healthz` exclusion is implemented IN-DEPENDENCY: `require_auth(request: Request, ...)` short-circuits with an early `return None` when `request.url.path == "/healthz"`.
- Settings API: the existing module at `template/{% if has_backend %}app{% endif %}/settings.py.jinja2` exposes `class Settings(BaseSettings)` with UPPERCASE fields (ENV, LOG_LEVEL, ...) and a `load(cwd: Path) -> Settings` factory. There is NO `get_settings` symbol. The `/healthz` handler reads settings via `request.app.state.settings` (set in the lifespan). `require_auth` MUST read settings the same way (via `request.app.state.settings`) rather than depending on a fictional `get_settings` factory.
- Phase 4 LIFO middleware order is preserved (dependencies run between middleware and handler — they do not register as middleware).
- REVIEW-CHECKLIST §3 (cross-plan contract drift): the contract this plan exposes is the import path `from app.auth import require_auth` and the env var name `VERIFYKIT_AUTH_TOKEN`. Downstream plans (06-03, 06-04, 06-08, 06-06) MUST reference these exact symbols.
- REVIEW-CHECKLIST §4 (plan API-surface drift): the existing Phase 4 settings.py + main.py are landed; this plan must Edit, not rewrite. Read the existing files first to extract their exact import + Settings class structure.
- REVIEW-CHECKLIST §5: any Jinja conditional touching YAML/env files must span block-level lines, not inline fragments.
- Two-key model: `/__debug/*` keeps its existing `ENV=dev` env-gate AND inherits the new token-gate (defense-in-depth).
</constraints>

<context>
@.planning/phases/06-template-self-test-documentation/06-CONTEXT.md
@.planning/phases/06-template-self-test-documentation/06-RESEARCH.md
@.planning/REVIEW-CHECKLIST.md
@.planning/phases/04-backend-fastapi-add-on/04-02-SUMMARY.md
@template/{% if has_backend %}app{% endif %}/main.py.jinja2
@template/{% if has_backend %}app{% endif %}/settings.py.jinja2
@template/{% if has_backend %}app{% endif %}/api.py.jinja2

<interfaces>
<!-- Producer API surface — downstream plans MUST quote these exact symbols. -->

From this plan (NEW):
- `from app.auth import require_auth` — FastAPI dependency function `(request: Request) -> None`; raises HTTPException 401/503
- Env var: `VERIFYKIT_AUTH_TOKEN` (blank means "use dev fallback if settings.ENV=='dev', else 503")
- Settings field: `Settings.VERIFYKIT_AUTH_TOKEN: str | None` (UPPERCASE — matches existing field convention)
- HTTP header consumers must send: `X-VerifyKit-Token: <token>`
- Excluded route: `GET /healthz` (in-dependency path short-circuit)

From Phase 4 (existing — read before editing):
- `app/main.py` already builds `app = FastAPI(title="app", lifespan=lifespan)` via `create_app(cwd)` and registers middleware in LIFO order. Lifespan stores `app.state.settings = load_settings(cwd)`.
- `app/settings.py` defines `class Settings(BaseSettings)` with UPPERCASE fields (`ENV: str`, `LOG_LEVEL: str`, ...) and a `load(cwd: Path) -> Settings` factory. No `get_settings` symbol exists.
- Settings access pattern (from existing `/healthz` handler): `settings = request.app.state.settings`. `require_auth` MUST use this same pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/auth.py.jinja2 with require_auth dependency</name>
  <files>template/{% if has_backend %}app{% endif %}/auth.py.jinja2, template/{% if has_backend %}app{% endif %}/settings.py.jinja2</files>
  <read_first>
    - template/{% if has_backend %}app{% endif %}/settings.py.jinja2 (FULL — must understand current Settings shape + get_settings signature before Editing)
    - .planning/phases/06-template-self-test-documentation/06-RESEARCH.md (§3 — canonical implementation)
  </read_first>
  <behavior>
    - Test 1: `from app.auth import require_auth` imports cleanly in a rendered (has_backend=true) project.
    - Test 2: A TestClient request to a token-gated route without `X-VerifyKit-Token` header, with VERIFYKIT_AUTH_TOKEN set, returns 401.
    - Test 3: Same request with matching `X-VerifyKit-Token` header returns 200 (or whatever the route normally returns).
    - Test 4: With VERIFYKIT_AUTH_TOKEN unset AND env=dev, the request is ALLOWED (no 401) — dev-mode zero-config fallback.
    - Test 5: With VERIFYKIT_AUTH_TOKEN unset AND env=prod, the request returns 503.
    - Test 6: secrets.compare_digest is used (grep `secrets.compare_digest` in auth.py — constant-time check).
  </behavior>
  <action>
    Create `template/{% if has_backend %}app{% endif %}/auth.py.jinja2` (plain Python — no Jinja interpolation). The file MUST contain the canonical implementation below, which differs from §3 RESEARCH in two specific ways to align with the actual source: (a) settings is read via `request.app.state.settings` rather than a fictional `get_settings` Depends factory, (b) the `/healthz` exclusion is handled inside `require_auth` via a path short-circuit (NOT by route-level `dependencies=[]`, which FastAPI does not honor as an override of app-level dependencies).

    Canonical implementation:
    ```python
    from __future__ import annotations
    import secrets
    from typing import Annotated
    from fastapi import Depends, HTTPException, Request, status
    from fastapi.security import APIKeyHeader
    from harness.logging import log

    _api_key_header = APIKeyHeader(name="X-VerifyKit-Token", auto_error=False)
    _dev_fallback_warned = False

    def require_auth(
        request: Request,
        presented: Annotated[str | None, Depends(_api_key_header)],
    ) -> None:
        # In-dependency exclusion: /healthz must remain unauthenticated for docker
        # healthchecks and smoke tests. FastAPI app-level dependencies are NOT
        # overridable via route-level dependencies=[]; the short-circuit lives here.
        if request.url.path == "/healthz":
            return None

        settings = request.app.state.settings
        expected = settings.VERIFYKIT_AUTH_TOKEN

        if not expected:
            # Zero-config dev UX: log once, allow.
            if settings.ENV == "dev":
                global _dev_fallback_warned
                if not _dev_fallback_warned:
                    log.warning("auth.dev_fallback_no_token_configured")
                    _dev_fallback_warned = True
                return None
            raise HTTPException(
                status_code=status.HTTP_503_SERVICE_UNAVAILABLE,
                detail="VERIFYKIT_AUTH_TOKEN not configured",
            )

        if presented is None or not secrets.compare_digest(
            presented.encode("utf-8"), expected.encode("utf-8")
        ):
            raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="invalid token")
        return None
    ```

    Edit `template/{% if has_backend %}app{% endif %}/settings.py.jinja2`: locate the existing `class Settings(BaseSettings)` block (currently exposes `ENV`, `LOG_LEVEL`, `LOG_FORMAT`, `DATABASE_URL`, `PROFILE_ENABLED`). Add `VERIFYKIT_AUTH_TOKEN: str | None = Field(default=None, description="X-VerifyKit-Token header value; unset enables dev fallback when ENV='dev'")` near the other fields. Pydantic-settings will pick up the env var automatically because BaseSettings reads from environment by default and the field name matches the env var (UPPERCASE convention).

    Per REVIEW-CHECKLIST §1: no relative paths. Per §2: no statements after return inside `require_auth` (every branch ends in `return None` or `raise`). Per §6: no internal planning IDs (D-NN, bead IDs) in the rendered template. The inline comment about "/healthz must remain unauthenticated for docker healthchecks" is permanent reference and acceptable.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-06-02-auth && uv run --no-project python -c "
import subprocess, pathlib, os
src = pathlib.Path(os.environ.get('VERIFY_KIT_ROOT', pathlib.Path.cwd()))
subprocess.run(['copier','copy','--trust','--defaults','--data','has_backend=true','--vcs-ref','HEAD',str(src),'/tmp/scratch-06-02-auth'], check=True)
auth = pathlib.Path('/tmp/scratch-06-02-auth/app/auth.py').read_text()
assert 'X-VerifyKit-Token' in auth
assert 'secrets.compare_digest' in auth
assert 'APIKeyHeader' in auth
assert 'auto_error=False' in auth
assert 'def require_auth' in auth
assert 'request.url.path' in auth and '/healthz' in auth, 'in-dependency /healthz short-circuit missing'
assert 'request.app.state.settings' in auth, 'must read settings via request.app.state.settings (no get_settings)'
settings = pathlib.Path('/tmp/scratch-06-02-auth/app/settings.py').read_text()
assert 'VERIFYKIT_AUTH_TOKEN' in settings, 'Settings missing VERIFYKIT_AUTH_TOKEN field (UPPERCASE)'
print('OK')
"</automated>
  </verify>
  <done>app/auth.py renders with require_auth + APIKeyHeader + secrets.compare_digest + in-dependency /healthz short-circuit + request.app.state.settings access; Settings exposes VERIFYKIT_AUTH_TOKEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire global dependency in app/main.py (no api.py route edits — exclusion lives in require_auth)</name>
  <files>template/{% if has_backend %}app{% endif %}/main.py.jinja2</files>
  <read_first>
    - template/{% if has_backend %}app{% endif %}/main.py.jinja2 (FULL — must preserve LIFO middleware order; locate the FastAPI(title="app", lifespan=lifespan) call inside create_app)
    - .planning/phases/06-template-self-test-documentation/06-RESEARCH.md (§3 exclusion table)
  </read_first>
  <behavior>
    - Test 1: Rendered app/main.py contains `from app.auth import require_auth` and `dependencies=[Depends(require_auth)]` inside the `FastAPI(...)` call.
    - Test 2: TestClient GET /healthz with no `X-VerifyKit-Token` header returns 200 even when VERIFYKIT_AUTH_TOKEN is set (the in-dependency short-circuit in require_auth handles this — NOT a route-level dependencies=[] override).
    - Test 3: Phase 4 middleware registration order is preserved (LIFO: secure outermost / pyinstrument innermost) — grep main.py for the existing order; the diff for this plan should add ONLY the FastAPI(dependencies=...) arg and the import.
    - Test 4: api.py is NOT modified by this plan (06-03 and 06-04 own the api.py edits for their respective routes).
  </behavior>
  <action>
    Read main.py.jinja2 in full. Edit the `FastAPI(title="app", lifespan=lifespan)` constructor call inside `create_app()` to become `FastAPI(title="app", lifespan=lifespan, dependencies=[Depends(require_auth)])` (preserve all existing args). Add `Depends` to the existing `from fastapi import FastAPI, Request` import → `from fastapi import Depends, FastAPI, Request`. Add `from app.auth import require_auth` next to the existing `from app.api import router as api_router` import. Do NOT touch the middleware registration block — Phase 4 LIFO order is preserved by leaving it alone.

    Do NOT edit api.py in this plan. The `/healthz` exclusion lives inside `require_auth` itself (path short-circuit in Task 1). FastAPI does NOT support overriding an app-level dependency via `dependencies=[]` on a route decorator; this was the cycle-1 review's HIGH #1. The `/__debug/*` and `/echo` routes inherit the global dependency automatically, which is the desired behavior (06-03 and 06-04 build on this).

    Per REVIEW-CHECKLIST §3: this plan defines `require_auth` (the contract); downstream plans must `from app.auth import require_auth` literally.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-06-02-wire && uv run --no-project python -c "
import subprocess, pathlib, os
src = pathlib.Path(os.environ.get('VERIFY_KIT_ROOT', pathlib.Path.cwd()))
subprocess.run(['copier','copy','--trust','--defaults','--data','has_backend=true','--vcs-ref','HEAD',str(src),'/tmp/scratch-06-02-wire'], check=True)
main = pathlib.Path('/tmp/scratch-06-02-wire/app/main.py').read_text()
assert 'from app.auth import require_auth' in main
import re
assert re.search(r'FastAPI\([^)]*dependencies\s*=\s*\[\s*Depends\(require_auth\)\s*\]', main, re.DOTALL), 'global dependency not wired'
# This plan must NOT modify api.py — 06-03/06-04 own that file
api = pathlib.Path('/tmp/scratch-06-02-wire/app/api.py').read_text()
assert 'dependencies=[]' not in api, '06-02 must NOT add dependencies=[] to api.py routes (FastAPI ignores it for app-level overrides)'
# has_backend=false polarity: no app/ at all
subprocess.run(['rm','-rf','/tmp/scratch-06-02-nb'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_backend=false','--vcs-ref','HEAD',str(src),'/tmp/scratch-06-02-nb'], check=True)
assert not pathlib.Path('/tmp/scratch-06-02-nb/app').exists(), 'app/ leaked into has_backend=false'
print('OK')
"</automated>
  </verify>
  <done>Global require_auth dependency wired in main.py; /healthz exclusion is handled by the in-dependency path check from Task 1 (not by route edits); Phase 4 middleware order preserved; has_backend=false polarity intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document VERIFYKIT_AUTH_TOKEN in both .env.example variants + write polarity test</name>
  <files>template/{% if has_backend %}app{% endif %}/.env.example.jinja2, template/{% if has_llm and not has_backend %}.env.example{% endif %}.jinja2, tests/test_phase06_auth_polarity.py</files>
  <read_first>
    - template/{% if has_backend %}app{% endif %}/.env.example.jinja2 (backend env file — present whenever has_backend=true, regardless of has_llm)
    - template/{% if has_llm and not has_backend %}.env.example{% endif %}.jinja2 (LLM-only root env file — present only when has_llm=true AND has_backend=false)
    - tests/test_phase05_polarity.py (structural reference — same render_scratch_project + _CLEAN_ENV pattern)
    - tests/_helpers.py (verify _CLEAN_ENV present from 05-05)
  </read_first>
  <behavior>
    - Test 1: has_backend=true render exposes `VERIFYKIT_AUTH_TOKEN=` (blank or placeholder) inside `app/.env.example`.
    - Test 1b: The `app/.env.example` comment text MUST NOT contain internal planning IDs (no `D-16`, no `verify-kit-3u2`, no plan/phase IDs) — this is a forcing-function test for REVIEW-CHECKLIST Pattern 6.
    - Test 2: A TestClient instantiated against the rendered app, with `settings.ENV='dev'` and `VERIFYKIT_AUTH_TOKEN` unset, allows GET /healthz AND POST /echo (dev fallback). Note: `/echo` uses field name `message` (see EchoRequest in app/models.py), so the request body is `{"message": "hi"}`.
    - Test 3: With ENV='prod' and token unset, POST /echo returns 503 (not 401 — distinguishes config error from auth error).
    - Test 4: With ENV='dev' and token='dev-token', POST /echo with header `X-VerifyKit-Token: dev-token` returns 200; without header returns 401.
    - Test 5: GET /healthz returns 200 in all cases (in-dependency short-circuit on path).
    - Test 6: has_backend=false render has no app/.env.example file at all. The LLM-only (has_llm=true, has_backend=false) root `.env.example` is NOT expected to contain VERIFYKIT_AUTH_TOKEN — this plan intentionally does not write to it.
  </behavior>
  <action>
    The actual env-file layout in the template is:
    - `template/{% if has_backend %}app{% endif %}/.env.example.jinja2` — rendered to `app/.env.example` whenever `has_backend=true` (regardless of `has_llm`).
    - `template/{% if has_llm and not has_backend %}.env.example{% endif %}.jinja2` — rendered to `.env.example` at the project root only when `has_llm=true AND has_backend=false`.

    Read both jinja files to confirm current contents and structure. Then append a `# Auth token for the X-VerifyKit-Token header. Leave blank in dev for the zero-config fallback; required in prod.` user-facing comment and a `VERIFYKIT_AUTH_TOKEN=` blank line under a `## Auth` section.

    IMPORTANT (REVIEW-CHECKLIST Pattern 6 — meta-comments in rendered templates): the comment text inserted into `.env.example` MUST NOT reference internal planning identifiers such as `D-16`, `verify-kit-3u2`, phase IDs, or plan IDs. These are internal planning metadata and will render verbatim into consumer projects. Use ONLY user-facing wording (the example sentence above is the canonical phrasing for this plan).

    Per REVIEW-CHECKLIST §5: place the new lines outside any Jinja conditional that could merge lines.

    Note: the LLM-only env file (`{% if has_llm and not has_backend %}`) governs a configuration where there is NO `app/` directory and hence no FastAPI surface — `VERIFYKIT_AUTH_TOKEN` is irrelevant there. Per the source-grounding pass, this plan SKIPS that file and writes the auth token slot ONLY to `template/{% if has_backend %}app{% endif %}/.env.example.jinja2`. This is the only env file that ever sees an HTTP route. Document this decision in 06-02-SUMMARY.md.

    Create `tests/test_phase06_auth_polarity.py` at repo top-level (NOT under tests/backend/ — REVIEW-CHECKLIST §7 recursion-guard). Import `render_scratch_project` and `_CLEAN_ENV` from `tests._helpers`. Parametrize over `(has_backend ∈ {T,F})`. The test:
    - Renders scratch project.
    - For has_backend=true: uses subprocess to run a small Python script inside the scratch project that does `from fastapi.testclient import TestClient; from app.main import app; client = TestClient(app); ...` with `env=_CLEAN_ENV | {"VERIFYKIT_AUTH_TOKEN": "...", "ENV": "..."}` and asserts response codes. Echo request body is `{"message": "hi"}` (matching the actual EchoRequest schema).
    - For has_backend=false: asserts `app/` directory absent.

    Test cases parametrized over (ENV, token_set, header_sent) tuples — 4 cases minimum: (dev, no, no)→200, (prod, no, no)→503, (dev, yes, no)→401, (dev, yes, yes)→200.

    Also include a forcing-function check on the rendered `app/.env.example`: assert the file contains `VERIFYKIT_AUTH_TOKEN=` AND assert it does NOT contain any of: `D-16`, `verify-kit-3u2`, `D-17`, `Plan 06-02`, `Phase 6` (Pattern 6 enforcement).

    Per REVIEW-CHECKLIST §8: all subprocess invocations into the scratch project pass `env=_CLEAN_ENV | {custom_vars}` — never the outer process env.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase06_auth_polarity.py -v</automated>
  </verify>
  <done>app/.env.example documents VERIFYKIT_AUTH_TOKEN slot with user-facing comments only (no planning IDs); polarity test passes all 4 auth-cell cases plus has_backend=false polarity.</done>
</task>

</tasks>

<contract>
**Producer-side contract this plan exposes to downstream plans:**

- Import path: `from app.auth import require_auth`
- Env var name: `VERIFYKIT_AUTH_TOKEN`
- HTTP header name: `X-VerifyKit-Token`
- Settings field: `Settings.VERIFYKIT_AUTH_TOKEN: str | None` (UPPERCASE — matches existing Settings field naming)
- Settings access pattern inside `require_auth`: `settings = request.app.state.settings` (NOT `Depends(get_settings)` — no such factory exists)
- Excluded routes: `GET /healthz` — via in-dependency `request.url.path` short-circuit inside `require_auth` (NOT via route-level `dependencies=[]`, which FastAPI does not honor as an override of app-level dependencies). All other routes inherit the global dependency.
- Dev-mode fallback: token unset + settings.ENV == 'dev' → allowed (warning logged once)
- Non-dev with unset token: HTTP 503 (config error, distinct from 401 auth error)

**Downstream plans must reference these exact symbols** (REVIEW-CHECKLIST §3 cross-plan drift):
- 06-03 (summarize defenses): TestClient sets `X-VerifyKit-Token` header in tests
- 06-04 (echo hardening): same header usage in tests
- 06-08 (self-test CI): matrix env sets `VERIFYKIT_AUTH_TOKEN=dev-token-for-tests` OR relies on `ENV=dev` fallback
- 06-06 (README): documents the X-VerifyKit-Token header + VERIFYKIT_AUTH_TOKEN env var in the consumer-facing setup section
</contract>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public consumer's deployed FastAPI app → external clients | Untrusted; require_auth gates every route by default |
| Settings → env var VERIFYKIT_AUTH_TOKEN | Trusted (env var is operator-set); secret never logged |
| `/healthz` → docker/CI healthcheck | Intentionally unauthenticated for liveness probes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Spoofing | Attacker sends forged X-VerifyKit-Token | mitigate | secrets.compare_digest (constant-time); 401 on mismatch |
| T-06-04 | Information Disclosure | Token logged in structured logs | mitigate | require_auth never logs `presented`; only logs "no token configured in dev" one-liner |
| T-06-05 | Tampering | Future refactor removes `dependencies=[Depends(require_auth)]` from main.py | mitigate | Task 3 polarity test grep-asserts the global dependency wiring + 401 behavior |
| T-06-06 | Denial of Service | Timing attack on token compare | mitigate | secrets.compare_digest is constant-time over equal-length encoded byte strings |
</threat_model>

<completion_checklist>
- `uv run pytest tests/test_phase06_auth_polarity.py -v` passes
- `grep -q "X-VerifyKit-Token" 'template/{% if has_backend %}app{% endif %}/auth.py.jinja2'` — verify producer artifact exists
- `grep -q "request.url.path" 'template/{% if has_backend %}app{% endif %}/auth.py.jinja2'` — verify in-dependency /healthz exclusion present
- `! grep -E "D-1[0-9]|verify-kit-[a-z0-9]{3}" 'template/{% if has_backend %}app{% endif %}/.env.example.jinja2'` — Pattern 6 forcing function (no planning IDs leak)
- `bd update verify-kit-3u2 --status=in_progress` at plan start
- `bd close verify-kit-3u2 --reason="Token auth scaffold landed in 06-02; APIKeyHeader + global dependency + dev fallback + /healthz exclusion"` at plan end
- Phase 4 polarity test still passes: `uv run pytest tests/test_phase04_scaffold_polarity.py -v`
</completion_checklist>

<commit_strategy>
Two atomic commits:
1. `feat(auth): add X-VerifyKit-Token route auth scaffold (verify-kit-3u2)` — auth.py + main.py + settings.py wiring + .env.example updates
2. `test(auth): polarity test for require_auth + /healthz exclusion + dev fallback` — tests/test_phase06_auth_polarity.py

Each commit uses `ALLOW_AI_WORKFLOW_FILES=1` only if it touches `.planning/` (these don't).
</commit_strategy>

<output>
Create `.planning/phases/06-template-self-test-documentation/06-02-SUMMARY.md` recording:
- The exact `require_auth` signature as committed
- The header + env-var contract for downstream plans
- Test coverage matrix (4 auth cells × 2 has_backend cells)
- bead verify-kit-3u2 close confirmation
</output>
