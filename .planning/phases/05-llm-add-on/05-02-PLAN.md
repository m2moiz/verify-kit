---
phase: 05-llm-add-on
plan: "05-02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - template/harness/{% if has_llm %}llm.py{% endif %}.jinja2
  - template/harness/observability.py.jinja2
autonomous: true
requirements: [LLM-02, LLM-03, LLM-04, LLM-05, LLM-08, LLM-09]
must_haves:
  truths:
    - "harness/llm.py renders ONLY when has_llm=true (per copier _exclude in 05-01)"
    - "@llm_call emits an OTel span named llm.<name> with the FULL gen_ai.* attribute set required by phase success criterion #2 (cycle-2 HIGH #4): gen_ai.operation.name, gen_ai.request.model, gen_ai.response.model, gen_ai.usage.input_tokens, gen_ai.usage.output_tokens, plus verify_kit.cost_usd, verify_kit.latency_ms, verify_kit.routing_path, verify_kit.retry_count"
    - "@cost_budget raises CostBudgetExceeded with typed exception (budget_usd, accumulated_usd, call_name)"
    - "Decorator stacking @cost_budget OUTER + @llm_call INNER produces correct ordering — budget check fires AFTER cost recorded"
    - "call_llm() public entry point (LLM-03) dispatches via _routing_path(): claude-agent-sdk path OR litellm path — every verify-kit-shipped call site (e.g., /summarize from 05-05) MUST go through call_llm(), NOT through pydantic_ai.Agent.run() or litellm.completion() directly. Per D-21 (cycle-2 HIGH #5), pydantic-ai is shipped as the consumer-facing typed-call ergonomic layer (importable, documented in README LLM-12) — but it is NOT the routing entry point. call_llm() is the routing entry point because it owns D-03 USE_CLAUDE_CODE_SDK switching and the full gen_ai.* span attribute contract; pydantic-ai bypasses both."
    - "Adapter routing follows D-03 rule: USE_CLAUDE_CODE_SDK=1 OR (no ANTHROPIC_API_KEY AND sdk importable) → claude-agent-sdk path; else litellm"
    - "claude-agent-sdk path emits span with cost_usd=0.0 and verify_kit.routing_path=claude-code-sdk attribute (no-cost asymmetry per Pitfall 5)"
    - "litellm path emits span with verify_kit.routing_path=litellm and cost_usd computed from tokencost (real cost)"
    - "Traceloop.init() is called from observability.py inside the existing _otel_enabled block, BEFORE any LLM library is imported"
    - "Path-gate shape is filename-level (template/harness/{% if has_llm %}llm.py{% endif %}.jinja2) — the harness/ directory is universal, so the gate goes on the leaf filename, not on the directory segment"
  artifacts:
    - path: "template/harness/{% if has_llm %}llm.py{% endif %}.jinja2"
      provides: "call_llm, llm_call, cost_budget, CostBudgetExceeded, reset_cost_accumulator, _routing_path, call_via_claude_agent_sdk, call_via_litellm"
      exports: ["call_llm", "llm_call", "cost_budget", "CostBudgetExceeded", "reset_cost_accumulator"]
      min_lines: 160
    - path: "template/harness/observability.py.jinja2"
      provides: "Traceloop.init() call inside _otel_enabled gate when has_llm=true"
      contains: "{% if has_llm %}"
  key_links:
    - from: "harness/llm.py @llm_call"
      to: "harness/observability.py tracer"
      via: "from harness.observability import tracer"
      pattern: "from harness.observability import"
    - from: "harness/llm.py adapter"
      to: "claude_agent_sdk.query"
      via: "isinstance message AssistantMessage / TextBlock"
      pattern: "claude_agent_sdk"
---

<objective>
Build the single source of truth for Phase 5: harness/llm.py. This file provides the `@llm_call` + `@cost_budget` decorator pair and the D-03 routing adapter. Every downstream LLM call site (tests, /summarize endpoint, eval scaffolding) goes through this one file — no call site sees the routing decision directly.

Purpose: Centralize OTel span emission, cost computation, and budget enforcement so there is exactly one place to read for "how does verify-kit call an LLM." Encode the load-bearing decorator ordering rule (`@cost_budget` OUTER, `@llm_call` INNER) and the claude-agent-sdk no-cost asymmetry in the code itself so executors and reviewers cannot silently regress.

Output: harness/llm.py.jinja2 (new, has_llm-gated) + minimal Traceloop.init() addition to observability.py.jinja2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/REVIEW-CHECKLIST.md
@.planning/phases/05-llm-add-on/05-CONTEXT.md
@.planning/phases/05-llm-add-on/05-RESEARCH.md
@.planning/phases/02-universal-harness-core/02-02-SUMMARY.md
@template/harness/observability.py.jinja2
@template/harness/logging.py.jinja2
@template/harness/trace_id.py.jinja2

<interfaces>
<!-- Authoritative API surface from Phase 2 landed code — match these spellings (REVIEW-CHECKLIST §4). -->

From template/harness/observability.py.jinja2:
- Module-level `tracer = trace.get_tracer("verify-kit")` exported symbol
- `_otel_enabled` flag is set inside the existing setup block when OTEL_EXPORTER_OTLP_ENDPOINT is present
- TracerProvider + BatchSpanProcessor wired in the same block
- Must call Traceloop.init() inside that block, AFTER tracer provider is set, BEFORE any LLM lib import

From template/harness/logging.py.jinja2:
- `log = structlog.get_logger(...)` module-level export (used for log.info bind in decorator)

From template/harness/trace_id.py.jinja2:
- `set_trace_id(rid: str)` and `get_trace_id() -> str | None` for contextvar correlation (existing Phase 2 API)

From RESEARCH.md, exact claude-agent-sdk API surface (verified PyPI 2026-05-21):
- `from claude_agent_sdk import query, ClaudeAgentOptions, AssistantMessage, TextBlock`
- `query(prompt: str, options: ClaudeAgentOptions) -> AsyncIterator[Message]`
- Iterate; `if isinstance(message, AssistantMessage): for block in message.content: if isinstance(block, TextBlock): use block.text`
- SDK does NOT expose token usage on the message stream — cost MUST be reported as 0.0 in this path

From RESEARCH.md, exact pydantic-ai v1.100 API:
- `Agent("anthropic:claude-haiku-4-5", output_type=Model)`
- `result = await agent.run(prompt)` — output via `result.output` (NOT result.data — pydantic-ai v1+ rename)
- `result.usage` (RunUsage) provides input_tokens/output_tokens
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Traceloop.init() to observability.py inside existing _otel_enabled block</name>
  <files>template/harness/observability.py.jinja2</files>
  <read_first>
    - template/harness/observability.py.jinja2 (full file — must understand existing _otel_enabled block placement before editing)
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "Pitfall 7: Traceloop.init() called too late" — placement is load-bearing)
  </read_first>
  <action>
    Edit observability.py.jinja2. Locate the existing `_otel_enabled` setup block from Phase 2 — this is where TracerProvider + BatchSpanProcessor are configured. AFTER the TracerProvider is set and the exporter is registered, AND BEFORE the block ends, add a `{% if has_llm %}` block that imports Traceloop from traceloop.sdk and calls `Traceloop.init(disable_batch=False)`. Per Pitfall 7, this MUST execute before any LLM library is imported into the process — observability.py is loaded early (during harness package import) so this placement is correct. Wrap the call in try/except ImportError to keep the universal layer importable in renders where has_llm=false (defense-in-depth — the {% if has_llm %} gate is the primary guard). Per REVIEW-CHECKLIST §6, do NOT add cycle-N or replan meta-comments; explanatory docstring text about Pitfall 7 IS permitted because it documents permanent behavior.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-02-trace && uv run --no-project python -c "
import subprocess, pathlib
src='/Users/moiz/Documents/code/verify-kit'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-02-trace'], check=True)
text = pathlib.Path('/tmp/scratch-05-02-trace/harness/observability.py').read_text()
assert 'Traceloop' in text and 'init' in text, 'Traceloop.init missing'
# has_llm=false should NOT have Traceloop reference
subprocess.run(['rm','-rf','/tmp/scratch-05-02-trace-off'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=false','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-02-trace-off'], check=True)
off = pathlib.Path('/tmp/scratch-05-02-trace-off/harness/observability.py').read_text()
assert 'Traceloop' not in off, 'Traceloop leaked into has_llm=false render'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - has_llm=true render's harness/observability.py contains Traceloop.init call
    - has_llm=false render does NOT contain Traceloop reference (polarity preserved)
    - The Traceloop.init call is positioned AFTER TracerProvider setup, BEFORE the _otel_enabled block ends
    - try/except ImportError wraps the import so missing traceloop-sdk does not crash universal harness
  </acceptance_criteria>
  <done>Traceloop.init wired into the existing Phase 2 OTel setup; polarity verified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create harness/llm.py with call_llm() routing API, @llm_call, @cost_budget, adapter</name>
  <files>template/harness/{% if has_llm %}llm.py{% endif %}.jinja2</files>
  <read_first>
    - template/harness/observability.py.jinja2 (for `tracer` import shape)
    - template/harness/logging.py.jinja2 (for `log` import shape)
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (sections "Pattern 2: @llm_call Decorator Sketch", "Pattern 3: Adapter Routing Sketch", "Pitfall 3: decorator ordering", "Pitfall 5: claude-agent-sdk no usage")
    - .planning/phases/05-llm-add-on/05-CONTEXT.md (D-01, D-02, D-03, D-21 — pydantic-ai role clarification per cycle-2 HIGH #5)
  </read_first>
  <behavior>
    NOTE: This plan ships the production code; behavior tests live in 05-03 (where the test scaffold lands). The behavior list below describes the contract 05-03 will assert against.
    - Contract 1: `@llm_call(name="x")` decorator wraps an async function; calling the wrapped function emits an OTel span named `llm.x` with `gen_ai.operation.name="x"` attribute.
    - Contract 2 (cycle-2 HIGH #4 — full span attribute contract): Span carries the FULL required attribute set per phase success criterion #2. Required attribute names (exact strings to set via `span.set_attribute(...)`):
      - `gen_ai.operation.name` (str — the decorator's `name` argument)
      - `gen_ai.request.model` (str — the model string passed into the adapter; for claude-agent-sdk path use the literal `"claude-agent-sdk"` since SDK does not expose model)
      - `gen_ai.response.model` (str — the model the provider actually used, read from the adapter response dict; falls back to the request model when the adapter does not expose it, e.g. claude-agent-sdk path)
      - `gen_ai.usage.input_tokens` (int — from adapter response `usage.input_tokens`; 0 for claude-agent-sdk path per Pitfall 5)
      - `gen_ai.usage.output_tokens` (int — from adapter response `usage.output_tokens`; 0 for claude-agent-sdk path per Pitfall 5)
      - `verify_kit.cost_usd` (float — 0.0 acceptable for claude-agent-sdk path)
      - `verify_kit.latency_ms` (int)
      - `verify_kit.routing_path` (str — one of "claude-agent-sdk" | "litellm")
      - `verify_kit.retry_count` (int — number of retries attempted by the adapter; default 0 when the adapter does not surface retry info; for litellm read from `litellm._response_headers` / completion response retry metadata when available, else 0)
      Each attribute MUST be set unconditionally on the span before the decorator returns (use 0 / fallback values per the rules above; never omit). Forcing-functioned by 05-03 Task 3 `test_span_carries_gen_ai_attrs`, which asserts the presence of every attribute name above on an in-memory exported span.
    - Contract 3: `@cost_budget(usd=0.0, on_exceed="raise")` placed OUTER over `@llm_call` raises `CostBudgetExceeded` after the inner call records any cost > 0; with `usd=999.0`, no exception.
    - Contract 4: CostBudgetExceeded carries `.budget_usd`, `.accumulated_usd`, `.call_name` attributes.
    - Contract 5: `_routing_path()` returns "claude-agent-sdk" when `USE_CLAUDE_CODE_SDK=1` env is set; returns "claude-agent-sdk" when ANTHROPIC_API_KEY is unset AND claude_agent_sdk importable; returns "litellm" when ANTHROPIC_API_KEY is set and USE_CLAUDE_CODE_SDK unset.
    - Contract 6: `reset_cost_accumulator()` resets the contextvar to 0.0.
    - Contract 7: Decorator ordering — placing `@llm_call` OUTER and `@cost_budget` INNER (wrong order) does NOT raise even when budget=0.0; doc-string explicitly checks this asymmetry to forcing-function the documented rule from Pitfall 3.
    - Contract 8 (D-01/D-03 routing — LLM-03): `call_llm(prompt, *, model=None, output_type=str)` is the single public entry point. Calling `await call_llm(...)` with USE_CLAUDE_CODE_SDK=1 dispatches to `call_via_claude_agent_sdk()` and the emitted span has `verify_kit.routing_path="claude-agent-sdk"` and `verify_kit.cost_usd=0.0`. Calling with ANTHROPIC_API_KEY set and USE_CLAUDE_CODE_SDK unset dispatches to `call_via_litellm()` and emits `verify_kit.routing_path="litellm"` with non-zero `verify_kit.cost_usd` (computed via tokencost on the litellm response usage block). The dispatch is the load-bearing piece of D-03 — without it, the decorator alone cannot route arbitrary call sites.
  </behavior>
  <action>
    Create template/harness/{% if has_llm %}llm.py{% endif %}.jinja2 (Shape 2 — FILENAME-LEVEL gate; the harness/ directory is universal so the conditional goes on the leaf filename, NOT on the directory segment per REVIEW-CHECKLIST §3). Implement, in order:
    (1) Module docstring stating the LOAD-BEARING decorator-ordering rule from RESEARCH.md Pitfall 3: `@cost_budget` MUST be the OUTER decorator (applied last, executed first); `@llm_call` MUST be INNER. Include the rationale: the budget check runs AFTER the inner call has recorded its cost into the contextvar.
    (2) `_cumulative_cost_usd: contextvars.ContextVar[float]` accumulator with default 0.0.
    (3) `class CostBudgetExceeded(RuntimeError)` with `__init__(self, budget_usd, accumulated_usd, call_name)` storing all three as attributes.
    (4) `def reset_cost_accumulator() -> None` test helper that sets the contextvar to 0.0.
    (5) `def _routing_path() -> Literal["claude-agent-sdk", "litellm"]` implementing D-03 rule precisely as stated in RESEARCH.md Pattern 3: returns "claude-agent-sdk" if `USE_CLAUDE_CODE_SDK==1` OR (no ANTHROPIC_API_KEY AND `claude_agent_sdk` importable); else "litellm".
    (6) `async def call_via_claude_agent_sdk(prompt: str, *, model: str | None = None) -> dict` — uses exact API from interfaces block (query/ClaudeAgentOptions/AssistantMessage/TextBlock); returns dict shaped `{"content": str, "usage": {"input_tokens": 0, "output_tokens": 0}, "model": "claude-agent-sdk", "provider": "claude-code-sdk", "cost_usd": 0.0}`. Inline comment cites Pitfall 5 (no usage exposed; cost=0 asymmetry is intentional and documented in README LLM-12).
    (7) `async def call_via_litellm(prompt: str, *, model: str | None = None) -> dict` — uses `from litellm import acompletion`; default model from `os.environ.get("LLM_DEFAULT_MODEL", "anthropic/claude-haiku-4-5")` when caller passes None; returns dict shaped `{"content": str, "usage": {"input_tokens": int, "output_tokens": int}, "model": <model>, "provider": "litellm", "cost_usd": float}` where cost_usd is computed via `tokencost.calculate_prompt_cost`+`calculate_completion_cost` (or `litellm.completion_cost` if exposed) — wrap in try/except defaulting cost_usd=0.0 if computation fails.
    (8) **PUBLIC ENTRY POINT** `@llm_call(name="call_llm") async def call_llm(prompt: str, *, model: str | None = None) -> dict` — load-bearing for D-01/D-03/LLM-03. The body reads `_routing_path()` once, then dispatches: `if _routing_path() == "claude-agent-sdk": result = await call_via_claude_agent_sdk(prompt, model=model)` else `result = await call_via_litellm(prompt, model=model)`. Returns the adapter's dict. Because `call_llm` is itself decorated with `@llm_call(name="call_llm")`, every invocation produces a span at `llm.call_llm` regardless of which adapter ran — so consumers get one consistent observability surface. (Inner adapters do NOT add their own `@llm_call` — that would double-emit spans.) Inline comment cites LLM-03 + D-03.
    (9) `def llm_call(*, name: str)` decorator factory. Wraps async functions. Inside `_inner`: capture monotonic start time; open `tracer.start_as_current_span(f"llm.{name}")`; set `gen_ai.operation.name=name` and `verify_kit.routing_path=_routing_path()` attributes; await the inner fn; extract the FULL set of required span attributes from the adapter result dict (cycle-2 HIGH #4):
        - `cost_usd = result.get("cost_usd", 0.0) if isinstance(result, dict) else 0.0` (try/except defaults to 0.0)
        - `req_model = result.get("model", "")` and `resp_model = result.get("response_model", req_model)` — set `gen_ai.request.model=req_model` and `gen_ai.response.model=resp_model`
        - `usage = result.get("usage", {}) if isinstance(result, dict) else {}`; set `gen_ai.usage.input_tokens=int(usage.get("input_tokens", 0))` and `gen_ai.usage.output_tokens=int(usage.get("output_tokens", 0))`
        - `retry_count = int(result.get("retry_count", 0)) if isinstance(result, dict) else 0`; set `verify_kit.retry_count=retry_count`
        - set `verify_kit.cost_usd=cost_usd` and `verify_kit.latency_ms=int((monotonic() - start) * 1000)`
        Every attribute is set unconditionally (use 0 / empty-string fallback rather than omitting). Update the cost accumulator via `_cumulative_cost_usd.set(_cumulative_cost_usd.get() + cost_usd)`; emit a structlog `log.info("llm_call.complete", name=name, cost_usd=cost_usd, latency_ms=..., input_tokens=..., output_tokens=..., retry_count=...)`; on exception, `span.record_exception(e)` and re-raise; return result. Per REVIEW-CHECKLIST §2, return is the LAST statement in the function — no cleanup after return.

    **Cycle-2 HIGH #4 — adapter contract update:** the adapter return dicts (steps 6 and 7 above) must also expose `retry_count` and `response_model` keys so the decorator can pull them. Update step 6 (`call_via_claude_agent_sdk`) to return `{"content": str, "usage": {"input_tokens": 0, "output_tokens": 0}, "model": "claude-agent-sdk", "response_model": "claude-agent-sdk", "provider": "claude-code-sdk", "cost_usd": 0.0, "retry_count": 0}`. Update step 7 (`call_via_litellm`) to additionally set `"response_model": <response.model || model>` (read from the litellm response object) and `"retry_count": <retries>` (read from litellm response metadata when exposed, else 0). The exact litellm field names are: `response.model` for the response model echo, and `response._hidden_params.get("attempted_retries", 0)` (or fallback 0) for retry count — litellm 1.85 exposes attempt counts under `_hidden_params`.
    (10) `def cost_budget(*, usd: float, on_exceed: str = "raise")` decorator factory. Wraps async functions. Inside `_inner`: `result = await fn(*args, **kwargs)`; read `accumulated = _cumulative_cost_usd.get()`; if `accumulated > usd` and `on_exceed == "raise"`, raise `CostBudgetExceeded(usd, accumulated, fn.__name__)`; return result. Docstring restates the ordering rule.
    (11) `__all__ = ["call_llm", "llm_call", "cost_budget", "CostBudgetExceeded", "reset_cost_accumulator", "_routing_path", "call_via_claude_agent_sdk", "call_via_litellm"]`.

    Per REVIEW-CHECKLIST §1, do not introduce any bare `Path("...")` literal. Per §4, do not invent decorator names — exports MUST match those in `__all__`. Decorator ordering rule MUST appear in module docstring AND in `cost_budget` docstring (defense-in-depth documentation per Pitfall 3). Per REVIEW-CHECKLIST §3, downstream plans (05-03 tests, 05-05 /summarize) MUST consume `call_llm` (not pydantic_ai.Agent directly) — this is the contract producer plan for that name.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-02-llm && uv run --no-project python -c "
import subprocess, pathlib, sys
src='/Users/moiz/Documents/code/verify-kit'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-02-llm'], check=True)
p = pathlib.Path('/tmp/scratch-05-02-llm/harness/llm.py')
assert p.exists(), 'harness/llm.py missing'
text = p.read_text()
for sym in ['call_llm', 'llm_call', 'cost_budget', 'CostBudgetExceeded', 'reset_cost_accumulator', '_routing_path', 'call_via_claude_agent_sdk', 'call_via_litellm']:
    assert sym in text, f'{sym} missing'
# D-03 routing is wired (not just helpers): call_llm body must reference both adapters
import re
m = re.search(r'async def call_llm\(.*?\):', text, re.S)
assert m, 'call_llm async def signature missing'
# call_llm body must dispatch via _routing_path
assert 'call_via_claude_agent_sdk' in text and 'call_via_litellm' in text and '_routing_path' in text, 'call_llm must dispatch via _routing_path()'
assert 'OUTER' in text and 'INNER' in text, 'Decorator ordering rule not documented'
assert 'gen_ai.operation.name' in text
# Cycle-2 HIGH #4: full gen_ai.* attribute set required by phase success criterion #2
for attr in ['gen_ai.request.model', 'gen_ai.response.model', 'gen_ai.usage.input_tokens', 'gen_ai.usage.output_tokens']:
    assert attr in text, f'missing required span attribute: {attr}'
assert 'verify_kit.cost_usd' in text and 'verify_kit.latency_ms' in text
assert 'verify_kit.routing_path' in text and 'verify_kit.retry_count' in text, 'verify_kit.retry_count missing (cycle-2 HIGH #4)'
# Polarity
subprocess.run(['rm','-rf','/tmp/scratch-05-02-llm-off'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=false','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-02-llm-off'], check=True)
assert not pathlib.Path('/tmp/scratch-05-02-llm-off/harness/llm.py').exists(), 'llm.py leaked into has_llm=false'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - harness/llm.py.jinja2 exists at template/harness/{% if has_llm %}llm.py{% endif %}.jinja2 (Shape 2 FILENAME-level gate — the harness/ directory is universal, so the conditional MUST be on the leaf filename, NOT on a directory segment)
    - File exports via __all__: call_llm, llm_call, cost_budget, CostBudgetExceeded, reset_cost_accumulator, _routing_path, call_via_claude_agent_sdk, call_via_litellm
    - `call_llm(prompt, *, model=None) -> dict` is the public entry point — its body dispatches via `_routing_path()` to either `call_via_claude_agent_sdk` or `call_via_litellm` (D-03/LLM-03 — the load-bearing routing piece)
    - Module docstring contains the words "OUTER" and "INNER" describing decorator ordering
    - Span attributes set (cycle-2 HIGH #4 — full set per phase success criterion #2): `gen_ai.operation.name`, `gen_ai.request.model`, `gen_ai.response.model`, `gen_ai.usage.input_tokens`, `gen_ai.usage.output_tokens`, `verify_kit.cost_usd`, `verify_kit.latency_ms`, `verify_kit.routing_path`, `verify_kit.retry_count`. Every attribute set unconditionally (no omissions; 0 / empty-string fallbacks on the claude-agent-sdk path)
    - `_routing_path` returns "claude-agent-sdk" when USE_CLAUDE_CODE_SDK=1
    - has_llm=false render does NOT contain harness/llm.py (polarity)
    - No bare relative `Path("...")` literal (REVIEW-CHECKLIST §1)
    - All in-function code precedes the return statement (REVIEW-CHECKLIST §2)
  </acceptance_criteria>
  <done>harness/llm.py.jinja2 created with full decorator pair + adapter; polarity verified; decorator ordering rule documented in code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer code → harness/llm.py | Trusted (in-process); but the decorators MUST raise typed exceptions (CostBudgetExceeded) rather than silently swallowing |
| harness/llm.py → claude-agent-sdk → Anthropic OAuth | Untrusted network; auto-instrumentation via httpx instrumentor handles span emission |
| harness/llm.py → litellm → provider APIs | Untrusted network; same as above |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Decorator stacking order (Pitfall 3) | mitigate | Module + decorator docstrings repeat the rule; behavior tests in 05-03 forcing-function it |
| T-05-05 | Repudiation | LLM call cost reporting | mitigate | Cost attribute on every span; structlog log emission with cost_usd/latency_ms; routing_path attribute distinguishes claude-agent-sdk (always $0) from litellm (real cost) per Pitfall 5 |
| T-05-06 | Information Disclosure | Prompt/response in span | accept | gen_ai.prompt.* / gen_ai.completion.* attributes are deprecated per OTel GenAI spec v1.38; we set neither — content goes to Langfuse via httpx instrumentor only |
| T-05-07 | Denial of Service (wallet) | Runaway LLM cost | mitigate | @cost_budget decorator raises CostBudgetExceeded once accumulated cost > usd threshold; contextvar scope means async tasks have isolated budgets |
</threat_model>

<verification>
1. harness/llm.py renders only when has_llm=true (polarity check in T2 verify command)
2. Five exported symbols (llm_call, cost_budget, CostBudgetExceeded, reset_cost_accumulator, _routing_path) all present
3. Decorator-ordering rule documented in module docstring AND cost_budget docstring
4. observability.py renders Traceloop.init() only when has_llm=true
</verification>

<success_criteria>
- harness/llm.py.jinja2 exists at template/harness/{% if has_llm %}llm.py{% endif %}.jinja2 (filename-level gate, not directory-level) with all eight exports and the documented decorator-ordering rule
- `call_llm(prompt, *, model=None)` is the public routing entry point (LLM-03/D-03), dispatching via `_routing_path()` to either claude-agent-sdk or litellm adapter
- observability.py.jinja2 calls Traceloop.init() inside its existing _otel_enabled block under {% if has_llm %}
- Polarity: has_llm=false → harness/llm.py absent AND observability.py contains no Traceloop reference
- No bare Path() literals, no statements after return, no @register_check or CheckResult(ok=) drift (this plan touches neither, but the discipline applies)
</success_criteria>

<output>
Create `.planning/phases/05-llm-add-on/05-02-SUMMARY.md` when done. Include:
- The exact `__all__` exports
- The decorator-ordering rule text as committed (so 05-03 tests can quote it)
- The span attribute names set (so 05-03 tests can assert on the exact strings)
- The _routing_path decision table as committed
</output>
