---
phase: 05-llm-add-on
plan: "05-05"
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - template/{% if has_backend %}app{% endif %}/api.py.jinja2
  - template/{% if has_claude_code %}.claude{% endif %}/skills/verify-kit-eval/SKILL.md.jinja2
  - template/README.md.jinja2
  - tests/test_phase05_polarity.py
autonomous: true
requirements: [LLM-12]
must_haves:
  truths:
    - "POST /summarize exists in (has_backend=true AND has_llm=true) renders ONLY (D-12); absent in other 3 cells"
    - "/summarize uses @cost_budget OUTER + @llm_call INNER decorator stack (Pitfall 3 ordering)"
    - "/summarize handler references `result.output` (NOT result.data — Pitfall 4)"
    - "verify-kit-eval SKILL.md is filled in (no longer a Phase 3 stub per D-14), covers when to run evals, how to read .verify/eval-results.json, when to escalate to human"
    - "README LLM section documents: (a) personal-vs-consumer setup per D-18, (b) ~/.zshrc Langfuse pattern per D-06, (c) `none` privacy option per D-08, (d) Cloud Hobby → Hetzner CX32 migration steps per D-17"
    - "tests/test_phase05_polarity.py parametrizes over the (has_backend × has_llm) 4-cell matrix and the 3-value llm_backend axis"
  artifacts:
    - path: "template/{% if has_backend %}app{% endif %}/api.py.jinja2"
      provides: "Appended /summarize endpoint under {% if has_llm %} block"
      contains: "/summarize"
    - path: "template/{% if has_claude_code %}.claude{% endif %}/skills/verify-kit-eval/SKILL.md.jinja2"
      provides: "Filled-in eval skill content"
      min_lines: 30
    - path: "template/README.md.jinja2"
      provides: "Appended LLM section with migration story + setup paths"
      contains: "Langfuse"
    - path: "tests/test_phase05_polarity.py"
      provides: "12-cell polarity test (4 has_backend×has_llm × 3 llm_backend)"
      contains: "summarize"
  key_links:
    - from: "/summarize route handler"
      to: "harness.llm decorators (from 05-02)"
      via: "from harness.llm import llm_call, cost_budget"
      pattern: "from harness.llm import"
    - from: "tests/test_phase05_polarity.py"
      to: "rendered scratch projects"
      via: "render_scratch_project helper + assertions on app.routes"
      pattern: "render_scratch_project"
---

<objective>
Compose Phase 4 × Phase 5 (the `/summarize` endpoint demonstrating @llm_call over HTTP — D-12), fill in the Phase 3 SKILL.md stub (D-14), document the operator's migration story in README (D-17, D-18, LLM-12), and lock in the entire phase via a polarity test that parametrizes over the 4-way scaffold matrix × 3-value llm_backend axis.

Purpose: This is the integration plan. Everything from 05-01..05-04 is exercised by the polarity test in this plan. The /summarize endpoint is the running proof that @llm_call works end-to-end; the SKILL.md is what the consumer's Claude Code instance reads to know how to operate the eval gate; the README is what a human reads to migrate from Cloud Hobby to self-host without losing data.

Output: One appended route, one filled-in skill, one appended README section, one polarity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/REVIEW-CHECKLIST.md
@.planning/phases/05-llm-add-on/05-CONTEXT.md
@.planning/phases/05-llm-add-on/05-RESEARCH.md
@.planning/phases/04-backend-fastapi-add-on/04-01-SUMMARY.md
@.planning/phases/04-backend-fastapi-add-on/04-02-SUMMARY.md
@template/{% if has_backend %}app{% endif %}/api.py.jinja2
@tests/test_phase04_scaffold_polarity.py
@tests/_helpers.py

<interfaces>
<!-- API surface to consume (REVIEW-CHECKLIST §4 anti-drift). -->

From 05-02 harness/llm.py:
- `from harness.llm import llm_call, cost_budget`
- Decorator order: @cost_budget OUTER, @llm_call INNER
- Span attributes: gen_ai.operation.name, verify_kit.cost_usd, verify_kit.latency_ms, verify_kit.routing_path

From Phase 4 04-02 api.py:
- Existing routes: GET /healthz, POST /echo, GET /events/stream
- Router pattern: `router = APIRouter(); @router.<method>("/path", response_model=...)`
- Settings accessed via `request.app.state.settings`

From pydantic-ai v1.100+ (RESEARCH.md Pitfall 4):
- `Agent("anthropic:claude-haiku-4-5", output_type=str)`
- `result = await agent.run(prompt); summary = result.output` — NEVER result.data

From tests/_helpers.py (Phase 4 04-01 SUMMARY):
- `render_scratch_project(dst, data={...})` is the canonical scratch-render helper using copier's Python API

From tests/test_phase04_scaffold_polarity.py:
- Existing 3 test functions establish the polarity contract pattern; this phase ADDS a parallel file at tests/test_phase05_polarity.py (separate file per phase, do not edit Phase 4's)

From Phase 3 .claude/skills/verify-kit-eval/SKILL.md.jinja2:
- Exists as a stub from Phase 3; this plan FILLS IT IN (does not create from scratch)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append POST /summarize to app/api.py.jinja2 (D-12 composition)</name>
  <files>template/{% if has_backend %}app{% endif %}/api.py.jinja2</files>
  <read_first>
    - template/{% if has_backend %}app{% endif %}/api.py.jinja2 (FULL — must understand Phase 4's exact router/import structure before appending)
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "Pattern 4: /summarize Endpoint Append" — verified shape; section "Pitfall 4" — result.output not result.data; section "Pitfall 6" — APPEND, never rewrite)
    - .planning/phases/05-llm-add-on/05-CONTEXT.md (D-12, D-13)
  </read_first>
  <behavior>
    - Test 1: When has_backend=true AND has_llm=true, the rendered app exposes POST /summarize accepting `{"text": "..."}` and returning `{"summary": str, "cost_usd": float, "latency_ms": float}`.
    - Test 2: The /summarize route handler chain uses @cost_budget OUTER + @llm_call(name="summarize") INNER (forcing-function test: grep the rendered file for the decorator order).
    - Test 3: The handler body uses `result.output` (NOT result.data — Pitfall 4 forcing function).
    - Test 4: When has_llm=false, /summarize is NOT in app.routes (polarity), and api.py contains no `from harness.llm` import.
    - Test 5: When has_backend=false, app/ does not exist at all (Phase 4 polarity preserved).
  </behavior>
  <action>
    Read the existing api.py.jinja2 in full first. Use Edit with precise old_string anchored to the last existing route (likely /events/stream) and new_string that PRESERVES the old route and APPENDS a `{% if has_llm %}` block AFTER it. Per Pitfall 6, NEVER rewrite the file — only Edit-append.

    The appended block contains:
    (1) `from harness.llm import llm_call, cost_budget` and `from pydantic_ai import Agent` and necessary pydantic models (SummarizeRequest with single `text: str` field; SummarizeResponse with `summary: str`, `cost_usd: float`, `latency_ms: float`)
    (2) Module-level Agent instance: `_summarize_agent = Agent("anthropic:claude-haiku-4-5", output_type=str, system_prompt="Summarize the input in one sentence.")`
    (3) Internal decorated function `_summarize(text: str) -> str` with `@cost_budget(usd=0.05, on_exceed="raise")` (OUTER) and `@llm_call(name="summarize")` (INNER) — ordering rule from Pitfall 3 MUST appear as an inline comment above the stack
    (4) Inside `_summarize`: `result = await _summarize_agent.run(text); return result.output` — use `result.output`, NEVER `result.data` (Pitfall 4)
    (5) Route handler `@router.post("/summarize", response_model=SummarizeResponse) async def summarize_route(req: SummarizeRequest) -> SummarizeResponse` — captures monotonic start time, calls `await _summarize(req.text)`, returns SummarizeResponse with summary, cost_usd=0.0 placeholder (real cost lives in span attribute per claude-agent-sdk asymmetry from Pitfall 5), and computed latency_ms

    Per REVIEW-CHECKLIST §1 no relative paths; §2 the route body's `return SummarizeResponse(...)` is the final statement; §6 no meta-comments — the Pitfall 3 inline comment is permanent reference, not meta. The endpoint ships UNAUTHENTICATED matching Phase 4's /echo posture (RESEARCH.md V4 ASVS note — documented as starter scaffold; consumer adds auth).
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-05-sum && uv run --no-project python -c "
import subprocess, pathlib
src='/Users/moiz/Documents/code/verify-kit'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-05-sum'], check=True)
api = pathlib.Path('/tmp/scratch-05-05-sum/app/api.py').read_text()
# Phase 4 routes preserved (Pitfall 6: append, never rewrite)
assert '/healthz' in api and '/echo' in api, 'Phase 4 routes lost — REGRESSION'
# Phase 5 route present
assert '/summarize' in api
# Decorator order: cost_budget appears BEFORE llm_call on adjacent decorator lines
import re
m = re.search(r'@cost_budget\([^)]*\)\s*\n\s*@llm_call', api)
assert m, 'Decorator order wrong (must be @cost_budget OUTER, @llm_call INNER per Pitfall 3)'
# Pitfall 4: must use .output not .data
assert '.output' in api and '.data' not in api.replace('output_type', '').replace('output:', ''), 'Pitfall 4: use result.output not result.data'
# Polarity (has_llm=false)
subprocess.run(['rm','-rf','/tmp/scratch-05-05-sum-noLLM'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=false','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-05-sum-noLLM'], check=True)
api2 = pathlib.Path('/tmp/scratch-05-05-sum-noLLM/app/api.py').read_text()
assert '/summarize' not in api2 and 'from harness.llm' not in api2, '/summarize leaked into has_llm=false'
assert '/healthz' in api2, 'Phase 4 routes lost in noLLM render'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - (has_backend=true AND has_llm=true) render contains both Phase 4 routes (/healthz, /echo) AND /summarize
    - Decorator stack on _summarize is @cost_budget IMMEDIATELY followed by @llm_call on the next line (OUTER over INNER per Pitfall 3)
    - api.py contains `.output` (Pitfall 4) and does NOT contain `result.data`
    - (has_backend=true AND has_llm=false) render contains Phase 4 routes but NOT /summarize, NOT `from harness.llm` import
    - (has_backend=false) render does not contain app/api.py at all (Phase 4 polarity preserved)
  </acceptance_criteria>
  <done>/summarize appended without disturbing Phase 4 routes; decorator order + result.output enforced; four-cell polarity preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fill in verify-kit-eval/SKILL.md.jinja2 (D-14)</name>
  <files>template/{% if has_claude_code %}.claude{% endif %}/skills/verify-kit-eval/SKILL.md.jinja2</files>
  <read_first>
    - template/{% if has_claude_code %}.claude{% endif %}/skills/verify-kit-eval/SKILL.md.jinja2 (existing Phase 3 stub — extend, do not rewrite shape)
    - .planning/phases/05-llm-add-on/05-CONTEXT.md (D-14, D-15)
    - .planning/phases/05-llm-add-on/05-03-PLAN.md (eval check details — id, tier, skip-by-default)
    - .planning/phases/05-llm-add-on/05-04-PLAN.md (`just eval` recipe, EVAL_BUDGET_USD)
  </read_first>
  <action>
    Read the existing stub. Replace the placeholder content with substantive content per D-14, covering five sections:
    (1) **When to run evals** — opt-in via `just eval` (D-15: `just verify` does NOT call eval); examples of when the user-facing agent SHOULD trigger an eval (after a prompt edit, before merging a PR that touches LLM logic, after dependency bump for pydantic-ai/litellm)
    (2) **How to interpret .verify/eval-results.json** — promptfoo writes results here; structure has `prompts`, `tests`, `results[].score`, `results[].passed`; the score per scorer type and the threshold convention
    (3) **The decorator ordering rule** — @cost_budget OUTER, @llm_call INNER. Restate Pitfall 3 verbatim so a consumer's agent reading this skill cannot regress it.
    (4) **How to use fix_propose MCP tool to propose prompt edits** — invoke `mcp_call fix_propose '{"target": "eval", "failure_id": "..."}'` to get a unified-diff prompt patch
    (5) **When to escalate to human** — when CostBudgetExceeded fires (budget cap is human policy, not agent decision); when eval results regress below threshold despite multiple fix attempts; when claude-agent-sdk path returns $0 cost but operator expects real cost (mismatch indicates routing misconfiguration per Pitfall 5)

    Per REVIEW-CHECKLIST §6, no meta-comments. The Pitfall 3 paragraph is permanent reference. Match the lazy-load skill format from Phase 3 (frontmatter with `description:` so Claude can match the skill to user requests).
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-05-skill && uv run --no-project python -c "
import subprocess, pathlib
src='/Users/moiz/Documents/code/verify-kit'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--data','has_claude_code=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-05-skill'], check=True)
p = pathlib.Path('/tmp/scratch-05-05-skill/.claude/skills/verify-kit-eval/SKILL.md')
assert p.exists()
text = p.read_text()
# Must cover all five sections
for keyword in ['just eval', 'eval-results.json', 'cost_budget', 'fix_propose', 'escalate']:
    assert keyword in text, f'{keyword} missing from SKILL.md'
# Pitfall 3 must appear
assert 'OUTER' in text and 'INNER' in text
assert len(text.splitlines()) >= 30, 'SKILL.md too thin'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains all five sections (just eval, eval-results.json, cost_budget OUTER/INNER, fix_propose, escalation)
    - File is >=30 lines (no longer a stub)
    - has_claude_code=false render does NOT contain the file (Phase 3 polarity preserved)
  </acceptance_criteria>
  <done>SKILL.md filled with substantive content; decorator-ordering rule documented for the consumer's agent; D-14 satisfied.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append LLM-12 section to README.md.jinja2 (migration story + setup paths)</name>
  <files>template/README.md.jinja2</files>
  <read_first>
    - template/README.md.jinja2 (existing — append under {% if has_llm %}, do NOT rewrite)
    - .planning/phases/05-llm-add-on/05-CONTEXT.md (D-04, D-06, D-08, D-17, D-18)
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "Pitfall 8" — Langfuse Basic Auth idiom for documentation)
  </read_first>
  <action>
    Append a `{% if has_llm %}` block to template/README.md.jinja2 with these subsections (per D-17, README is Jinja-rendered per consumer project — refer to `{{ project_name }}` where appropriate):

    (a) **Personal setup vs. consumer setup** (D-04, D-18): explain that the project's local-dev path uses `claude-agent-sdk` (no API key — leverages the developer's Claude Max OAuth) and consumer-deployed code uses ANTHROPIC_API_KEY. Routing is automatic via D-03 in harness/llm.py.

    (b) **Langfuse credential sharing** (D-06): the operator pattern is to set LANGFUSE_PUBLIC_KEY / LANGFUSE_SECRET_KEY / LANGFUSE_HOST in `~/.zshrc` once after Cloud signup. Every verify-kit-scaffolded project picks them up automatically without touching .env. Show the Basic-Auth idiom from Pitfall 8 (NOT Bearer): `OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic $(echo -n "$LANGFUSE_PUBLIC_KEY:$LANGFUSE_SECRET_KEY" | base64)`.

    (c) **`llm_backend=none` for privacy** (D-08): explain that `none` ships zero third-party LLM observability; spans go to Jaeger (when has_backend=true) or stdout. Documented as the minimal-dependency option.

    (d) **Cloud Hobby → Hetzner CX32 self-host migration** (D-17, LLM-12): concrete steps in order:
       1. Spin up Hetzner CX32 (~€7.60/mo) with Docker installed
       2. `docker compose -f docker-compose.langfuse.yml up -d` (the file from 05-04)
       3. Update `LANGFUSE_HOST` env var from `https://cloud.langfuse.com` to `http://<cx32-ip>:3000` (or use Tailscale for loopback-secure access — RESEARCH.md T-05-12)
       4. Cloud → self-host data backup: explain that Langfuse Cloud Hobby tier offers data export from the dashboard; download the JSON dump then `langfuse-cli import` on the self-host instance (or document the SDK manual-trace approach if the CLI is not available — RESEARCH.md A2 caveat)
       5. DNS update if using a custom domain
       6. .env migration: same env-var names, just different LANGFUSE_HOST value
       7. Verify migration with a test trace via `just verify --check=eval` (or the eval check from 05-03)

    Per REVIEW-CHECKLIST §6 no meta-comments. Per §5 the {% if has_llm %} and {% endif %} on their own lines. Polarity: has_llm=false README does NOT contain the LLM section.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-05-readme && uv run --no-project python -c "
import subprocess, pathlib
src='/Users/moiz/Documents/code/verify-kit'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-05-readme'], check=True)
readme = pathlib.Path('/tmp/scratch-05-05-readme/README.md').read_text()
for keyword in ['claude-agent-sdk','ANTHROPIC_API_KEY','Langfuse','~/.zshrc','Hetzner','self-host','llm_backend=none']:
    assert keyword in readme, f'{keyword} missing from README LLM section'
# Pitfall 8: Basic not Bearer in the idiom
assert 'Basic' in readme and 'OTEL_EXPORTER_OTLP_HEADERS' in readme
# Polarity
subprocess.run(['rm','-rf','/tmp/scratch-05-05-readme-off'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=false','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-05-readme-off'], check=True)
off = pathlib.Path('/tmp/scratch-05-05-readme-off/README.md').read_text()
assert 'Langfuse' not in off and 'Hetzner' not in off, 'README LLM section leaked into has_llm=false'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - has_llm=true README contains all four subsections (personal-vs-consumer, ~/.zshrc Langfuse pattern, none privacy option, Hetzner CX32 migration with concrete steps)
    - README contains `Basic` (Pitfall 8) inside the OTEL_EXPORTER_OTLP_HEADERS idiom, NOT `Bearer`
    - has_llm=false README does NOT contain Langfuse / Hetzner / migration content
  </acceptance_criteria>
  <done>README LLM-12 section landed with migration story + three setup paths; satisfies D-04, D-06, D-08, D-17, D-18.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create tests/test_phase05_polarity.py (12-cell matrix)</name>
  <files>tests/test_phase05_polarity.py</files>
  <read_first>
    - tests/test_phase04_scaffold_polarity.py (structural reference — same render_scratch_project + rglob pattern)
    - tests/_helpers.py (render_scratch_project helper signature)
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "Test Architecture" — assertions per requirement)
    - .planning/phases/05-llm-add-on/05-01-PLAN.md (the LLM paths gated by _exclude — what should be present/absent per cell)
    - .planning/phases/05-llm-add-on/05-04-PLAN.md (docker-compose.langfuse.yml conditional rendering)
  </read_first>
  <behavior>
    - Twelve parametrized cells: (has_backend ∈ {T,F}) × (has_llm ∈ {T,F}) × (llm_backend ∈ {langfuse-cloud, langfuse-self-host, none}) — but only the has_llm=true axis is parametrized over llm_backend (when has_llm=false, llm_backend is irrelevant), yielding 2×3 + 2 = 8 effective cells. Use pytest.mark.parametrize on the meaningful combos.
    - test_llm_artifacts_polarity: assert presence/absence of `harness/llm.py`, `eval/`, `tests/llm/`, `.github/workflows/nightly-eval.yml` per cell
    - test_docker_compose_langfuse_only_in_self_host_cell: present ONLY in (has_llm=true AND llm_backend=langfuse-self-host)
    - test_summarize_endpoint_polarity: import the rendered app via `importlib` (or grep its source) and assert `/summarize` route is present ONLY in (has_backend=true AND has_llm=true)
    - test_no_empty_segment_leaks: rglob the rendered tree for any path containing `{% if` or empty segments (e.g., `tests//llm`) — banned shape from 04-01 Task T03
    - test_pyproject_has_tokenx_core_not_tokenx: forcing-function for Pitfall 1 — has_llm=true pyproject contains "tokenx-core" and does NOT contain bare `"tokenx"` (regex-anchored)
    - test_no_result_data_anywhere: grep every .py file in has_llm=true render; assert `result.data` substring is absent (Pitfall 4 forcing function)
  </behavior>
  <action>
    Create tests/test_phase05_polarity.py at the repo top-level (NOT under template/ — this tests the repo's own renders per Phase 4 04-01 contract). Use the same render_scratch_project helper from tests/_helpers.py — DO NOT use raw subprocess.run with copier (REVIEW-CHECKLIST §1 cwd risk; the helper uses copier's Python API with explicit dst_path).

    Per REVIEW-CHECKLIST §7, this test file lives at tests/test_phase05_polarity.py (top-level), NOT under tests/backend/ or tests/llm/ — those are paths the harness recursively invokes, and a polarity test that itself runs render_scratch_project would recurse.

    Per REVIEW-CHECKLIST §8, every subprocess call into a scratch project (if any) MUST pass `env=_CLEAN_ENV` to strip VIRTUAL_ENV / UV_PROJECT_ENVIRONMENT / PYTHONPATH from the outer venv. If `_CLEAN_ENV` is not already exposed in tests/_helpers.py, add it (small extension, document in the test docstring).

    Per REVIEW-CHECKLIST §4, every assertion on a symbol name from another plan MUST quote the exact name as it lands. The test for the /summarize route asserts on the string `/summarize` (NOT `summarize_endpoint` or invented variant). The test for harness/llm.py asserts exports match the __all__ committed in 05-02. The test for the eval check asserts `id=eval` (the registered check_id from 05-03).
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase05_polarity.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_phase05_polarity.py exists at repo top-level (NOT under tests/backend/ or tests/llm/ — REVIEW-CHECKLIST §7)
    - All parametrized cells pass under `uv run pytest tests/test_phase05_polarity.py -v`
    - test_docker_compose_langfuse_only_in_self_host_cell asserts presence ONLY in (T, T, "langfuse-self-host") cell
    - test_summarize_endpoint_polarity asserts /summarize ONLY in (has_backend=true AND has_llm=true) cell
    - test_pyproject_has_tokenx_core_not_tokenx forcing-functions Pitfall 1
    - test_no_result_data_anywhere forcing-functions Pitfall 4
    - Existing Phase 4 polarity tests (tests/test_phase04_scaffold_polarity.py) still pass
    - No subprocess.run call without env=_CLEAN_ENV (§8)
  </acceptance_criteria>
  <done>12-cell polarity test passes; entire Phase 5 contract locked under automated assertion; Pitfalls 1 and 4 forcing-functioned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /summarize → external Anthropic API | Untrusted; route handler ships UNAUTHENTICATED matching Phase 4 /echo posture; consumer is expected to add auth before deploy |
| README.md → consumer reading documentation | Trusted (text only); but the Langfuse OTLP idiom MUST show Basic Auth (NOT Bearer per Pitfall 8) or the consumer will produce 401 errors |
| SKILL.md → consumer's Claude Code instance | Trusted (advisory); but the decorator-ordering rule MUST appear verbatim so the agent cannot silently regress it |
| tests/test_phase05_polarity.py → outer process env | Test renders scratch projects; subprocess calls into scratch MUST scrub VIRTUAL_ENV per REVIEW-CHECKLIST §8 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Information Disclosure | /summarize prompt injection → exfiltration | accept | Documented as out-of-scope for v0.1 starter scaffold (RESEARCH.md V4 ASVS note); consumer adds prompt-injection defenses |
| T-05-16 | Denial of Service (wallet) | /summarize cost runaway via large `text` field | mitigate | `@cost_budget(usd=0.05)` decorator caps per-call cost; CostBudgetExceeded → FastAPI default 500 response |
| T-05-17 | Tampering | Future refactor removes Pitfall 3/4 documentation | mitigate | T4 polarity test forcing-functions both: test_no_result_data_anywhere and decorator-ordering assertion (greps the appended block) |
| T-05-18 | Information Disclosure | README accidentally documents the API-key path as the operator's default | mitigate | T3 explicit two-section split (personal vs consumer); test could grep for the specific "claude-agent-sdk" personal-path label |
</threat_model>

<verification>
After all tasks complete:
1. `uv run pytest tests/test_phase04_scaffold_polarity.py tests/test_phase05_polarity.py -v` exits 0
2. has_llm=true AND has_backend=true scratch render's app/api.py contains BOTH Phase 4 routes AND /summarize
3. has_llm=true scratch render's README contains all four LLM subsections (personal-vs-consumer, ~/.zshrc, none privacy, Hetzner migration)
4. SKILL.md for verify-kit-eval is >=30 lines, no longer a Phase 3 stub
5. No `result.data` substring anywhere in has_llm=true renders (Pitfall 4)
6. tokenx-core (not tokenx) in pyproject (Pitfall 1)
7. docker-compose.langfuse.yml present ONLY in (T, T, "langfuse-self-host") cell
</verification>

<success_criteria>
- /summarize endpoint composes Phase 4 × Phase 5 cleanly; both phases' polarity tests pass
- SKILL.md filled in per D-14
- README LLM-12 section covers migration + setup distinctions per D-17, D-18, D-04, D-06, D-08
- 12-cell polarity test catches any regression of the Phase 5 contract
- Pitfalls 1, 3, 4, 6, 8 are all forcing-functioned by tests (not just documented)
- No regression in Phase 4 (existing tests pass)
</success_criteria>

<output>
Create `.planning/phases/05-llm-add-on/05-05-SUMMARY.md` when done. Include:
- The exact /summarize request/response schema as committed (for any future API consumer)
- The polarity test matrix as committed (so Phase 6 self-test workflow knows which cells to exercise)
- The migration steps as committed in README (so LLM-12 acceptance is auditable)
- Confirmation that pytest tests/test_phase04_scaffold_polarity.py still passes (no Phase 4 regression)
</output>
