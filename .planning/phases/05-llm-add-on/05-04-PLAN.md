---
phase: 05-llm-add-on
plan: "05-04"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - template/{% if has_llm %}eval{% endif %}/promptfoo.config.yaml.jinja2
  - template/{% if has_llm %}eval{% endif %}/datasets/golden.jsonl.jinja2
  - template/justfile.jinja2
  - template/.github/workflows/{% if has_llm %}nightly-eval.yml{% endif %}.jinja2
  - template/{% if has_llm %}docker-compose.langfuse.yml{% endif %}.jinja2
autonomous: true
requirements: [LLM-10, LLM-11, CI-05]
must_haves:
  truths:
    - "`just eval` runs `pnpm dlx promptfoo eval -c eval/promptfoo.config.yaml` against the starter golden.jsonl"
    - "`just refresh-cassettes` deletes tests/cassettes/*.yaml and reruns pytest with --record-mode=once"
    - "nightly-eval.yml runs Sunday 04:00 UTC (cron `0 4 * * 0`) per D-09"
    - "nightly-eval.yml refuses to start if EVAL_BUDGET_USD is unset (D-11)"
    - "docker-compose.langfuse.yml is rendered ONLY when llm_backend=langfuse-self-host"
    - "promptfoo.config.yaml uses EVAL_MODEL env var (D-10), defaults to claude-haiku-4-5"
    - "golden.jsonl ships 5 starter rows covering factuality, equals, contains, answer-relevance, moderation scorers (D-19)"
  artifacts:
    - path: "template/{% if has_llm %}eval{% endif %}/promptfoo.config.yaml.jinja2"
      provides: "Promptfoo config wired to datasets/golden.jsonl"
      contains: "EVAL_MODEL"
    - path: "template/{% if has_llm %}eval{% endif %}/datasets/golden.jsonl.jinja2"
      provides: "5 starter rows demonstrating each scorer type"
      min_lines: 5
    - path: "template/justfile.jinja2"
      provides: "Two appended recipes: eval, refresh-cassettes under {% if has_llm %}"
      contains: "promptfoo"
    - path: "template/.github/workflows/{% if has_llm %}nightly-eval.yml{% endif %}.jinja2"
      provides: "Weekly cost-capped eval workflow with EVAL_BUDGET_USD pre-flight gate"
      contains: "0 4 * * 0"
    - path: "template/{% if has_llm %}docker-compose.langfuse.yml{% endif %}.jinja2"
      provides: "5-container self-host stack (web, worker, postgres, clickhouse, redis) — rendered only when llm_backend=langfuse-self-host"
  key_links:
    - from: "nightly-eval.yml pre-flight step"
      to: "EVAL_BUDGET_USD env"
      via: "shell guard: if [ -z $EVAL_BUDGET_USD ]; then exit 1; fi"
      pattern: "EVAL_BUDGET_USD"
    - from: "justfile eval recipe"
      to: "eval/promptfoo.config.yaml"
      via: "promptfoo eval -c eval/promptfoo.config.yaml"
      pattern: "promptfoo.config.yaml"
---

<objective>
Land the four out-of-process eval artifacts: Promptfoo config + starter dataset, two justfile recipes (`just eval`, `just refresh-cassettes`), the weekly nightly-eval.yml workflow with cost cap, and the self-host docker-compose.langfuse.yml (rendered only when llm_backend=langfuse-self-host).

Purpose: Give the operator a runnable eval gate from day one — `just eval` exits 0 against synthetic starter data, the weekly CI catches drift without daily-spend anxiety, and the self-host docker-compose is a real artifact (not a stub) for users who choose privacy over the cloud default.

Output: Five new template files; one extension to justfile.jinja2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/REVIEW-CHECKLIST.md
@.planning/phases/05-llm-add-on/05-CONTEXT.md
@.planning/phases/05-llm-add-on/05-RESEARCH.md
@.planning/phases/05-llm-add-on/05-01-PLAN.md
@template/justfile.jinja2

<interfaces>
<!-- API surface from previously-landed code (REVIEW-CHECKLIST §4 anti-drift). -->

From template/justfile.jinja2: existing recipes use the pattern `{% if has_backend %}<recipe>{% endif %}` at recipe-block boundaries. Phase 4 recipes (just docker-up, etc.) are the structural reference. Recipe names must appear in TOOL-02's canonical list — `eval` and `refresh-cassettes` are already listed.

From 05-01 SUMMARY (after landing): copier.yml _exclude block already gates `eval`, `eval/**`, `docker-compose.langfuse.yml`, `.github/workflows/nightly-eval.yml` under `{% if not has_llm %}`. This plan creates the files those entries gate.

From RESEARCH.md "LLM-11 — Promptfoo config + dataset":
- providers shape: `[{id: "anthropic:messages:{{env.EVAL_MODEL}}", config: {apiKey: "{{env.ANTHROPIC_API_KEY}}"}}]` — Jinja-template `{{env.X}}` is promptfoo's own env interpolation, NOT copier Jinja. Use `{% raw %}{{env.X}}{% endraw %}` to escape.
- tests reference: `file://datasets/golden.jsonl`
- assertion types verified against promptfoo.dev: factuality, equals, contains, answer-relevance, moderation

From RESEARCH.md "LLM-CI-05 — nightly-eval.yml shape":
- Cron: `"0 4 * * 0"` (Sunday 04:00 UTC per D-09)
- Required env vars: ANTHROPIC_API_KEY, LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY from GitHub secrets; EVAL_MODEL=claude-haiku-4-5 (D-10); EVAL_BUDGET_USD="1.00" (D-11)
- Pre-flight guard: `if [ -z "$EVAL_BUDGET_USD" ]; then echo "::error::EVAL_BUDGET_USD must be set" && exit 1; fi`
- pnpm setup via pnpm/action-setup@v3 then `pnpm dlx promptfoo eval -c eval/promptfoo.config.yaml --max-concurrency 2`

GitHub Actions Jinja-vs-syntax escape: GitHub Actions uses `${{ secrets.X }}` which collides with copier's `{{ }}` delimiter — must wrap as `${{ '{{ secrets.X }}' }}` or use `{% raw %}` / `{% endraw %}` blocks (RESEARCH.md example shows the wrap pattern).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create eval/promptfoo.config.yaml.jinja2 + eval/datasets/golden.jsonl.jinja2</name>
  <files>template/{% if has_llm %}eval{% endif %}/promptfoo.config.yaml.jinja2, template/{% if has_llm %}eval{% endif %}/datasets/golden.jsonl.jinja2</files>
  <read_first>
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "LLM-11 — Promptfoo config + dataset" — verified config shape)
    - .planning/phases/05-llm-add-on/05-CONTEXT.md (D-19, D-20 — starter dataset spec)
  </read_first>
  <action>
    Create both files. `promptfoo.config.yaml.jinja2`: declare a single anthropic provider whose `id` is `anthropic:messages:{% raw %}{{env.EVAL_MODEL}}{% endraw %}` (promptfoo's own env interpolation — wrap with copier `{% raw %}` so copier does NOT consume the braces) and whose `config.apiKey` is `{% raw %}{{env.ANTHROPIC_API_KEY}}{% endraw %}`. `tests: file://datasets/golden.jsonl`. `defaultTest.options.cache: true`. Add a YAML comment stating EVAL_BUDGET_USD is enforced by nightly-eval.yml's pre-flight step (NOT by promptfoo itself — RESEARCH.md Assumption A6).

    `golden.jsonl.jinja2`: ship exactly 5 JSON lines, each a complete object with `vars`, `assert`, and `_comment` keys. The five rows demo (D-19): factuality, equals, contains, answer-relevance, moderation. Cost stays under $0.005 per RESEARCH.md D-20 with EVAL_MODEL=claude-haiku-4-5. Each `_comment` field explains the scorer being demoed and tells the consumer "Replace with your actual use case." No real PII (Security threat T-05-13 from RESEARCH.md) — synthetic examples only.

    Both files sit under Shape 1 gate (`eval/` is LLM-unique). Per REVIEW-CHECKLIST §5, `{% raw %}/{% endraw %}` blocks span at most a fragment of a line within the YAML and JSONL — these are leaf substitutions, NOT inline-conditional dropouts that would collapse line boundaries.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-04-pf && uv run --no-project python -c "
import subprocess, pathlib, yaml, json
src='/Users/moiz/Documents/code/verify-kit'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--data','llm_backend=langfuse-cloud','--vcs-ref','HEAD',src,'/tmp/scratch-05-04-pf'], check=True)
cfg = yaml.safe_load(pathlib.Path('/tmp/scratch-05-04-pf/eval/promptfoo.config.yaml').read_text())
assert 'providers' in cfg and 'tests' in cfg
assert '{{env.EVAL_MODEL}}' in str(cfg) or 'EVAL_MODEL' in str(cfg), 'EVAL_MODEL interpolation missing'
gold_path = pathlib.Path('/tmp/scratch-05-04-pf/eval/datasets/golden.jsonl')
lines = [l for l in gold_path.read_text().splitlines() if l.strip()]
assert len(lines) == 5, f'expected 5 rows, got {len(lines)}'
scorers = set()
for ln in lines:
    obj = json.loads(ln)
    for a in obj['assert']:
        scorers.add(a['type'])
required = {'factuality','equals','contains','answer-relevance','moderation'}
assert scorers == required, f'scorers {scorers} != {required}'
# Polarity
subprocess.run(['rm','-rf','/tmp/scratch-05-04-pf-off'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=false','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-04-pf-off'], check=True)
assert not pathlib.Path('/tmp/scratch-05-04-pf-off/eval').exists(), 'eval/ leaked'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - eval/promptfoo.config.yaml parses as YAML and contains EVAL_MODEL env-var interpolation
    - eval/datasets/golden.jsonl has exactly 5 JSON-valid lines
    - Five distinct assert types: factuality, equals, contains, answer-relevance, moderation
    - has_llm=false render does NOT contain eval/ directory
    - {% raw %} blocks correctly preserve promptfoo's {{env.X}} syntax
  </acceptance_criteria>
  <done>Promptfoo config + 5-row starter dataset land under has_llm gate; YAML/JSONL parse; polarity verified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append `eval` and `refresh-cassettes` recipes to justfile.jinja2</name>
  <files>template/justfile.jinja2</files>
  <read_first>
    - template/justfile.jinja2 (existing — observe Phase 4 has_backend recipe block placement; APPEND, do not rewrite per REVIEW-CHECKLIST §4)
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "`just eval` recipe")
    - .planning/phases/05-llm-add-on/05-CONTEXT.md (D-15 — eval stays opt-in, NOT called from just verify)
  </read_first>
  <action>
    Read the existing justfile.jinja2 in full. Locate the Phase 4 `{% if has_backend %} ... {% endif %}` block — model the new block on its shape. Append a new `{% if has_llm %} ... {% endif %}` block at the end with two recipes:

    (a) `eval`: runs `pnpm dlx promptfoo eval -c eval/promptfoo.config.yaml`. Doc comment above it explains: "Opt-in eval gate (D-15) — NOT called by `just verify`. Requires Node + pnpm + ANTHROPIC_API_KEY."

    (b) `refresh-cassettes`: two commands — `rm -f tests/cassettes/*.yaml` then `uv run pytest tests/llm/ --record-mode=once`. Doc comment: "WARNING: hits the live LLM API to re-record cassettes; requires ANTHROPIC_API_KEY set and consumes real tokens."

    Critically, per D-15, `just verify` MUST NOT call `just eval`. Do NOT add an `eval` invocation to any other recipe (verify, verify --quick, etc.). The eval recipe stands alone.

    Per REVIEW-CHECKLIST §5, the `{% if has_llm %}` and `{% endif %}` go on their own lines; per §6 no meta-comments. Per TOOL-02, both recipe names (`eval`, `refresh-cassettes`) are pre-declared in the canonical list — this task fulfills the LLM-conditional portion.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-04-just && uv run --no-project python -c "
import subprocess, pathlib
src='/Users/moiz/Documents/code/verify-kit'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-04-just'], check=True)
text = pathlib.Path('/tmp/scratch-05-04-just/justfile').read_text()
assert 'promptfoo' in text and 'eval' in text
assert 'refresh-cassettes' in text and '--record-mode=once' in text
# Forcing function for D-15 — verify recipe must NOT contain promptfoo invocation
verify_section = text.split('verify')[1].split('eval')[0] if 'verify' in text else ''
# crude: locate the 'verify:' recipe body region and assert it doesn't call promptfoo
# Polarity
subprocess.run(['rm','-rf','/tmp/scratch-05-04-just-off'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=false','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-04-just-off'], check=True)
off = pathlib.Path('/tmp/scratch-05-04-just-off/justfile').read_text()
assert 'promptfoo' not in off and 'refresh-cassettes' not in off, 'justfile LLM block leaked'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - has_llm=true justfile contains `eval` recipe invoking promptfoo
    - has_llm=true justfile contains `refresh-cassettes` recipe with --record-mode=once
    - `verify` recipe body does NOT call eval / promptfoo (D-15 polarity)
    - has_llm=false justfile contains NONE of: promptfoo, refresh-cassettes
    - {% if %} / {% endif %} on their own lines (§5)
  </acceptance_criteria>
  <done>Two recipes appended; D-15 invariant preserved (verify does not invoke eval).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create .github/workflows/nightly-eval.yml.jinja2 with EVAL_BUDGET_USD pre-flight gate</name>
  <files>template/.github/workflows/{% if has_llm %}nightly-eval.yml{% endif %}.jinja2</files>
  <read_first>
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "LLM-CI-05 — nightly-eval.yml shape" — verified YAML)
    - .planning/phases/05-llm-add-on/05-CONTEXT.md (D-09, D-10, D-11)
  </read_first>
  <action>
    Create the workflow file under Shape 2 gate (filename-level — parent dir .github/workflows/ is universal). Use exactly the schema from RESEARCH.md "LLM-CI-05":
    - `name: nightly-eval`
    - `on.schedule.[0].cron: "0 4 * * 0"` (Sunday 04:00 UTC per D-09)
    - `on.workflow_dispatch: {}` (manual trigger)
    - `jobs.eval.runs-on: ubuntu-latest`
    - `env`: ANTHROPIC_API_KEY/LANGFUSE_PUBLIC_KEY/LANGFUSE_SECRET_KEY from GitHub secrets (wrap as `${{ '{{ secrets.NAME }}' }}` per the escape pattern from RESEARCH.md), EVAL_MODEL=claude-haiku-4-5 (D-10), EVAL_BUDGET_USD="1.00" (D-11)
    - Steps in order: (1) `actions/checkout@v4`, (2) Cost-cap pre-flight: shell step running `if [ -z "$EVAL_BUDGET_USD" ]; then echo "::error::EVAL_BUDGET_USD must be set" && exit 1; fi; echo "Budget cap = $EVAL_BUDGET_USD USD"`, (3) `pnpm/action-setup@v3` with version 9, (4) `pnpm dlx promptfoo eval -c eval/promptfoo.config.yaml --max-concurrency 2`, (5) `actions/upload-artifact@v4` with name=eval-results path=./.promptfoo/

    Critical per REVIEW-CHECKLIST §5: GitHub Actions YAML is structured-text — every `${{ ... }}` substitution wrapped properly so copier doesn't consume the braces. The pre-flight guard is the LOAD-BEARING gate per D-11 and RESEARCH.md A6 (promptfoo's own cost estimate is informational only).
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-04-ci && uv run --no-project python -c "
import subprocess, pathlib, yaml
src='/Users/moiz/Documents/code/verify-kit'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-04-ci'], check=True)
p = pathlib.Path('/tmp/scratch-05-04-ci/.github/workflows/nightly-eval.yml')
assert p.exists(), 'nightly-eval.yml missing'
text = p.read_text()
data = yaml.safe_load(text)
schedule = data.get('on', {}).get('schedule') if isinstance(data.get('on'), dict) else None
# yaml may parse 'on' as True due to YAML 1.1 — handle both
on_block = data.get('on') or data.get(True)
assert on_block, 'on: block missing'
crons = [s.get('cron') for s in (on_block.get('schedule') or [])]
assert '0 4 * * 0' in crons, f'cron 0 4 * * 0 missing, got {crons}'
assert 'EVAL_BUDGET_USD' in text and 'exit 1' in text, 'pre-flight gate missing'
assert 'promptfoo eval' in text
assert 'secrets.ANTHROPIC_API_KEY' in text
# Polarity
subprocess.run(['rm','-rf','/tmp/scratch-05-04-ci-off'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=false','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-04-ci-off'], check=True)
assert not pathlib.Path('/tmp/scratch-05-04-ci-off/.github/workflows/nightly-eval.yml').exists()
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/nightly-eval.yml exists in has_llm=true render and parses as YAML
    - on.schedule.cron == "0 4 * * 0" (D-09)
    - env.EVAL_BUDGET_USD set to "1.00" (D-11)
    - env.EVAL_MODEL set to "claude-haiku-4-5" (D-10)
    - Pre-flight step contains `if [ -z "$EVAL_BUDGET_USD" ]; then ... exit 1`
    - Promptfoo invocation present
    - GitHub secrets correctly escaped (`secrets.ANTHROPIC_API_KEY` appears verbatim post-render)
    - has_llm=false render does NOT contain nightly-eval.yml (CI-05 polarity)
  </acceptance_criteria>
  <done>Weekly cost-capped CI workflow shipped; pre-flight guard refuses to run without EVAL_BUDGET_USD.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create docker-compose.langfuse.yml.jinja2 (rendered only when llm_backend=langfuse-self-host)</name>
  <files>template/{% if has_llm %}docker-compose.langfuse.yml{% endif %}.jinja2</files>
  <read_first>
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (D-07 — 5-container stack: web + worker + postgres + clickhouse + redis, ~900 MB RAM)
    - .planning/phases/05-llm-add-on/05-CONTEXT.md (D-07, D-08)
    - template/{% if has_backend %}docker-compose.yml{% endif %}.jinja2 (Phase 4 reference for compose shape — services, environment, networks, volumes)
  </read_first>
  <action>
    Create template/{% if has_llm %}docker-compose.langfuse.yml{% endif %}.jinja2 (Shape 1 path gate — top-level unique-name).

    Suppression of the file in non-self-host cells is handled by 05-01 — its copier.yml _exclude block contains a `{% if llm_backend != "langfuse-self-host" %}docker-compose.langfuse.yml{% endif %}` entry. THIS task does NOT touch copier.yml — file ownership rests with 05-01 (Wave 1). This task only creates the .jinja2 source file; copier's _exclude evaluation handles the cell-matrix suppression.

The .jinja2 file body itself is NOT wrapped in an inner `{% if llm_backend == ... %}` block — the body unconditionally emits the docker-compose YAML. Copier's _exclude (set in 05-01) is the load-bearing gate. This keeps the source file readable and matches the Phase 4 docker-compose.yml.jinja2 pattern (which also has no inline llm_backend conditional inside the body).

    File contents per D-07: services langfuse-web (port 127.0.0.1:3000:3000 per RESEARCH.md security T-05-12), langfuse-worker, postgres, clickhouse, redis. Bind volumes for postgres + clickhouse data persistence. NETWORKS isolated to a `langfuse-net` so Phase 4's docker-compose.yml (api+postgres+jaeger) doesn't accidentally cross-connect. Environment variables reference `${LANGFUSE_PUBLIC_KEY}` / `${LANGFUSE_SECRET_KEY}` etc. from .env (the operator's shell env per D-06 also flows here).

    Per RESEARCH.md security T-05-12: langfuse-web port binding MUST be `127.0.0.1:3000:3000` (loopback only), NOT `3000:3000` (which binds 0.0.0.0). For remote access use Tailscale, not 0.0.0.0 exposure.

    Per REVIEW-CHECKLIST §5, inline conditionals in YAML must not collapse lines — each `{% if %}` / `{% endif %}` on its own line. Per §6, no meta-comments in the rendered file.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-04-{self,cloud,none,off} && uv run --no-project python -c "
import subprocess, pathlib, yaml
src='/Users/moiz/Documents/code/verify-kit'
def render(name, data):
    args = ['copier','copy','--trust','--defaults','--vcs-ref','HEAD']
    for k,v in data.items(): args += ['--data', f'{k}={v}']
    args += [src, f'/tmp/scratch-05-04-{name}']
    subprocess.run(args, check=True)
render('self', {'has_llm':'true','has_backend':'true','llm_backend':'langfuse-self-host'})
render('cloud', {'has_llm':'true','has_backend':'true','llm_backend':'langfuse-cloud'})
render('none', {'has_llm':'true','has_backend':'true','llm_backend':'none'})
render('off', {'has_llm':'false','has_backend':'true'})

self_path = pathlib.Path('/tmp/scratch-05-04-self/docker-compose.langfuse.yml')
assert self_path.exists(), 'self-host should ship the compose file'
parsed = yaml.safe_load(self_path.read_text())
assert 'services' in parsed
services = parsed['services']
for s in ['langfuse-web','langfuse-worker','postgres','clickhouse','redis']:
    assert s in services, f'service {s} missing'
# Loopback binding
web = services['langfuse-web']
ports = web.get('ports', [])
assert any('127.0.0.1:3000:3000' in str(p) for p in ports), f'must bind loopback only: {ports}'

for name in ['cloud','none','off']:
    p = pathlib.Path(f'/tmp/scratch-05-04-{name}/docker-compose.langfuse.yml')
    assert not p.exists(), f'{name}: docker-compose.langfuse.yml should be excluded'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - llm_backend=langfuse-self-host render contains docker-compose.langfuse.yml that parses as YAML
    - YAML has 5 services: langfuse-web, langfuse-worker, postgres, clickhouse, redis (D-07)
    - langfuse-web ports list contains 127.0.0.1:3000:3000 (loopback per T-05-12), NOT 3000:3000 alone
    - llm_backend=langfuse-cloud render does NOT contain docker-compose.langfuse.yml
    - llm_backend=none render does NOT contain docker-compose.langfuse.yml
    - has_llm=false render does NOT contain docker-compose.langfuse.yml
    - copier.yml has an _exclude entry `{% if llm_backend != "langfuse-self-host" %}docker-compose.langfuse.yml{% endif %}` (or equivalent gating logic)
  </acceptance_criteria>
  <done>Self-host compose file ships only in the (has_llm AND llm_backend=langfuse-self-host) cell; loopback binding enforced; three other cells exclude it cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nightly-eval.yml → Anthropic API | Untrusted network; cost-bounded by EVAL_BUDGET_USD pre-flight gate |
| docker-compose.langfuse.yml → operator's local docker daemon | Trusted (local); but the langfuse-web port binding MUST be loopback so a misconfigured firewall doesn't expose the dashboard to LAN |
| golden.jsonl → CI runner → Anthropic | Untrusted; starter rows are synthetic (no PII per T-05-13) |
| GitHub secrets → workflow env vars | Trusted; escaped via `${{ '{{ secrets.X }}' }}` to survive copier render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Denial of Service (wallet) | nightly-eval cost runaway | mitigate | Pre-flight step refuses to run without EVAL_BUDGET_USD; cron is weekly (D-09), not daily; default cap $1.00 (D-11) |
| T-05-12 | Spoofing | Self-host langfuse-web exposed on 0.0.0.0 | mitigate | port binding is `127.0.0.1:3000:3000` (loopback); remote access via Tailscale documented in README LLM-12 (05-05) |
| T-05-13 | Information Disclosure | golden.jsonl with real PII | mitigate | Starter rows are synthetic per D-19; row headers comment "Replace with your actual use case" |
| T-05-14 | Tampering | promptfoo's cost-estimate as the load-bearing gate | mitigate | EVAL_BUDGET_USD enforcement is in the workflow pre-flight step, NOT in promptfoo (RESEARCH.md A6); promptfoo's `--max-concurrency 2` is the only knob that touches its own runtime |
</threat_model>

<verification>
1. eval/promptfoo.config.yaml + eval/datasets/golden.jsonl exist in has_llm=true render
2. golden.jsonl has exactly 5 rows demoing factuality/equals/contains/answer-relevance/moderation
3. justfile has `eval` + `refresh-cassettes` recipes under {% if has_llm %}; `verify` does NOT invoke them
4. .github/workflows/nightly-eval.yml has cron "0 4 * * 0" + EVAL_BUDGET_USD pre-flight gate
5. docker-compose.langfuse.yml present ONLY in (has_llm=true AND llm_backend=langfuse-self-host)
6. langfuse-web port binding is loopback-only (127.0.0.1:3000:3000)
7. has_llm=false renders contain NONE of: eval/, justfile recipes, nightly-eval.yml, docker-compose.langfuse.yml
</verification>

<success_criteria>
- `just eval` runs without error in scratch render (subject to ANTHROPIC_API_KEY availability)
- nightly-eval.yml cron is exactly `0 4 * * 0` (D-09)
- Self-host docker-compose ships only in the matching llm_backend cell
- Polarity holds across all four (has_llm × llm_backend) cells
- No new gate shape introduced — Shape 1 + Shape 2 only
</success_criteria>

<output>
Create `.planning/phases/05-llm-add-on/05-04-SUMMARY.md` when done. Include:
- The exact list of 5 scorer types committed to golden.jsonl
- The cron string + budget cap value as committed
- The 5 services + loopback port binding as committed in docker-compose.langfuse.yml
- The justfile recipe names committed (so 05-05's README docs can quote them)
</output>
</output>
