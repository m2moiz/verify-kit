---
phase: 05-llm-add-on
plan: "05-03"
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - template/tests/conftest.py.jinja2
  - template/tests/cassettes/{% if has_llm %}.gitkeep{% endif %}
  - template/tests/llm/{% if has_llm %}conftest.py{% endif %}.jinja2
  - template/tests/llm/{% if has_llm %}test_skip_fixture_contract.py{% endif %}.jinja2
  - template/tests/llm/{% if has_llm %}test_llm_call.py{% endif %}.jinja2
  - template/tests/llm/{% if has_llm %}test_vcr_scrub.py{% endif %}.jinja2
  - template/tests/llm/{% if has_llm %}test_smoke.py{% endif %}.jinja2
  - template/tests/llm/{% if has_llm %}test_autoevals.py{% endif %}.jinja2
  - template/tests/llm/cassettes/{% if has_llm %}.gitkeep{% endif %}
  - template/harness/checks/{% if has_llm %}eval.py{% endif %}.jinja2
autonomous: true
requirements: [LLM-03, LLM-06, LLM-07]
must_haves:
  truths:
    - "vcr_config fixture scrubs authorization, x-api-key, anthropic-api-key, openai-api-key, openai-organization, x-langfuse-public-key headers BEFORE first cassette is written"
    - "test_llm_call.py forcing-functions the @cost_budget OUTER / @llm_call INNER rule from RESEARCH.md Pitfall 3"
    - "test_smoke.py verifies pydantic-ai Agent constructs and instructor patches OpenAI client (no live call)"
    - "Cassette-backed tests (those using @pytest.mark.vcr against record_mode=none) SKIP cleanly when their cassette file is absent — first-clean-render `uv run pytest tests/llm/` exits 0, never failing on missing cassette files (clean-scaffold first-run discipline per Phase 5 review feedback HIGH #7). Cycle-2 MEDIUM: the vcr_config fixture's `record_mode='none'` is the CI/default; the CLI flag `pytest --record-mode=once` MUST override the fixture (this is pytest-recording's documented precedence: CLI > fixture). 05-04 Task 4 (`just refresh-cassettes`) relies on this override to re-record. Forcing-functioned by an explicit comment in tests/conftest.py vcr_config block documenting the CLI precedence."
    - "test_autoevals.py uses an autouse skip-when-no-cassette guard, so it skips on first render and runs only after `just refresh-cassettes` has populated the cassette"
    - "Cycle-6 RESTRUCTURE: the _skip_when_no_cassette record-mode gate is verified by a real pytest contract test (tests/llm/test_skip_fixture_contract.py) using pytester, NOT by an inline subprocess probe in Task 2's <verify> block. Contracts live with producers (REVIEW-CHECKLIST §3). The contract test exercises both branches: (a) default record_mode='none' + missing cassette MUST skip with 'cassette not recorded' reason, (b) --record-mode=once + missing cassette MUST NOT skip — proven by presence of the unique sentinel string UNREACHED-SENTINEL-fixture-fell-through in run output."
    - "harness/checks/eval.py registers eval check via @register (NOT @register_check) and returns CheckResult(status=...) skip per D-15"
    - "All test files under tests/llm/ use Shape 2 (filename-level) gating (cycle-2 HIGH #2 fix: `template/tests/llm/{% if has_llm %}<name>.py{% endif %}.jinja2`). The `tests/llm/` directory segment is HARD-CODED — no mid-path Jinja like `tests/{% if has_llm %}llm{% endif %}/...` (segment collapse leaks files into has_llm=false renders before _exclude filters apply). tests/cassettes/ stays universal with a {% if has_llm %}.gitkeep{% endif %}; tests/llm/cassettes/ also uses filename-level .gitkeep gating."
  artifacts:
    - path: "template/tests/conftest.py.jinja2"
      provides: "Appended vcr_config fixture under {% if has_llm %} with filter_headers + before_record_request"
      contains: "filter_headers"
    - path: "template/tests/llm/{% if has_llm %}conftest.py{% endif %}.jinja2"
      provides: "reset_cost_accumulator autouse fixture per test (Shape 2 filename-level gate inside the universal tests/llm/ directory — cycle-2 HIGH #2 fix; mid-path-segment Jinja like {% if has_llm %}llm{% endif %} is FORBIDDEN because copier resolves segment-collapse before _exclude filters apply, risking file leaks into has_llm=false renders)"
    - path: "template/tests/llm/{% if has_llm %}test_skip_fixture_contract.py{% endif %}.jinja2"
      provides: "Cycle-6 RESTRUCTURE — contract test for the _skip_when_no_cassette fixture's record-mode gate. Lives WITH the producer plan per REVIEW-CHECKLIST §3 (contracts live with producers). Uses pytest's pytester plugin to spawn sub-pytest runs against a stub vcr-marked test under two conditions: (a) default record_mode='none' + missing cassette MUST skip, (b) --record-mode=once + missing cassette MUST NOT skip. Replaces the brittle inline subprocess probe in Task 2's <verify> block that oscillated across cycles 4/5/6."
      contains: "pytest_plugins"
    - path: "template/harness/checks/{% if has_llm %}eval.py{% endif %}.jinja2"
      provides: "@register('eval', ...) discoverable check that returns status=skip with hint"
      contains: "@register"
  key_links:
    - from: "tests/llm/cassettes/**/*.yaml"
      to: "no plaintext credentials on disk"
      via: "vcr_config filter_headers + before_record_request scrubbing"
      pattern: "Bearer|sk-ant-|sk-proj-"
      note: "cycle-3 HIGH #2 fix — canonical LLM cassette path is tests/llm/cassettes/<module>/<test>.yaml (recursive), not flat tests/cassettes/. The scrub test walks rglob('*.yaml') under tests/llm/cassettes/."
    - from: "tests/llm/test_llm_call.py"
      to: "harness/llm.py exports (from 05-02)"
      via: "from harness.llm import llm_call, cost_budget, CostBudgetExceeded, reset_cost_accumulator"
      pattern: "from harness.llm import"
---

<objective>
Land the test scaffold that proves harness/llm.py (from 05-02) behaves correctly AND that no cassette ever contains a live credential. Add the optional `eval` check to harness/checks/ so `verify-kit list-checks` advertises it (discoverable but skip-by-default per D-15 — `just verify` does not call it).

Purpose: Three things at once: (a) forcing-function the decorator-ordering rule with a behavior test (Pitfall 3 mitigation), (b) install the vcr filter_headers BEFORE any cassette can be recorded (Pitfall 2 mitigation), (c) make the eval check discoverable to list-checks without entangling it with the umbrella verify command (D-15).

Output: vcr_config fixture (appended to existing conftest.py), four LLM test files, one optional check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/REVIEW-CHECKLIST.md
@.planning/phases/05-llm-add-on/05-CONTEXT.md
@.planning/phases/05-llm-add-on/05-RESEARCH.md
@.planning/phases/05-llm-add-on/05-01-PLAN.md
@.planning/phases/05-llm-add-on/05-02-PLAN.md
@template/tests/conftest.py.jinja2
@template/harness/checks/__init__.py.jinja2
@template/harness/registry.py.jinja2
@template/harness/models.py.jinja2

<interfaces>
<!-- API surface from Phase 2 landed code (REVIEW-CHECKLIST §4 anti-drift). -->

From template/harness/registry.py.jinja2:
- Decorator is `@register(check_id, *, tier, category, ...)` — NOT @register_check
- Tier values used elsewhere: "fast", "slow" (per Phase 4 checks)

From template/harness/models.py.jinja2:
- `CheckResult(check_id=..., status=..., error=ErrorEnvelope(...) | None, ...)` — status is one of pass/fail/skip
- NEVER `CheckResult(ok=False)` — that field does not exist

From template/harness/checks/__init__.py.jinja2:
- This is the registration site for check modules (per REVIEW-CHECKLIST §4 — real site is `harness/checks/__init__.py.jinja2`, NOT `harness/registry.py`)
- Phase 4 added `{% if has_backend %}backend.py{% endif %}.jinja2` and `{% if has_backend %}backend_inprocess_fuzz.py{% endif %}.jinja2` here

From 05-02 harness/llm.py exports:
- `llm_call`, `cost_budget`, `CostBudgetExceeded`, `reset_cost_accumulator`, `_routing_path`

From RESEARCH.md "LLM-07 — vcrpy header scrub recipe":
- filter_headers list-of-tuple shape: `[("authorization", "REDACTED"), ("x-api-key", "REDACTED"), ...]`
- before_record_request: callable taking request, mutating headers, returning request
- record_mode: "none" default (CI); dev overrides via `--record-mode=once`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append vcr_config fixture to tests/conftest.py.jinja2 AND add cassettes/.gitkeep</name>
  <files>template/tests/conftest.py.jinja2, template/tests/cassettes/{% if has_llm %}.gitkeep{% endif %}</files>
  <read_first>
    - template/tests/conftest.py.jinja2 (existing — append, do NOT rewrite per REVIEW-CHECKLIST §4)
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "LLM-07 — vcrpy header scrub recipe" — the canonical recipe)
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "Pitfall 2: First-recorded cassette contains real Authorization header")
  </read_first>
  <action>
    Edit tests/conftest.py.jinja2 to append a `{% if has_llm %}` block. Inside, define a module-scope pytest fixture `vcr_config` returning a dict with keys: `filter_headers` (list-of-tuple including authorization, x-api-key, openai-organization, anthropic-api-key, openai-api-key, x-langfuse-public-key — each paired with "REDACTED"), `before_record_request` (a nested function `_scrub(request)` that iterates `list(request.headers)` and replaces any case-insensitive match against the same six header names with "REDACTED", returning request), `filter_query_parameters` ([("api_key", "REDACTED")]), and `record_mode` ("none" — CI default per Pitfall 2; dev overrides via --record-mode=once).

    Also create `template/tests/cassettes/{% if has_llm %}.gitkeep{% endif %}` as an empty file. This is Shape 2 (filename-level gate): the cassettes/ directory itself is universal so backend tests could share it, but the .gitkeep that creates the directory in git is only present when has_llm=true. Per REVIEW-CHECKLIST §5, the {% if %} and {% endif %} in the fixture function MUST be on their own lines so they wrap a block, not collapse adjacent lines.

    Crucial: this task MUST land BEFORE Task 3 writes any @pytest.mark.vcr test, because vcrpy default config does NOT scrub headers and a first-run recording without this fixture leaks the live Authorization header to disk (Pitfall 2).

    **Cycle-2 MEDIUM (record-mode precedence):** Include an explicit comment inside the vcr_config fixture body documenting that `record_mode="none"` is the default and that the CLI flag `pytest --record-mode=once` (used by `just refresh-cassettes` in 05-04) overrides this fixture-level value per pytest-recording's documented precedence (CLI > fixture). The comment text must contain the literal substrings `record-mode=once` and `refresh-cassettes` so future readers find the documented override path.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-03-vcr && uv run --no-project python -c "
import subprocess, pathlib
src='/Users/moiz/Documents/code/verify-kit'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-03-vcr'], check=True)
conf = pathlib.Path('/tmp/scratch-05-03-vcr/tests/conftest.py').read_text()
for h in ['authorization','x-api-key','anthropic-api-key','openai-api-key','openai-organization']:
    assert h in conf.lower(), f'{h} missing from vcr_config'
assert 'filter_headers' in conf and 'before_record_request' in conf
assert 'record_mode' in conf and 'none' in conf
# Cycle-2 MEDIUM: explicit documentation of CLI precedence
assert 'record-mode=once' in conf and 'refresh-cassettes' in conf, 'cycle-2 MEDIUM: vcr_config must document that --record-mode=once CLI flag overrides the fixture (used by just refresh-cassettes)'
assert pathlib.Path('/tmp/scratch-05-03-vcr/tests/cassettes/.gitkeep').exists() or pathlib.Path('/tmp/scratch-05-03-vcr/tests/cassettes').exists()
# Polarity
subprocess.run(['rm','-rf','/tmp/scratch-05-03-vcr-off'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=false','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-03-vcr-off'], check=True)
off = pathlib.Path('/tmp/scratch-05-03-vcr-off/tests/conftest.py').read_text()
assert 'filter_headers' not in off and 'vcr_config' not in off, 'vcr block leaked into has_llm=false'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - has_llm=true scratch render's tests/conftest.py contains vcr_config fixture with all six filter_headers entries
    - record_mode default is "none"
    - has_llm=false render does NOT contain vcr_config or filter_headers
    - tests/cassettes/.gitkeep present in has_llm=true render
    - {% if %} / {% endif %} are on their own lines (REVIEW-CHECKLIST §5)
  </acceptance_criteria>
  <done>vcr_config fixture appended; cassettes/ directory established with gated .gitkeep; polarity verified BEFORE any test file is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/llm/conftest.py with reset_cost_accumulator autouse fixture + record-mode contract test</name>
  <files>template/tests/llm/{% if has_llm %}conftest.py{% endif %}.jinja2, template/tests/llm/cassettes/{% if has_llm %}.gitkeep{% endif %}, template/tests/llm/{% if has_llm %}test_skip_fixture_contract.py{% endif %}.jinja2</files>
  <read_first>
    - .planning/phases/05-llm-add-on/05-02-PLAN.md (harness.llm exports — reset_cost_accumulator)
    - template/tests/conftest.py.jinja2 (top-level conftest — for fixture inheritance context)
  </read_first>
  <behavior>
    - Test of fixture itself: any test in tests/llm/ starts with `_cumulative_cost_usd.get() == 0.0` even after a sibling test mutated it.
  </behavior>
  <action>
    Create template/tests/llm/{% if has_llm %}conftest.py{% endif %}.jinja2 (**cycle-2 HIGH #2 fix: Shape 2 FILENAME-level gate, NOT directory-segment gate**. The `tests/llm/` directory segment is hard-coded — NO mid-path Jinja like `{% if has_llm %}llm{% endif %}`, which copier renders to a collapsed `tests//conftest.py` on has_llm=false and slips past the `_exclude: tests/llm/**` filter. The `_exclude` block in copier.yml is the primary suppressor; this filename-level gate is defense-in-depth). Define TWO autouse function-scope fixtures:

    (1) `_reset_llm_cost(autouse=True)`: calls `from harness.llm import reset_cost_accumulator; reset_cost_accumulator()` BEFORE yield. Ensures @cost_budget tests are isolated.

    (2) `_skip_when_no_cassette(autouse=True, request)`: implements the first-run-clean discipline (Phase 5 review HIGH #7). Body: if the test has a `pytest.mark.vcr` marker, FIRST check the active record_mode — read it from `request.config.getoption("--record-mode", default=None)` (pytest-recording CLI flag) OR fall back to the vcr_config fixture's `record_mode` value. **If `record_mode != "none"` (i.e., the recording path is active — `just refresh-cassettes` runs `pytest --record-mode=once`), DO NOT SKIP** — let VCR proceed and record the cassette (cycle-3 HIGH #1 fix: the prior shape skipped before VCR could ever record, making `just refresh-cassettes` self-defeating). Only when `record_mode == "none"` (CI/default), locate the expected cassette path using vcrpy's default naming convention (`Path(__file__).parent / "cassettes" / request.node.module.__name__ / f"{request.node.name}.yaml"`); if the cassette file does NOT exist, call `pytest.skip(f"cassette not recorded: {cassette_path}. Run `just refresh-cassettes` to populate.")` BEFORE the test body runs. Net effect: clean-scaffold first-run `uv run pytest tests/llm/` exits 0 with skipped (not failed) cassette-backed tests; `just refresh-cassettes` (which sets `--record-mode=once`) bypasses the skip and lets VCR actually record; after a developer runs the refresh recipe, default `uv run pytest tests/llm/` exercises the recorded interactions.

    Also create `template/tests/llm/cassettes/{% if has_llm %}.gitkeep{% endif %}` (filename-level gate; the `cassettes/` subdirectory itself stays in source but renders empty when has_llm=false, and is suppressed by `_exclude: tests/llm/**`) so the cassettes/ directory exists in the rendered has_llm=true tree but is empty — vcrpy's record_mode="none" will fail loudly if a test attempts a live call (correct behavior), and the autouse skip fixture intercepts before vcrpy is engaged.

    No before-import side effects beyond the reset call and the skip-when-no-cassette check.

    **Cycle-6 RESTRUCTURE — contract test (rule 08 oscillation signal at 1→3 HIGHs prompted moving the gate verification OUT of the inline `<verify>` block and INTO a real pytest test owned by this producer plan, per REVIEW-CHECKLIST §3).** Create `template/tests/llm/{% if has_llm %}test_skip_fixture_contract.py{% endif %}.jinja2` containing pytest tests that exercise the `_skip_when_no_cassette` fixture under both record-mode conditions using pytest's built-in `pytester` plugin (canonical mechanism for testing pytest fixtures). The file structure:

    ```python
    # tests/llm/test_skip_fixture_contract.py
    # Contract test: proves _skip_when_no_cassette's record-mode gate is not self-defeating.
    # Uses pytester to run sub-pytest invocations in isolation, asserting on outcomes.
    import sys
    from pathlib import Path
    import pytest

    pytest_plugins = ["pytester"]

    # Cycle-7 HIGH #1 fix: pytester.runpytest_subprocess runs in an isolated tmpdir with
    # only that tmpdir on PYTHONPATH — outer 'tests.conftest' / 'tests.llm.conftest' would
    # ModuleNotFoundError without an explicit sys.path inject. Derive the scratch project
    # root from this file's path (tests/llm/test_skip_fixture_contract.py -> parents[2]).
    _PROJECT_ROOT = Path(__file__).resolve().parents[2]

    _CONFTEST_BRIDGE = f'''
    # Inherit project conftest fixtures into the pytester subtest.
    # PROJECT_ROOT must be on sys.path BEFORE the imports below or they fail.
    import sys
    sys.path.insert(0, {str(_PROJECT_ROOT)!r})
    from tests.conftest import vcr_config  # noqa: F401, E402
    from tests.llm.conftest import _reset_llm_cost, _skip_when_no_cassette  # noqa: F401, E402
    '''

    _PROBE_TEST = '''
    import pytest
    @pytest.mark.vcr
    def test_probe():
        raise AssertionError("UNREACHED-SENTINEL-fixture-fell-through")
    '''

    def test_default_mode_skips_when_cassette_missing(pytester):
        pytester.makeconftest(_CONFTEST_BRIDGE)
        pytester.makepyfile(_PROBE_TEST)
        result = pytester.runpytest_subprocess("-q")
        result.assert_outcomes(skipped=1)
        # The skip reason must reference 'cassette not recorded' — proves the
        # actual fixture branch ran, not pytest's own collection-level skip.
        assert any("cassette not recorded" in line.lower()
                   for line in result.stdout.lines + result.stderr.lines), (
            "default-mode skip must cite 'cassette not recorded'"
        )

    def test_record_mode_once_does_not_skip(pytester):
        pytester.makeconftest(_CONFTEST_BRIDGE)
        pytester.makepyfile(_PROBE_TEST)
        result = pytester.runpytest_subprocess("-q", "--record-mode=once")
        outcomes = result.parseoutcomes()
        # Critical assertion: under recording, the fixture must NOT skip — the
        # probe body MUST execute (and fail on the sentinel AssertionError).
        # If skipped >= 1, the fixture is still self-defeating.
        assert outcomes.get("skipped", 0) == 0, (
            f"--record-mode=once must NOT trigger skip; got: {outcomes}"
        )
        # Defense-in-depth: prove the probe body actually executed by finding
        # the unique sentinel string in the failure output.
        all_lines = result.stdout.lines + result.stderr.lines
        assert any("UNREACHED-SENTINEL-fixture-fell-through" in line
                   for line in all_lines), (
            "probe body must execute under --record-mode=once (sentinel missing)"
        )
    ```

    **Why pytester rather than `subprocess.run`:** pytester is pytest's documented harness for testing fixtures. Each test gets an isolated tmpdir + sub-pytest invocation; outcomes come from `result.parseoutcomes()` / `result.assert_outcomes()` rather than parsing stdout substrings. The test runs in the project's own pytest environment, inheriting dev deps (pytest-recording) — no `uv run --no-project` env-leak, no missing-deps risk. **Use the sentinel string `UNREACHED-SENTINEL-fixture-fell-through` exactly** — uniqueness is what distinguishes "fixture fell through and body ran" from "fixture errored before body" or "collection failure". Verify case (b)'s assertion ONLY trusts presence of this sentinel + zero skips; do NOT accept "exit code non-zero" alone (the cycle-6 false-positive shape).

    The `_CONFTEST_BRIDGE` imports the project's actual `vcr_config` and `_skip_when_no_cassette` fixtures into pytester's spawned test's conftest, so the contract test exercises the REAL fixture, not a copy.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-03-llm-conf && uv run --no-project python -c "
import subprocess, pathlib, os
src='/Users/moiz/Documents/code/verify-kit'
scratch='/tmp/scratch-05-03-llm-conf'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--vcs-ref','HEAD',src,scratch], check=True)

# 1) Fixture file exists and has the expected shape (fast-fail).
conf = pathlib.Path(scratch, 'tests/llm/conftest.py')
assert conf.exists()
text = conf.read_text()
assert 'reset_cost_accumulator' in text and 'autouse=True' in text
assert '_skip_when_no_cassette' in text, 'autouse skip-when-no-cassette guard missing'
assert 'pytest.skip' in text, 'fixture must call pytest.skip when cassette absent'
assert 'record_mode' in text, 'skip fixture must reference record_mode'

# 2) Contract test file exists.
contract = pathlib.Path(scratch, 'tests/llm/test_skip_fixture_contract.py')
assert contract.exists(), 'cycle-6 RESTRUCTURE: contract test file missing — see Task 2 action block'
assert 'pytester' in contract.read_text(), 'contract test must use pytester plugin'

# 3) Install scratch deps (dev extras, which include pytest-recording) into the scratch project.
subprocess.run(['uv','sync','--extra','dev'], cwd=scratch, check=True)

# 4) Run the contract test inside the scratch project with a CLEAN environment (REVIEW-CHECKLIST §8: no VIRTUAL_ENV/UV_PROJECT_ENVIRONMENT leak).
clean_env = {k: v for k, v in os.environ.items() if k not in ('VIRTUAL_ENV','UV_PROJECT_ENVIRONMENT','PYTHONPATH','PYTHONHOME','PYTEST_ADDOPTS','PYTEST_DISABLE_PLUGIN_AUTOLOAD','COVERAGE_PROCESS_START','UV_CACHE_DIR')}
clean_env['PATH'] = os.environ.get('PATH','')
result = subprocess.run(
    ['uv','run','pytest','-q','-x','tests/llm/test_skip_fixture_contract.py'],
    cwd=scratch, env=clean_env, capture_output=True, text=True,
)
assert result.returncode == 0, f'contract test FAILED — record-mode gate is broken. exit={result.returncode}\\nSTDOUT:\\n{result.stdout}\\nSTDERR:\\n{result.stderr}'

# 5) cassettes/.gitkeep must exist.
cas_dir = pathlib.Path(scratch, 'tests/llm/cassettes')
assert cas_dir.exists() and (cas_dir/'.gitkeep').exists(), 'tests/llm/cassettes/.gitkeep missing'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - tests/llm/conftest.py exists in has_llm=true render
    - File contains autouse fixture calling reset_cost_accumulator
    - File contains autouse skip-when-no-cassette fixture that calls `pytest.skip(...)` when a vcr-marked test's cassette file is absent
    - **Cycle-6 RESTRUCTURE (record-mode gate contract test, replacing prior inline-subprocess probes):** tests/llm/test_skip_fixture_contract.py exists in has_llm=true render AND uses `pytester` (declared via `pytest_plugins = ["pytester"]`). The file contains exactly two tests:
      (a) `test_default_mode_skips_when_cassette_missing` — uses `pytester.runpytest_subprocess("-q")` against a stub vcr-marked test with no cassette; asserts `result.assert_outcomes(skipped=1)` AND that the skip reason cites "cassette not recorded".
      (b) `test_record_mode_once_does_not_skip` — uses `pytester.runpytest_subprocess("-q", "--record-mode=once")` against the same stub; asserts `outcomes.get("skipped", 0) == 0` AND that the unique sentinel string `UNREACHED-SENTINEL-fixture-fell-through` appears in the run output (proves the probe body actually executed under recording mode).
    - **Contract test passes** when executed inside a fresh scratch render via `uv run pytest -q -x tests/llm/test_skip_fixture_contract.py` with `uv sync --extra dev` having installed pytest-recording, and the subprocess invocation strips `VIRTUAL_ENV`/`UV_PROJECT_ENVIRONMENT`/`PYTHONPATH`/`PYTHONHOME` from the env (REVIEW-CHECKLIST §8: clean env discipline).
    - tests/llm/cassettes/.gitkeep exists in has_llm=true render
    - On a clean scratch render, `uv run pytest tests/llm/ -q --co` (collect-only) succeeds AND running the suite produces SKIPPED (not FAILED) for cassette-backed tests
    - has_llm=false render does NOT contain any file under tests/llm/ (the directory itself may be absent OR present-but-empty; the `_exclude: tests/llm/**` entry from 05-01 plus filename-level Jinja on every leaf jointly guarantee zero file leaks)
    - **Cycle-2 HIGH #2:** No PLAN frontmatter `files_modified:` entry or `<files>` tag uses mid-path Jinja of the form `tests/{% if has_llm %}llm{% endif %}/...`. The only acceptable shapes for tests/llm/ are: filename-level Jinja inside the hard-coded `tests/llm/` directory (`template/tests/llm/{% if has_llm %}<name>.py{% endif %}.jinja2`) — narrative text explaining the prohibition is fine; structural file-path entries violating the rule are not.
  </acceptance_criteria>
  <done>tests/llm/conftest.py landed with both autouse fixtures; tests/llm/test_skip_fixture_contract.py landed with the two pytester-based gate tests; cassette directory seeded with .gitkeep; clean-scaffold first-run pytest exits 0; contract test passes in scratch render.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create LLM test suite (test_llm_call.py, test_vcr_scrub.py, test_smoke.py, test_autoevals.py)</name>
  <files>template/tests/llm/{% if has_llm %}test_llm_call.py{% endif %}.jinja2, template/tests/llm/{% if has_llm %}test_vcr_scrub.py{% endif %}.jinja2, template/tests/llm/{% if has_llm %}test_smoke.py{% endif %}.jinja2, template/tests/llm/{% if has_llm %}test_autoevals.py{% endif %}.jinja2</files>
  <read_first>
    - .planning/phases/05-llm-add-on/05-02-PLAN.md (harness.llm exports, span attribute names)
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (sections "Phase Requirements → Test Map", "Pitfall 3", "Pitfall 4", "LLM-09 — @llm_call decorator usage")
    - template/tests/conftest.py.jinja2 (after Task 1 — vcr_config available)
  </read_first>
  <behavior>
    test_llm_call.py (NO @pytest.mark.vcr — uses in-memory span exporter + monkeypatched stub adapter; runs on first clean render):
    - test_decorator_emits_span: @llm_call(name="x") wraps an async function returning `{"cost_usd": 0.001}`; awaiting it produces a span named "llm.x" with gen_ai.operation.name attribute equal to "x" (use opentelemetry SDK InMemorySpanExporter via observability harness API).
    - test_span_carries_gen_ai_attrs (cycle-2 HIGH #4 — full attribute contract): span carries the FULL set required by phase success criterion #2 — `gen_ai.operation.name` (str), `gen_ai.request.model` (str), `gen_ai.response.model` (str), `gen_ai.usage.input_tokens` (int), `gen_ai.usage.output_tokens` (int), `verify_kit.cost_usd` (float), `verify_kit.latency_ms` (int), `verify_kit.routing_path` (str), `verify_kit.retry_count` (int). Assert every key is present on the in-memory exported span; missing any key fails the test. Forcing-functions HIGH #4.
    - test_cost_budget_raises: with @cost_budget(usd=0.0, on_exceed="raise") OUTER + @llm_call INNER, a call that records any cost > 0 raises CostBudgetExceeded; assertion checks budget_usd, accumulated_usd, call_name attributes.
    - test_cost_budget_does_not_raise_when_under_budget: usd=999.0; no exception even with non-zero accumulated cost.
    - test_decorator_ordering_documentation: marker test (skip with message) referencing Pitfall 3 — forcing function: opens harness/llm.py source and asserts the docstring contains both substrings "OUTER" and "INNER". This catches docstring drift on future refactors.
    - test_call_llm_dispatches_via_routing_path (LLM-03/D-03 routing forcing function): monkeypatch `harness.llm.call_via_claude_agent_sdk` and `harness.llm.call_via_litellm` to return distinguishable sentinel dicts; with USE_CLAUDE_CODE_SDK=1 in env, `await call_llm("hi")` returns the claude-agent-sdk sentinel AND the emitted span has `verify_kit.routing_path="claude-agent-sdk"`; with USE_CLAUDE_CODE_SDK unset and ANTHROPIC_API_KEY set, returns the litellm sentinel AND span has `verify_kit.routing_path="litellm"`. This is the forcing function for HIGH #2 — without `call_llm` actually routing, this test fails.

    test_vcr_scrub.py:
    - test_no_plaintext_credentials_in_cassettes (cycle-3 HIGH #2 + cycle-4 HIGH #2 fix — canonicalize path AND anchor it on __file__ to avoid cwd leaks per REVIEW-CHECKLIST §1): walks the cassette tree using `(Path(__file__).parent / "cassettes").rglob("*.yaml")` — `__file__` is `tests/llm/test_vcr_scrub.py`, so `Path(__file__).parent / "cassettes"` resolves to `tests/llm/cassettes/`. **Do NOT use bare `pathlib.Path("tests/llm/cassettes").rglob(...)`** — that resolves against pytest's cwd and silently scans the wrong tree when pytest is invoked from a parent directory (REVIEW-CHECKLIST §1 cwd-leak). vcrpy stores cassettes under `cassettes/<test_module>/<test_name>.yaml` (recursive — hence `rglob` not `glob`). Asserts NO file contains the substrings "Bearer ", "sk-ant-", "sk-proj-", "sk-lf-" (with the trailing characters that would indicate an unredacted token). If the directory is empty (clean scaffold), the test passes vacuously — which is correct first-run behavior.
    - test_vcr_config_lists_required_headers: imports the vcr_config fixture's return dict, asserts authorization/x-api-key/anthropic-api-key/openai-api-key/openai-organization/x-langfuse-public-key all appear in filter_headers.

    test_smoke.py (no live API calls):
    - test_pydantic_ai_agent_constructs: `Agent("anthropic:claude-haiku-4-5", output_type=str)` constructs without exception (does not call .run).
    - test_pydantic_ai_uses_output_not_data: imports `pydantic_ai`; uses `inspect.signature` or `hasattr` on the AgentRunResult-like return type — asserts the attribute name is "output" not "data" (RESEARCH.md Pitfall 4). If type is hard to introspect statically, run a minimal `agent.run` against a mocked transport that returns a recorded response, and assert `result.output` works.
    - test_instructor_patch (cycle-2 MEDIUM fix — avoid unpinned `anthropic` dependency): use `import instructor; from openai import OpenAI; client = instructor.from_openai(OpenAI(api_key="dummy"))` — this exercises the instructor patch path against `openai` (which IS transitively pinned via litellm's resolved deps) rather than against the unpinned `anthropic` package. The test asserts `client` has the patched `.chat.completions.create` attribute (no live call). If openai is somehow unavailable, the test should skip with a clear reason (`pytest.importorskip("openai")` at top) so the suite still exits cleanly. Do NOT use `instructor.from_anthropic(anthropic.Anthropic())` — that would require pinning the standalone `anthropic` package in 05-01, expanding scope.
    - test_litellm_imports_and_completion_cost_callable: `from litellm import completion_cost; assert callable(completion_cost)`.

    test_autoevals.py:
    - test_autoevals_factuality_score: instantiate `autoevals.Factuality()` scorer; call with a recorded cassette using `@pytest.mark.vcr`; assert `result.score` is a float in [0.0, 1.0]. Cassette must already be scrubbed (filter_headers from Task 1).
    - **First-run-clean discipline (Phase 5 review HIGH #7):** clean-scaffold renders ship with EMPTY cassettes/ (only `.gitkeep`). Cassette-backed tests MUST skip cleanly when their cassette file is absent — `uv run pytest tests/llm/` on a clean scratch render MUST exit 0, NOT fail loudly. To enforce this:
      1. tests/llm/conftest.py (Task 2) defines an autouse fixture `_skip_when_no_cassette` that detects a `@pytest.mark.vcr` marker on the test and checks whether the corresponding cassette file exists under `tests/llm/cassettes/<test_module>/<test_name>.yaml`; if not, calls `pytest.skip("cassette not recorded — run `just refresh-cassettes`")` with a clear reason.
      2. test_autoevals_factuality_score relies on this autouse skip — when developers run `just refresh-cassettes` to populate the cassette (Task 4 of 05-04 flips record_mode to "once"), the skip dissolves and the test exercises the real cached interaction.
      3. test_smoke.py tests are NOT @pytest.mark.vcr (no live LLM call needed); they always run.
      4. test_llm_call.py decorator/budget/span tests are NOT @pytest.mark.vcr (they use in-memory OTel span exporter + a stub adapter); they always run.
  </behavior>
  <action>
    Create four test files under **Shape 2 FILENAME-level gate** (`template/tests/llm/{% if has_llm %}<name>.py{% endif %}.jinja2` — cycle-2 HIGH #2 fix). The `tests/llm/` directory segment is hard-coded — do NOT introduce mid-path Jinja like `tests/{% if has_llm %}llm{% endif %}/...` which collapses to `tests//<name>.py` on has_llm=false and bypasses the `_exclude: tests/llm/**` filter. All tests use `import pytest`; tests that exercise live LLM behavior are marked `@pytest.mark.vcr`. Test bodies must use exact symbol names from 05-02's harness/llm.py exports: `llm_call`, `cost_budget`, `CostBudgetExceeded`, `reset_cost_accumulator`. Per REVIEW-CHECKLIST §4, test assertions on span attribute names MUST quote the names from 05-02's __all__ — `gen_ai.operation.name`, `verify_kit.cost_usd`, `verify_kit.latency_ms`, `verify_kit.routing_path`. No `result.data` references anywhere (Pitfall 4). Tests must not call live APIs — every external call goes through vcrpy.

    Per REVIEW-CHECKLIST §1, no bare relative paths — any file IO uses pytest's `tmp_path` or the cassette path resolved relative to the test file via `Path(__file__).parent`. Per §2, no statements after return. Per §7, these test files live under `tests/llm/` (filename-level shape from 04-01), which is NOT a path the universal harness recursively invokes — so no recursion risk.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-03-tests && uv run --no-project python -c "
import subprocess, pathlib
src='/Users/moiz/Documents/code/verify-kit'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-03-tests'], check=True)
base = pathlib.Path('/tmp/scratch-05-03-tests/tests/llm')
for f in ['test_llm_call.py','test_vcr_scrub.py','test_smoke.py','test_autoevals.py']:
    assert (base/f).exists(), f'{f} missing'
llm_call_text = (base/'test_llm_call.py').read_text()
assert 'CostBudgetExceeded' in llm_call_text
assert 'gen_ai.operation.name' in llm_call_text
# Cycle-2 HIGH #4: full gen_ai.* + verify_kit.retry_count set asserted in tests
for attr in ['gen_ai.request.model','gen_ai.response.model','gen_ai.usage.input_tokens','gen_ai.usage.output_tokens','verify_kit.retry_count']:
    assert attr in llm_call_text, f'cycle-2 HIGH #4: required span attribute not asserted in tests: {attr}'
assert 'verify_kit.cost_usd' in llm_call_text
assert 'result.data' not in llm_call_text, 'Pitfall 4: pydantic-ai uses result.output not result.data'
# LLM-03/D-03 routing forcing function — test_call_llm_dispatches_via_routing_path must exist
assert 'call_llm' in llm_call_text and 'call_via_claude_agent_sdk' in llm_call_text and 'call_via_litellm' in llm_call_text, 'routing test missing — must monkeypatch both adapters and assert call_llm dispatches via _routing_path()'
assert 'routing_path' in llm_call_text, 'verify_kit.routing_path span attribute must be asserted'
# test_llm_call.py must NOT be cassette-backed (first-run-clean discipline — must run on empty cassettes/)
assert '@pytest.mark.vcr' not in llm_call_text and 'pytest.mark.vcr' not in llm_call_text, 'test_llm_call.py must run on first clean render — do NOT use @pytest.mark.vcr; use in-memory span exporter + monkeypatched adapter stubs'
smoke = (base/'test_smoke.py').read_text()
assert 'result.data' not in smoke
vcr = (base/'test_vcr_scrub.py').read_text()
assert 'sk-ant-' in vcr and 'sk-proj-' in vcr, 'forbidden-token list missing'
# Polarity
subprocess.run(['rm','-rf','/tmp/scratch-05-03-tests-off'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=false','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-03-tests-off'], check=True)
assert not pathlib.Path('/tmp/scratch-05-03-tests-off/tests/llm').exists(), 'tests/llm leaked'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Four test files exist under tests/llm/ in has_llm=true render
    - test_llm_call.py imports CostBudgetExceeded and asserts on gen_ai.operation.name + verify_kit.cost_usd + verify_kit.latency_ms
    - No file contains the literal `result.data` (Pitfall 4)
    - test_vcr_scrub.py contains the forbidden-token substrings "sk-ant-" and "sk-proj-" as assertion strings
    - has_llm=false render has NO tests/llm/ directory
    - Forcing-function test for decorator-ordering docstring is present in test_llm_call.py
  </acceptance_criteria>
  <done>Four LLM test files landed; assertions quote exact 05-02 symbol/attribute names; pydantic-ai .output discipline enforced; cassettes still empty (record_mode=none).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Register optional eval check (discoverable, skip-by-default per D-15)</name>
  <files>template/harness/checks/{% if has_llm %}eval.py{% endif %}.jinja2, template/harness/checks/__init__.py.jinja2</files>
  <read_first>
    - template/harness/checks/__init__.py.jinja2 (existing registration site — REVIEW-CHECKLIST §4 confirmed this is the canonical site, NOT harness/registry.py)
    - template/harness/checks/{% if has_backend %}backend.py{% endif %}.jinja2 (Phase 4 reference — same @register shape)
    - template/harness/registry.py.jinja2 (for register signature)
    - template/harness/models.py.jinja2 (for CheckResult shape — status=skip path)
    - .planning/phases/05-llm-add-on/05-CONTEXT.md (D-15 — eval stays opt-in; do NOT call from `just verify`)
  </read_first>
  <action>
    Create template/harness/checks/{% if has_llm %}eval.py{% endif %}.jinja2 (Shape 2 gate — parent dir harness/checks/ is universal). Use the EXACT decorator name `@register` (NOT @register_check — REVIEW-CHECKLIST §4) imported from harness.registry. Register a check with id="eval", tier="slow", category="llm". The check body returns `CheckResult(check_id="eval", status="skip", ...)` (status field — REVIEW-CHECKLIST §4 forbids ok= field) with a hint pointing the consumer to `just eval`. This satisfies D-15: the check is DISCOVERABLE via `verify-kit list-checks` but never runs by default — `just verify` would see status="skip" and exit 0.

    Then edit harness/checks/__init__.py.jinja2 to add an import of the eval module under `{% if has_llm %}` so the @register decorator fires at import time. Match the existing import pattern used by Phase 4 for backend.py (line-level mirror — open the existing file to see exact shape).

    Per REVIEW-CHECKLIST §4, do NOT invent a new register API; use the existing one. Verify by running `verify-kit list-checks` in scratch and confirming "eval" appears.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-03-eval && uv run --no-project python -c "
import subprocess, pathlib
src='/Users/moiz/Documents/code/verify-kit'
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-03-eval'], check=True)
p = pathlib.Path('/tmp/scratch-05-03-eval/harness/checks/eval.py')
assert p.exists(), 'eval.py missing'
text = p.read_text()
assert '@register' in text and '@register_check' not in text, 'Must use @register, not @register_check'
assert 'status=' in text, 'CheckResult must use status= (not ok=)'
assert 'skip' in text, 'D-15: eval check status=skip default'
init_text = pathlib.Path('/tmp/scratch-05-03-eval/harness/checks/__init__.py').read_text()
assert 'eval' in init_text, 'eval module not imported in checks __init__'
# Polarity
subprocess.run(['rm','-rf','/tmp/scratch-05-03-eval-off'], check=True)
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=false','--data','has_backend=true','--vcs-ref','HEAD',src,'/tmp/scratch-05-03-eval-off'], check=True)
assert not pathlib.Path('/tmp/scratch-05-03-eval-off/harness/checks/eval.py').exists()
init_off = pathlib.Path('/tmp/scratch-05-03-eval-off/harness/checks/__init__.py').read_text()
# Authoritative structural absence: when has_llm=false, the eval module must not be imported at all.
# Assert no "from harness.checks.eval" import line and no _register/register call referencing eval module.
import re
assert not re.search(r'from\s+harness\.checks\.eval', init_off), 'eval module import leaked into has_llm=false render'
assert not re.search(r'(?:^|\W)import\s+harness\.checks\.eval', init_off), 'eval module import leaked into has_llm=false render'
# Defense-in-depth: also confirm the eval.py file itself is absent in the polarity render
assert not pathlib.Path('/tmp/scratch-05-03-eval-off/harness/checks/eval.py').exists(), 'eval.py file leaked'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - harness/checks/eval.py exists in has_llm=true render
    - Uses `@register` decorator (NOT @register_check — REVIEW-CHECKLIST §4)
    - CheckResult uses status= field (NOT ok= — REVIEW-CHECKLIST §4)
    - status defaults to "skip" with hint pointing to `just eval` (D-15)
    - harness/checks/__init__.py imports the eval module under {% if has_llm %}
    - has_llm=false render does NOT contain eval.py
  </acceptance_criteria>
  <done>eval check is discoverable via list-checks but skip-by-default; @register/CheckResult drift avoided.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/llm/cassettes/**/*.yaml → git repo | LLM cassettes ARE source code (vcrpy stores them at tests/llm/cassettes/<module>/<test>.yaml); if a real Authorization header lands in them, it ships to anyone cloning the repo |
| tests/llm/* → harness/llm.py | Trusted (in-process); tests forcing-function the documented decorator-ordering rule |
| pytest run with ANTHROPIC_API_KEY set in dev env → vcrpy | First-recording mode could capture live header IF filter_headers is not loaded — Task 1 lands BEFORE Task 3 to prevent this ordering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Information Disclosure | Cassettes leaking live Authorization | mitigate | Task 1 lands filter_headers BEFORE any @pytest.mark.vcr test exists (Pitfall 2 ordering); test_vcr_scrub.py asserts no forbidden tokens in committed cassettes |
| T-05-09 | Tampering | Decorator-ordering drift on future refactor | mitigate | test_decorator_ordering_documentation in test_llm_call.py asserts OUTER/INNER docstring substrings; T03 verify checks no `result.data` regression |
| T-05-10 | Repudiation | List-checks does not advertise eval gate | mitigate | T04 registers eval check; user discoverability proven by `verify-kit list-checks` containing "eval" |
</threat_model>

<verification>
1. tests/conftest.py has vcr_config with all six filter_headers entries (under {% if has_llm %})
2. tests/llm/{conftest,test_llm_call,test_vcr_scrub,test_smoke,test_autoevals}.py all present in has_llm=true render
3. Zero LLM tests exist in has_llm=false render
4. harness/checks/eval.py uses @register + CheckResult(status=...) — no @register_check, no ok= drift
5. test_llm_call.py asserts on the exact span-attribute spellings from 05-02 SUMMARY
6. No file contains `result.data` (Pitfall 4 discipline)
</verification>

<success_criteria>
- vcr_config fixture lands BEFORE any cassette-recording test file
- All four test files quote 05-02's exact exports and span attribute names
- eval check is registered via @register (not @register_check), returns CheckResult(status=skip)
- Polarity: zero LLM artifacts when has_llm=false
- forcing-function test for decorator-ordering documentation will fail loudly if Pitfall 3 wording is removed from harness/llm.py
</success_criteria>

<output>
Create `.planning/phases/05-llm-add-on/05-03-SUMMARY.md` when done. Include:
- The vcr_config filter_headers list as committed (so 05-04's refresh-cassettes recipe can document it)
- The test file paths so 05-05 polarity test can grep for them
- The eval check id + tier + category as registered (so README LLM-12 can document `verify-kit list-checks`)
</output>
