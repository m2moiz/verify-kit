---
phase: 05-llm-add-on
plan: "05-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - copier.yml
  - template/pyproject.toml.jinja2
  - template/{% if has_backend %}app{% endif %}/.env.example.jinja2
  - template/{% if has_llm and not has_backend %}.env.example{% endif %}.jinja2
autonomous: true  # was false (checkpoint:human-verify on Task 1); slopcheck resolved 2026-05-21, D-22 dropped tokenx-core, Task 1 now record-only
requirements: [LLM-01, LLM-10]
must_haves:
  truths:
    - "copier.yml _exclude block gates every LLM-only artifact when has_llm=false"
    - "has_llm=false renders produce zero LLM dependencies in pyproject.toml"
    - "has_llm=true installs the exact 11-package LLM dep block — tokenx-core DROPPED per D-22 (slopcheck SUS + redundancy with our own @llm_call). The remaining 11 are: pydantic-ai, instructor, litellm, tokencost, autoevals, opentelemetry-instrumentation-httpx, traceloop-sdk, claude-agent-sdk, langfuse (runtime) + vcrpy, pytest-recording (dev)."
    - "LLM dev deps land under [project.optional-dependencies].dev (NOT [dependency-groups].dev — the latter table does NOT exist in this repo)"
    - "(has_llm=true AND has_backend=true): app/.env.example.jinja2 contains the LLM credentials block under {% if has_llm %}"
    - "(has_llm=true AND has_backend=false): root-level .env.example.jinja2 contains the LLM credentials block (no other env destination exists in this cell)"
    - "(has_llm=false): NEITHER env destination contains LLM credentials"
  artifacts:
    - path: "copier.yml"
      provides: "_exclude entries for every Phase 5 path under {% if not has_llm %} (paths are matched against POST-RENDER tree shape — so the filename-gated harness/llm.py is excluded via the leaf filename 'harness/llm.py', not via a directory segment)"
      contains: "harness/llm.py"
    - path: "template/pyproject.toml.jinja2"
      provides: "has_llm-gated dependency block (11 packages — tokenx-core dropped per D-22) with pydantic-ai>=1.100"
      contains: "pydantic-ai"
      must_not_contain: "tokenx"
    - path: "template/{% if has_backend %}app{% endif %}/.env.example.jinja2"
      provides: "Appended LLM env-var block under {% if has_llm %} with three llm_backend branches (covers the has_backend=true cell)"
      contains: "ANTHROPIC_API_KEY"
    - path: "template/{% if has_llm and not has_backend %}.env.example{% endif %}.jinja2"
      provides: "Root-level .env.example for the (has_llm=true AND has_backend=false) cell — same LLM block, no backend-side env"
      contains: "ANTHROPIC_API_KEY"
  key_links:
    - from: "copier.yml _exclude block"
      to: "every LLM-only path in template/"
      via: "{% if not has_llm %}<path>{% endif %} entries"
      pattern: "if not has_llm"
    - from: "template/pyproject.toml.jinja2"
      to: "PyPI"
      via: "uv sync install resolution"
      pattern: "pydantic-ai"
---

<objective>
Establish the Phase 5 path-gating contract (extending Phase 4 04-01's two-guard rule) and land the LLM dependency block + .env.example credential slots. This plan creates the foundation that every other Phase 5 plan depends on: without the _exclude entries here, downstream LLM-only files leak into has_llm=false renders.

Purpose: Lock in the gate before any LLM-only file is written, so polarity is enforced from the first commit. Encode pydantic-ai>=1.100 (NOT ~1.0 with .data) as version pin to prevent that pitfall from RESEARCH.md. **D-22: tokenx-core dropped during 05-01 execution** after slopcheck flagged it SUS (81 PyPI downloads) and we recognized that its @measure_cost+@measure_latency decorators duplicate our own @llm_call (built in 05-02). The polarity test now asserts `tokenx` is NOT in pyproject (regression guard against re-adding a redundant niche dep).

Output: copier.yml _exclude entries for every LLM path, has_llm-gated pyproject deps block, and .env.example LLM credential section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/REVIEW-CHECKLIST.md
@.planning/phases/05-llm-add-on/05-CONTEXT.md
@.planning/phases/05-llm-add-on/05-RESEARCH.md
@.planning/phases/04-backend-fastapi-add-on/04-01-SUMMARY.md
@copier.yml
@template/pyproject.toml.jinja2

<interfaces>
<!-- Authoritative API surface from previously-landed code. Executor must match these spellings. -->

From copier.yml (lines 144-175, already present):
- has_llm: bool (default true)
- llm_backend: str, choices [langfuse-cloud, langfuse-self-host, none], default langfuse-cloud, when has_llm
- _exclude block exists at lines ~28-40 with has_backend / has_db entries (Phase 4 contract)

From template/pyproject.toml.jinja2 (VERIFIED at lines 5-78): existing Phase 4 block uses `{% if has_backend %} ... {% endif %}` inside `[project] dependencies = [ ... ]` (line ~5) AND inside `[project.optional-dependencies] dev = [ ... ]` (line 59). The repo does NOT contain a `[dependency-groups]` table — appending to `[dependency-groups].dev` would silently create a SECOND, COMPETING dev-deps convention. Plan MUST append under the existing `[project.optional-dependencies].dev` list.

From RESEARCH.md "Standard Stack" table — exact version pins to use:
- pydantic-ai>=1.100,<2
- instructor>=1.15,<2
- litellm>=1.85,<2
- tokencost>=0.1.26
- autoevals>=0.2,<0.3
- opentelemetry-instrumentation-httpx>=0.63b1
- traceloop-sdk>=0.60,<1
- claude-agent-sdk>=0.2.83
- langfuse>=4.6,<5
- vcrpy>=8.1,<9 (dev — slopcheck false-positive on name-similarity-to-scipy; allowlisted in .slopcheck)
- pytest-recording>=0.13.4 (dev)

NOTE: `tokenx-core` was REMOVED from the dep list per D-22 (slopcheck SUS during 05-01 execution + duplication with our @llm_call). The polarity test in 05-05 must assert `tokenx` is NOT in the rendered pyproject.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: slopcheck scan + record resolution (RESOLVED 2026-05-21)</name>
  <files>(none — record-only)</files>
  <read_first>
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "Package Legitimacy Audit")
    - .planning/phases/05-llm-add-on/05-CONTEXT.md (D-22)
    - .slopcheck (allowlist)
  </read_first>
  <action>
    No file modifications. This task documents the pre-execution resolution of the slopcheck gate.

    On 2026-05-21, the operator ran `slopcheck scan` against the draft 12-package dep list. Result: 10 OK, 2 SUS.

    Disposition:
    - `vcrpy` flagged "Suspiciously close to 'scipy'" — false positive on name-similarity heuristic. vcrpy is the canonical Python HTTP-cassette lib (used in Phase 4). ALLOWLISTED via `slopcheck allow vcrpy` → recorded in `.slopcheck`.
    - `tokenx-core` flagged "Only 81 downloads. Nobody uses this." — real package (dvlshah/tokenx, MIT, 20 stars, active 2025-05→2025-08) but its @measure_cost + @measure_latency decorators duplicate verify-kit's own @llm_call decorator (which 05-02 builds). Net: 0 unique value-add over tokencost+@llm_call. DROPPED per D-22.

    The other 10 packages passed slopcheck OK. They will be installed by Task 3.

    Task 2 (copier.yml _exclude block) and Task 3 (pyproject deps) MUST use the 11-package list — NO `tokenx-core` line, NO `tokenx` line.
  </action>
  <verify>
    <automated>
test -f /Users/moiz/Documents/code/verify-kit/.slopcheck
grep -q '^vcrpy' /Users/moiz/Documents/code/verify-kit/.slopcheck || (echo 'vcrpy missing from .slopcheck allowlist' && exit 1)
echo 'OK'
    </automated>
  </verify>
  <acceptance_criteria>
    - `.slopcheck` exists at project root with `vcrpy` allowlisted
    - tokenx-core does NOT appear in any task's dep list further down this plan
    - D-22 documented in 05-CONTEXT.md
  </acceptance_criteria>
  <done>Slopcheck resolution recorded; tokenx-core dropped; vcrpy allowlisted; ready for Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend copier.yml _exclude block with all has_llm-gated paths</name>
  <files>copier.yml</files>
  <read_first>
    - copier.yml (lines 20-50 — existing _exclude block from Phase 4 04-01)
    - .planning/phases/04-backend-fastapi-add-on/04-01-SUMMARY.md (canonical two-guard contract; PRIMARY gate is _exclude)
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "Pattern 1: Two-Guard Path Gating" — list of LLM-only paths)
  </read_first>
  <action>
    Edit copier.yml's existing _exclude block (do NOT create a new block; append to the existing one immediately after the Phase 4 has_db entries). Insert eleven new entries:

    Eleven entries shaped `"{% if not has_llm %}<path>{% endif %}"` for: harness/llm.py, harness/checks/eval.py, eval, eval/**, tests/llm, tests/llm/**, docker-compose.langfuse.yml, .github/workflows/nightly-eval.yml, .env.example (the root-level LLM env file added by Task 5 — primary-gate coverage per cycle-2 HIGH #3), plus harness/llm.py and the corresponding gitkeep for tests/cassettes — the cassettes/ directory itself stays universal.

    **Cycle-2 HIGH #3 (root .env.example primary-gate coverage):** The root-level `.env.example` artifact landed by Task 5 (`template/{% if has_llm and not has_backend %}.env.example{% endif %}.jinja2`) has a Shape 2 filename-level path-gate, but per the 04-01 two-guard contract the `_exclude` block is the PRIMARY gate and filename-level Jinja is DEFENSE-IN-DEPTH only. Add `"{% if not has_llm %}.env.example{% endif %}"` to the `_exclude` block so `has_llm=false` renders cannot leak the root `.env.example`. The `_exclude` entry composes safely with the filename gate (any single match suppresses the file) and is value-conditional on `has_llm` only — the `has_backend` polarity is enforced by the filename gate alone (when has_llm=true AND has_backend=true, the path-gate's `not has_backend` clause already suppresses the root .env.example; the `_exclude` does not need a `has_backend` clause).

    Plus ONE additional entry shaped `"{% if llm_backend != 'langfuse-self-host' %}docker-compose.langfuse.yml{% endif %}"` — this layered exclude fires when has_llm=true BUT llm_backend is cloud or none, suppressing the self-host compose file. Combined with the has_llm-gated entry above, docker-compose.langfuse.yml renders ONLY in the (has_llm=true AND llm_backend=langfuse-self-host) cell. This is the canonical pattern for value-conditional path-gating in this repo (D-12 cell-pattern precedent).

    Per REVIEW-CHECKLIST §3 cross-plan drift discipline, list these in the SAME ORDER and INDENTATION as the Phase 4 has_backend/has_db entries to keep diffs readable. Do NOT introduce a composite-condition gate (`{% if not has_llm or llm_backend != ... %}`); use TWO SEPARATE _exclude entries — copier evaluates them as a list, so any single match suppresses the file. Shape 1 (top-level unique-dir) and Shape 2 (filename-level) from 04-01 remain the only permitted JINJA PATH shapes (per D-20 contract); _exclude entries are not "path shapes," they are copier-engine-level filters. The _exclude block is the PRIMARY gate; jinja path conditionals are defense-in-depth only.
  </action>
  <verify>
    <automated>grep -c "if not has_llm" copier.yml | grep -v '^#' && python -c "import yaml; yaml.safe_load(open('copier.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - copier.yml parses as valid YAML (python -c "import yaml; yaml.safe_load(open('copier.yml'))" exits 0)
    - copier.yml contains at least 11 occurrences of the substring "if not has_llm" (excluding comments)
    - copier.yml contains exactly ONE entry shaped `{% if llm_backend != 'langfuse-self-host' %}docker-compose.langfuse.yml{% endif %}` (the value-conditional gate)
    - copier.yml contains entries `harness/llm.py`, `eval`, `eval/**`, `tests/llm`, `tests/llm/**`, `docker-compose.langfuse.yml`, `.github/workflows/nightly-eval.yml`, `.env.example` each under a `{% if not has_llm %} ... {% endif %}` wrap
    - **Cycle-2 HIGH #3:** copier.yml contains the entry `"{% if not has_llm %}.env.example{% endif %}"` (root-level `.env.example` primary-gate)
    - Phase 4 entries (has_backend, has_db) are unchanged
    - No COMPOSITE-condition _exclude entry (no `{% if not has_llm or llm_backend ... %}`) — separate entries only
  </acceptance_criteria>
  <done>copier.yml _exclude block gates every LLM-only path; YAML parses; Phase 4 polarity tests still pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append has_llm dependency block to pyproject.toml.jinja2</name>
  <files>template/pyproject.toml.jinja2</files>
  <read_first>
    - template/pyproject.toml.jinja2 (current Phase 4 has_backend block — gives exact indentation + Jinja shape to mirror)
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "Standard Stack" — exact version pins; section "Pitfall 4" — pydantic-ai>=1.100 NOT v0.x)
    - .planning/phases/05-llm-add-on/05-CONTEXT.md (D-22 — tokenx-core dropped)
  </read_first>
  <action>
    Append a `{% if has_llm %}` block inside the `[project] dependencies = [ ... ]` array, placed AFTER the existing `{% if has_backend %} ... {% endif %}` block. Use the exact version pins from RESEARCH.md "Standard Stack" table, **MINUS tokenx-core (dropped per D-22)**: pydantic-ai>=1.100,<2 | instructor>=1.15,<2 | litellm>=1.85,<2 | tokencost>=0.1.26 | autoevals>=0.2,<0.3 | opentelemetry-instrumentation-httpx>=0.63b1 | traceloop-sdk>=0.60,<1 | claude-agent-sdk>=0.2.83 | langfuse>=4.6,<5. Also append a `{% if has_llm %}` block inside the EXISTING `[project.optional-dependencies] dev = [ ... ]` list (at template/pyproject.toml.jinja2:59-78, AFTER the existing `{% if has_backend %} ... {% endif %}` block at lines 64-71 and the `{% if has_backend and has_db %}` block at lines 72-75) with vcrpy>=8.1,<9 and pytest-recording>=0.13.4. **CRITICAL — REVIEW-CHECKLIST §4 anti-drift:** do NOT introduce a `[dependency-groups]` table; that table does NOT exist in this repo. The Phase 4 dev-deps convention is `[project.optional-dependencies].dev` and Phase 5 MUST match. Do NOT add `tokenx` (the 2018 stub) NOR `tokenx-core` (dropped per D-22 — redundant with our own @llm_call). Do NOT use pydantic-ai<1.0 (would lack .output attribute per Pitfall 4). Per REVIEW-CHECKLIST §6, no meta-comments (no "cycle-N", "per Codex").
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-01-deps && uv run --no-project python -c "
import subprocess, sys, pathlib
p = pathlib.Path('/Users/moiz/Documents/code/verify-kit')
subprocess.run(['copier','copy','--trust','--defaults','--data','has_llm=true','--data','has_backend=true','--vcs-ref','HEAD',str(p),'/tmp/scratch-05-01-deps'], check=True)
text = pathlib.Path('/tmp/scratch-05-01-deps/pyproject.toml').read_text()
assert 'pydantic-ai' in text and ('>=1.100' in text or '>=1.10' in text), 'pydantic-ai pin missing or wrong'
import re
# D-22 regression guard: NO tokenx in any form (bare or -core)
bad_bare = re.search(r'\"tokenx[\"<>=,]', text)
assert not bad_bare, f'bare tokenx found: {bad_bare.group(0)}'
assert 'tokenx-core' not in text, 'tokenx-core was DROPPED per D-22; remove from deps'
assert '[project.optional-dependencies]' in text, 'expected dev deps under [project.optional-dependencies] (matches Phase 4 convention)'
assert '[dependency-groups]' not in text, 'do NOT introduce [dependency-groups] table — this repo uses [project.optional-dependencies].dev (REVIEW-CHECKLIST §4)'
assert 'vcrpy' in text and 'pytest-recording' in text, 'LLM dev deps (vcrpy, pytest-recording) must land under [project.optional-dependencies].dev'
# All 9 runtime LLM deps must be present
for pkg in ['pydantic-ai','instructor','litellm','tokencost','autoevals','opentelemetry-instrumentation-httpx','traceloop-sdk','claude-agent-sdk','langfuse']:
    assert pkg in text, f'{pkg} missing from rendered pyproject'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - has_llm=true scratch render contains all 9 runtime LLM packages: pydantic-ai, instructor, litellm, tokencost, autoevals, opentelemetry-instrumentation-httpx, traceloop-sdk, claude-agent-sdk, langfuse
    - has_llm=true scratch render contains `pydantic-ai>=1.100` (or `>=1.10`-prefixed range that resolves to 1.100+)
    - has_llm=true scratch render does NOT contain `tokenx` in any form (bare OR `-core`) per D-22
    - has_llm=true scratch render's pyproject.toml contains `[project.optional-dependencies]` (extended) but does NOT contain a `[dependency-groups]` table heading
    - has_llm=true scratch render's `dev` list under `[project.optional-dependencies]` contains both `vcrpy` and `pytest-recording`
    - has_llm=false scratch render contains zero of: pydantic-ai, instructor, litellm, autoevals, traceloop-sdk, claude-agent-sdk
  </acceptance_criteria>
  <done>pyproject.toml.jinja2 has both has_llm dependency blocks (9 runtime + 2 dev); scratch render verifies; no tokenx in any form per D-22.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Append LLM credentials block to .env.example.jinja2</name>
  <files>template/{% if has_backend %}app{% endif %}/.env.example.jinja2</files>
  <read_first>
    - template/{% if has_backend %}app{% endif %}/.env.example.jinja2 (existing Phase 4 file — append, do not rewrite per REVIEW-CHECKLIST §4). VERIFIED present (178 bytes; ENV=dev / DATABASE_URL / PROFILE_ENABLED / LOGFIRE_TOKEN as of HEAD). The Cycle-2 reviewer's `rg --files template | rg 'env|\.env'` missed this file because rg's path expansion does not match Jinja-braced directory segments; do NOT use that grep shape — verify with `ls "template/{% if has_backend %}app{% endif %}/.env.example.jinja2"` instead.
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "LLM-10 — Three-backend OTLP config" for exact env var names; section "Pitfall 8" — Langfuse uses Basic Auth, NOT Bearer)
    - .planning/phases/05-llm-add-on/05-CONTEXT.md (D-02, D-06, D-08, D-11)
  </read_first>
  <action>
    **Precondition (cycle-2 HIGH #1):** Before editing, run `ls "template/{% if has_backend %}app{% endif %}/.env.example.jinja2"` and confirm exit 0. If the file is absent (e.g., a prior workspace reset removed it), STOP and re-create it with the Phase 4 baseline content (ENV=dev / LOG_LEVEL=INFO / LOG_FORMAT=auto / DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/app / PROFILE_ENABLED=true / `{% if has_logfire %}LOGFIRE_TOKEN=\n{% endif %}`) before appending the LLM block — do NOT silently invent it. This task remains "append, do not rewrite" against the canonical Phase 4 baseline.

    Read existing .env.example.jinja2 (Phase 4 owns it; this plan APPENDS only). Append a `{% if has_llm %}` block at the end. The block contains four sub-sections: (1) LLM credentials — ANTHROPIC_API_KEY (D-02), OPENAI_API_KEY, USE_CLAUDE_CODE_SDK (D-03 toggle, blank default); (2) Langfuse observability — inside `{% if llm_backend == "langfuse-cloud" %}` write LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_HOST=https://cloud.langfuse.com, OTEL_EXPORTER_OTLP_ENDPOINT=https://cloud.langfuse.com/api/public/otel, plus a comment line documenting the Basic-Auth shell idiom `OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic $(echo -n "$LANGFUSE_PUBLIC_KEY:$LANGFUSE_SECRET_KEY" | base64)` (per Pitfall 8 — Basic Auth, NOT Bearer); (3) `{% elif llm_backend == "langfuse-self-host" %}` branch with LANGFUSE_HOST=http://localhost:3000 and matching OTLP endpoint; (4) `{% else %}` (none) branch with `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317` for Jaeger fallback per D-08; (5) Eval gating — EVAL_MODEL=claude-haiku-4-5 (D-10) and EVAL_BUDGET_USD=1.00 (D-11). Per REVIEW-CHECKLIST §5, put each `{% if/elif/else %}` on its own line so conditional content does not collapse adjacent lines. Per REVIEW-CHECKLIST §4, do not invent new env var names — every name must appear in RESEARCH.md section "LLM-10 — Three-backend OTLP config".
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-01-env-{cloud,self,none,noLLM} && uv run --no-project python -c "
import subprocess, pathlib
src = '/Users/moiz/Documents/code/verify-kit'
for name, data in [('cloud', {'has_llm':'true','has_backend':'true','llm_backend':'langfuse-cloud'}),
                   ('self',  {'has_llm':'true','has_backend':'true','llm_backend':'langfuse-self-host'}),
                   ('none',  {'has_llm':'true','has_backend':'true','llm_backend':'none'}),
                   ('noLLM', {'has_llm':'false','has_backend':'true'})]:
    args = ['copier','copy','--trust','--defaults','--vcs-ref','HEAD']
    for k,v in data.items(): args += ['--data', f'{k}={v}']
    args += [src, f'/tmp/scratch-05-01-env-{name}']
    subprocess.run(args, check=True)
    env = pathlib.Path(f'/tmp/scratch-05-01-env-{name}/app/.env.example').read_text()
    if name == 'cloud':
        assert 'cloud.langfuse.com/api/public/otel' in env and 'Basic' in env, f'cloud branch wrong'
        assert 'Bearer' not in env, 'Pitfall 8: should be Basic, not Bearer'
    elif name == 'self':
        assert 'localhost:3000' in env, 'self-host branch wrong'
    elif name == 'none':
        assert 'localhost:4317' in env and 'cloud.langfuse.com' not in env, 'none branch wrong'
    elif name == 'noLLM':
        assert 'ANTHROPIC_API_KEY' not in env and 'LANGFUSE' not in env and 'EVAL_BUDGET_USD' not in env
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - **Precondition (cycle-2 HIGH #1):** `template/{% if has_backend %}app{% endif %}/.env.example.jinja2` exists at task start (verified by `ls`, not by `rg --files`). If missing, the Phase 4 baseline content is re-created before append (NOT silently invented).
    - llm_backend=langfuse-cloud renders cloud.langfuse.com/api/public/otel endpoint and a Basic Auth idiom comment (NOT Bearer)
    - llm_backend=langfuse-self-host renders localhost:3000/api/public/otel
    - llm_backend=none renders localhost:4317 (Jaeger) and does NOT contain cloud.langfuse.com
    - has_llm=false renders NO ANTHROPIC_API_KEY, NO LANGFUSE_*, NO EVAL_BUDGET_USD
    - Each `{% if/elif/else %}` is on its own line (REVIEW-CHECKLIST §5)
    - The Phase 4 baseline content (ENV / LOG_LEVEL / LOG_FORMAT / DATABASE_URL / PROFILE_ENABLED / has_logfire LOGFIRE_TOKEN) is preserved verbatim — diff against pre-edit version shows ONLY an appended `{% if has_llm %}` block, never a rewrite.
  </acceptance_criteria>
  <done>.env.example.jinja2 has appended has_llm block with three-branch llm_backend wiring; four polarity renders verify.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Add root-level .env.example.jinja2 for the (has_llm=true AND has_backend=false) cell</name>
  <files>template/{% if has_llm and not has_backend %}.env.example{% endif %}.jinja2</files>
  <read_first>
    - template/{% if has_backend %}app{% endif %}/.env.example.jinja2 (after Task 4 — this is the CANONICAL LLM env block; Task 5 copies the same block to the root location for the no-backend cell)
    - .planning/phases/05-llm-add-on/05-RESEARCH.md (section "LLM-10 — Three-backend OTLP config")
  </read_first>
  <action>
    Create template/{% if has_llm and not has_backend %}.env.example{% endif %}.jinja2 (Shape 2 filename-level gate — root of template/). This file ONLY renders in the (has_llm=true AND has_backend=false) cell. Its body contains EXACTLY the same LLM credentials + three-branch llm_backend wiring + eval gating block that Task 4 appended to app/.env.example.jinja2 — NOT additionally wrapped in `{% if has_llm %}` (the path-gate already enforces has_llm=true; the path-gate already enforces NOT has_backend). 

    The file MUST contain the same three llm_backend branches as Task 4 (cloud / self-host / none), the same ANTHROPIC_API_KEY / OPENAI_API_KEY / USE_CLAUDE_CODE_SDK slots, and the same EVAL_MODEL / EVAL_BUDGET_USD slots — so that consumers in the LLM-only cell get a complete env contract, not a degraded subset.

    Per REVIEW-CHECKLIST §3 cross-plan contract drift, both env files MUST stay identical in body content (copy from Task 4 verbatim — do not let one drift from the other). The polarity test in 05-05 Task 4 will forcing-function this by parametrizing over the four (has_backend × has_llm) cells and asserting the LLM env block lands in exactly the right destination per cell.

    Per REVIEW-CHECKLIST §5, each `{% if/elif/else %}` (for llm_backend branches inside the file body) on its own line.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scratch-05-01-rootenv-{llmonly,llmbackend,nollm,nolm-nobackend} && uv run --no-project python -c "
import subprocess, pathlib
src='/Users/moiz/Documents/code/verify-kit'
def render(name, data):
    args = ['copier','copy','--trust','--defaults','--vcs-ref','HEAD']
    for k,v in data.items(): args += ['--data', f'{k}={v}']
    args += [src, f'/tmp/scratch-05-01-rootenv-{name}']
    subprocess.run(args, check=True)

# (has_llm=true AND has_backend=false) — ROOT .env.example MUST exist
render('llmonly', {'has_llm':'true','has_backend':'false','llm_backend':'langfuse-cloud'})
root = pathlib.Path('/tmp/scratch-05-01-rootenv-llmonly/.env.example')
assert root.exists(), 'root .env.example missing in (has_llm=true, has_backend=false) cell'
text = root.read_text()
for key in ['ANTHROPIC_API_KEY','LANGFUSE','EVAL_MODEL','EVAL_BUDGET_USD']:
    assert key in text, f'{key} missing from root .env.example'

# (has_llm=true AND has_backend=true) — ROOT .env.example MUST NOT exist (app/.env.example is the destination)
render('llmbackend', {'has_llm':'true','has_backend':'true','llm_backend':'langfuse-cloud'})
assert not pathlib.Path('/tmp/scratch-05-01-rootenv-llmbackend/.env.example').exists(), 'root .env.example should NOT leak when has_backend=true (app/.env.example is the destination)'

# (has_llm=false AND has_backend=true) — ROOT .env.example MUST NOT exist
render('nollm', {'has_llm':'false','has_backend':'true'})
assert not pathlib.Path('/tmp/scratch-05-01-rootenv-nollm/.env.example').exists()

# (has_llm=false AND has_backend=false) — ROOT .env.example MUST NOT exist
render('nolm-nobackend', {'has_llm':'false','has_backend':'false'})
assert not pathlib.Path('/tmp/scratch-05-01-rootenv-nolm-nobackend/.env.example').exists()
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Root-level .env.example exists ONLY in the (has_llm=true AND has_backend=false) cell
    - Root-level .env.example contains ANTHROPIC_API_KEY, LANGFUSE_*, EVAL_MODEL, EVAL_BUDGET_USD (same content as app/.env.example)
    - Root-level .env.example is ABSENT in the (has_llm=true AND has_backend=true) cell (app/.env.example is the canonical destination)
    - Root-level .env.example is ABSENT in both has_llm=false cells
    - Body content of root .env.example matches Task 4's app/.env.example content (no drift; REVIEW-CHECKLIST §3)
  </acceptance_criteria>
  <done>LLM env destination exists in every (has_llm=true) cell; no cell is left without a destination; no cell renders both env files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pypi.org → uv install | Untrusted package registry; supply-chain attack surface |
| .env.example template → consumer .env file | Documentation of credential slots; never contains real secrets |
| copier.yml _exclude → render output | Misconfiguration risks LLM artifacts leaking into has_llm=false renders (polarity failure, not security failure, but inflates attack surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | uv installs of 12 LLM packages | mitigate | Task 1 slopcheck checkpoint:human-verify-blocking gates the install; tokenx-core name pin prevents 2018-stub typosquat |
| T-05-02 | Information Disclosure | .env.example documenting secret slots | accept | File contains BLANK values only; pattern from Phase 4 is well-established (gitleaks pre-commit gates real .env) |
| T-05-03 | Spoofing | claude-agent-sdk supply chain | mitigate | Verified as Anthropic-official PyPI publisher in slopcheck task; pinned >=0.2.83 |
| T-05-SC | Tampering | npm/pip installs from upstream | mitigate | Task 1 blocking-human checkpoint per Package Legitimacy Gate protocol; all 12 packages [ASSUMED] until verified |
</threat_model>

<verification>
After all tasks complete:
1. `python -c "import yaml; yaml.safe_load(open('copier.yml'))"` exits 0
2. Phase 4 polarity tests still pass: `uv run pytest tests/test_phase04_scaffold_polarity.py -v`
3. Four-cell polarity render matrix passes (cloud / self / none / noLLM) as verified in T4 automated
4. Scratch render with has_llm=true contains tokenx-core (NOT tokenx) in pyproject.toml
5. Scratch render with has_llm=false produces zero LLM dependencies
</verification>

<success_criteria>
- copier.yml _exclude gates all 11 LLM paths under `{% if not has_llm %}` (the eleventh is the root `.env.example` per cycle-2 HIGH #3)
- pyproject.toml.jinja2 has has_llm production-deps inside `[project] dependencies` AND has_llm dev-deps inside the EXISTING `[project.optional-dependencies] dev` list (NOT under `[dependency-groups]` — that table is forbidden)
- LLM env destination exists in every (has_llm=true) cell: app/.env.example.jinja2 when has_backend=true, root .env.example.jinja2 when has_backend=false
- Both env destinations carry IDENTICAL LLM content (three-branch llm_backend wiring per choice)
- No cell renders BOTH env files (mutually exclusive path-gates)
- Polarity contract holds: has_llm=false → zero LLM env vars, zero LLM dependencies, neither env destination present
- No `tokenx` (without `-core`) appears anywhere in the rendered output
- No `Bearer` appears in the Langfuse OTLP auth idiom (must be `Basic`)
- Phase 4 polarity tests continue to pass unchanged
</success_criteria>

<output>
Create `.planning/phases/05-llm-add-on/05-01-SUMMARY.md` when done. The SUMMARY MUST include:
- The full list of _exclude entries added (so downstream plans 05-02..05-05 reference them by file:line)
- The version pins as actually committed (so 05-02 can import against them)
- A polarity matrix table showing the four cells and what each renders
</output>
