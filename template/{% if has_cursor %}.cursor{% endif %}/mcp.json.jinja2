{
  // Cursor MCP server registration for verify-kit. See https://docs.cursor.com/context/mcp.
  "mcpServers": {
    "verify-kit": {
      "command": "verify-kit",
      "args": ["mcp", "serve"],
      "env": {}
    }
  }
}
