{
  // Zed AI context server registration for verify-kit.
  "context_servers": {
    "verify-kit": {
      "source": "custom",
      "command": "verify-kit",
      "args": ["mcp", "serve"],
      "env": {}
    }
  }
}
